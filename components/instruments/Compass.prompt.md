Heading compass rose (SVG) with a centred mono readout.

```jsx
<Compass heading={tel.heading} size={200} target={bearingToHome} />
```

Rotating card (N in red), fixed amber lubber line, `nnn` heading in the centre. Pass `target` to show a blue bearing marker (e.g. bearing to home/target).
