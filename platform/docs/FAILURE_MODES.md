# Failure modes and effects analysis

This is the authoritative failure catalogue for the system: satellite change
detection → LLM planner → deterministic `MissionVerifier` → operator approval →
companion orchestrator → ArduPilot. It records 178 modes across every subsystem
and every seam between them.

Runtime paths are relative to [`platform/`](../platform/) unless the row names a
repository-root directory (`hub/`, `contracts/`, `argus-core/`, `console/`,
`sim/`, `scripts/`). Companion evidence is relative to
`platform/companion/src/eis_companion/`; ground evidence to `platform/ground/`.

Companion behaviour rows are measured against the response contract in
[§2](#2-intended-response-contract-normative). Related documents:
[`SITE_CONTRACT.md`](SITE_CONTRACT.md) (site-model schema and cutover),
[`THREAT_MODEL.md`](THREAT_MODEL.md), [`ADR-hackathon.md`](ADR-hackathon.md)
(why the design is shaped this way), [`SESSION_AUDIT.md`](SESSION_AUDIT.md)
(what exists), [`REPOSITORY_REVIEW.md`](REPOSITORY_REVIEW.md) (merge layout).

---

## 1. How to read this document

### Severity — what it costs if the mode fires

| Level | Meaning |
|---|---|
| `critical` | Puts the aircraft outside its safety envelope, breaks the trust chain the system is built on (verification, containment, provenance), or ends the demonstration with no in-place recovery. |
| `high` | Defeats a documented safety control, silently hands the operator a wrong result they will act on, or needs a restart to recover. |
| `medium` | Degrades behaviour or observability; recoverable in place by an operator who knows what happened. |
| `low` | Cosmetic, latent, or already bounded by an independent control. |

Severity is judged on the *worst credible* outcome of the mode, not the typical
one. A NaN that becomes a full-scale forward command is `critical` even though
it needs a malformed frame to trigger.

### Likelihood — how often it fires, given the system is exercised

Conditional on a full run of the mission chain, not per flight-hour.

| Level | Meaning |
|---|---|
| `likely` | Expected on a normal run — or already unconditionally true of the code. A permanently dead branch, a check that can never pass, a build artefact nothing produces: all `likely`. |
| `possible` | Needs a specific but realistic trigger: a sensor dropout, a port collision, a config choice, an operator sequence. |
| `rare` | Needs a malformed input, a hostile client, or two independent faults coinciding. |

### `✱` — demo risk

`✱` on the id marks a mode that can surface during a live run in front of an
audience. It is **orthogonal to severity**: a `medium ✱` mode that reliably
stalls the ground station is more urgent on the day than a `critical` mode that
needs a hand-crafted `NaN` frame. Every `✱` mode has an entry in
[§19 Demo-day contingency](#19-demo-day-contingency) giving its on-stage symptom
and the operator's recovery move.

### Mitigation-cell markers

| Marker | Meaning |
|---|---|
| `✔` | Mitigation claim was adversarially code-checked and **upheld**. The control exists, is reachable on the production path, and does what it claims. |
| `◐` | Mitigation is **half implemented**: the part needing no wire change is done and tested, and the `gap` cell names the exact contract change the rest requires. |
| `✖` | The mode is real but is **not this stack's to fix** (another team's component, a launcher, CI). The mitigation cell says where the operator's move is documented instead. |
| `✗` | Mitigation claim was adversarially code-checked and **refuted**. The mode is unmitigated; the anatomy of the refutation is in [§17](#17-verified-mitigations). |
| *(none)* | Mitigation is recorded but was not adversarially checked (below the `high` threshold). Treat as an assertion, not evidence. |
| `None.` | No mitigation exists. The cell carries the evidence that establishes the mode. |

Only `high` and `critical` mitigation claims were code-checked, so only those
carry `✔`/`✗`. See [§17](#17-verified-mitigations) for what the check consisted
of and which claims did not survive it.

### The `gap` column

`gap` is what remains after any mitigation: the specific control that is missing,
unreachable, or asserted rather than implemented. It is written so that closing
the gap is a defined change, not a research task.

---

## 2. Intended response contract (normative)

This is the specification the companion and ground layers are measured against.
Each row is one phase of one failure. `Response` carries exactly one wire/UI
failsafe state: `hold`, `rtl`, `escalate`, or `refuse`. Multi-stage failures get
separate rows so authority and assertions stay unambiguous. The UI displays the
state and the reason from the named authority.

Where the implementation does not meet a row, the FMEA row that records the
divergence names the contract row in its `gap` cell.

| Failure | Phase | Detection | Response | Authority | SITL/scripted injection | Required e2e assertion |
|---|---|---|---|---|---|---|
| Vehicle datalink loss | Initial watchdog window | Companion receives no ground heartbeat inside its configured watchdog window; commanded velocity is zeroed | `hold` | Companion watchdog | `--link-loss` drops the control WebSocket | State becomes `hold` within 5 s, setpoint is zero, position remains inside fence and standoff is not breached |
| Vehicle datalink loss | GCS failsafe timeout | ArduPilot GCS failsafe observes continued heartbeat loss | `rtl` | ArduPilot firmware | Keep the link dropped beyond the firmware timeout | Mode becomes RTL and vehicle returns without a new ground command |
| Planner heartbeat loss | Active planner mission | Companion misses planner heartbeat while vehicle link remains healthy | `hold` | Companion planner watchdog | Stop `planHeartbeat` only | State becomes `hold` at current standoff within 5 s |
| Planner heartbeat loss | Subsequent command | A `planCommand` arrives while planner heartbeat is stale | `refuse` | Companion planner executor | Send a command after heartbeat cutoff | Ack is rejected with stale-heartbeat reason and no setpoint changes |
| LLM timeout, malformed output, or refusal | Scripted recovery available | 20 s deadline, schema parse, or refusal check fails | `hold` | Ground planner host | Inject timeout, invalid JSON, and refusal fixtures | ScriptedPlanner is selected without waiting on network; any airborne vehicle stays held until its verified plan is accepted |
| LLM timeout, malformed output, or refusal | Scripted recovery unavailable or invalid | Scripted fallback cannot produce a schema-valid plan | `escalate` | Ground planner host | Make both live and scripted planner fixtures fail validation | Operator event names the LLM/fallback error and the planner emits no executable plan |
| Verifier rejects twice | Second deterministic rejection | Same request fails initial verification and the one allowed repair attempt | `escalate` | Ground MissionVerifier | Feed two invalid plans for one request | Operator escalation contains both check lists; there is no third LLM attempt |
| GPS loss | In flight, healthy fallback pending | EKF/GPS health vote fails for 2 s | `hold` | Companion navigation manager | Disable `SIM_GPS1_ENABLE` or set legacy `SIM_GPS_DISABLE=1` | Vehicle holds while source vote runs; ground cannot command a source switch |
| GPS loss | New mission | `navSource != gps` after GPS vote failure | `refuse` | Ground MissionVerifier and companion re-clamp | Attempt dispatch while GPS is disabled | Mission is refused with `nav_source` reason even if extnav or optflow is healthy |
| GPS loss with healthy LiDAR-inertial odometry | Source selection | GPS vote fails and extnav freshness/covariance checks pass | `hold` | Companion navigation manager | Disable GPS and inject healthy LIO | `MAV_CMD_SET_EKF_SOURCE_SET` selects extnav, telemetry reports `extnav`, and no health claim is made before evidence passes |
| GPS loss with healthy optical flow only | Source selection | GPS vote fails, extnav is unhealthy, flow quality ≥ 50, and velocity innovation ≤ 0.15 m/s | `hold` | Companion navigation manager | Disable GPS/LIO and inject qualifying flow | Source changes to optflow; sub-threshold quality or excessive innovation never reports optflow healthy |
| GPS loss plus GNSS interference | Correlation window | `navSource` leaves GPS and a `gnss_interference` event occurs within 60 s | `escalate` | Ground health correlator | Disable GPS, then inject SDR event inside 60 s | One `probable interference` health event appears within 5 s with both evidence IDs |
| Hostile drone | Pre-flight inside geofence or buffered route | `hostile_drone` RF event intersects geofence or route buffer | `refuse` | Ground MissionVerifier `airspace` check | Inject hostile-drone fixture before approval | Verification refuses and approve remains disabled with airspace reason |
| Hostile drone | In flight | Valid hostile-drone event enters active geofence | `hold` | Companion/ground airspace monitor | `--hostile-drone` during orbit | Vehicle holds within 5 s; only operator continue or operator RTL can leave the state |
| Battery below reserve | In flight | Lower authoritative SoC estimate crosses reserve/return threshold | `rtl` | Companion battery watchdog | Drain `SIM_BATT` | RTL preempts planner within 5 s and audit reason names reserve |
| Sortie timer | In flight | Elapsed time reaches `max_sortie_s - estimated_return_time_s` | `rtl` | Companion sortie watchdog | `--sortie-cap SECONDS` | RTL begins before cap; planner heartbeat cannot override it |
| Pack fault, cell imbalance, or over-temperature | On pad | Fault bit, cell delta > 0.10 V, or temperature > 60 °C | `refuse` | Companion arming gate and verifier readiness check | `--battery-fault` with each fault variant | Arm/dispatch is refused and UI lists the exact threshold failure |
| Pack fault, cell imbalance, or over-temperature | In flight | Same pack health checks become unsafe after takeoff | `rtl` | Companion battery watchdog | Inject fault after takeoff | RTL preempts planner and records the measured fault |
| Charge stalled or regressing | On pad | Requested charge gains < 0.5 percentage points in 15 s or falls > 0.5 points | `refuse` | Companion charge-state monitor | Freeze or reverse scripted charge curve | `battery charge_stalled` is emitted and readiness remains false |
| SoC estimate divergence | On pad | Coulomb and voltage estimates differ by > 10 percentage points | `refuse` | Companion battery estimator | Inject divergent estimates above and below readiness | Lower estimate is used, state is `degraded_estimate`, and dispatch stays disabled pending a trustworthy estimate |
| SoC estimate divergence | In flight above reserve | Same divergence occurs while the lower estimate remains above return threshold | `escalate` | Companion battery estimator | Inject divergence without crossing reserve | Lower estimate drives remaining time and a degraded-estimate event is shown; crossing reserve is covered by the battery-reserve row |
| Wind above limit | First detection | Wind estimate exceeds 12 m/s | `hold` | Companion safety monitor | Inject wind above threshold | Hold occurs within 5 s and no new mission leg starts |
| Wind above limit | Persistent/unsafe return decision | Above-limit wind persists for the configured confirmation period | `rtl` | Companion safety monitor | Keep injected wind above threshold | RTL starts and cannot be cancelled by planner output |
| Bad setpoint reaches flight controller | Firmware backstop | Raw env-gated test command targets outside loaded fence | `rtl` | ArduPilot native fence | Enable test hook and inject out-of-fence target | Configured fence action is observed, telemetry/audit identify firmware authority, maximum geofence excursion is measured, and the vehicle remains inside the outer perimeter containment margin |
| Camera or YOLO failure | Recognition | RGB capture is missing/stale or detector raises; no valid observation frame exists | `escalate` | Recognizer | `--camera-fail` | Incident says `no observation`; it does not describe an empty valid frame as sensor failure |
| Camera or YOLO failure | Mission terminal | Recognition completes with no observation | `rtl` | Companion mission executor | Continue camera-fail scenario | Mission still returns home after the escalation |
| Thermal failure | Pre-flight night mission | `simulateNight` and thermal readiness is not `ok` | `refuse` | Ground verifier readiness check and companion gate | `--night --thermal-fail` | Dispatch is refused with thermal-at-night reason |
| Thermal failure | In flight | Thermal rail becomes stale/failed after takeoff | `escalate` | Recognizer | Fail thermal during active mission | RGB/LiDAR evidence continues, report is marked degraded, and no thermal detections are fabricated |
| LiDAR failure | Pre-flight clutter route | Route intersects `clutter` while LiDAR readiness is not `ok` | `refuse` | Ground verifier readiness check | `--lidar-fail` before clutter mission | Dispatch is refused with LiDAR/clutter reason |
| LiDAR failure | In flight | LiDAR freshness/health fails after takeoff | `hold` | Companion obstacle/navigation manager | Fail LiDAR during active route | Avoidance disables, vehicle holds while climbing to `clear_altitude_m`, and only then continues outside clutter |
| Modality disagreement | Recognition | Associated thermal/RGB/LiDAR tracks disagree within fusion gate | `escalate` | Recognizer | Thermal-person fixture with no matching LiDAR cluster | Both modality tracks remain, fused confidence is reduced by 0.15, and low-confidence policy escalates |
| Companion crash | In flight | MAVLink/GCS heartbeat from companion disappears | `rtl` | ArduPilot firmware | Terminate companion process | Firmware enters RTL without ground involvement |
| Ground station crash | Active outbound/observation leg | Companion loses both ground and planner heartbeats | `hold` | Companion watchdog | Terminate Electron host during active leg | Current safe hold policy persists and local audit remains recoverable on reconnect |
| Ground station crash | Return already required | Companion loses ground while sortie/battery/fence policy already requires return | `rtl` | Companion watchdog and ArduPilot firmware | Terminate ground station after return trigger | Existing RTL continues; reconnect resyncs state without replaying commands |
| Vehicle lost mid-mission | Fleet view | Vehicle heartbeat and telemetry disappear beyond loss timeout | `escalate` | Ground fleet monitor | Drop one vehicle fixture permanently | Operator alert names `vehicleId`; no automatic fleet reallocation occurs |
| Mesh partition | Any | Mesh adapter reports partition or peer timeout | `escalate` | Ground fleet monitor | Script a mesh-partition health event | Event is documented and visible; no invented peer health or reallocation occurs |
| Invalid `site.json` | Pre-flight | Site parser or geometry invariants fail | `refuse` | Ground MissionVerifier `site_valid` check | Select malformed/missing-field site fixture | Every mission is refused with validation detail and no fallback occurs for an explicitly selected path |
| False-positive anomaly | Mission terminal | Valid observation supports `false_alarm` | `rtl` | Recognizer and companion mission executor | Inject false-alarm staging truth | Report verdict is `false_alarm`, audit is retained without incident escalation, and vehicle returns home |
| Manual engage | Any automated mode | Acked operator manual-engage command arrives | `hold` | Companion control arbiter | Engage manual during planner/tracking mode | Automation is released immediately, one control source remains active, and zero/hold precedes fresh manual input |

### Observation-state invariant

A healthy sensor frame containing zero tracks is a valid observation and may
support `false_alarm`. `No observation` is reserved for a missing, stale, failed,
or invalid sensor frame. The recognizer, UI, fixtures, and reports must preserve
this distinction. `FM-100` is the standing violation.

### Authority invariant

The LLM never owns a response in the contract table. Ground validation may refuse
or escalate; companion watchdogs may hold, refuse, or RTL; ArduPilot firmware
owns the final fence and heartbeat RTL backstops. Manual engagement is operator
intent enforced by the companion arbiter. Every e2e injection must assert both
the state and the named authority, so a ground-side event cannot masquerade as a
firmware or companion action.

`FM-16`, `FM-15` and `FM-154` are the standing violations: the firmware half of
the authority chain is configured by documentation rather than by code, so
several rows above name an authority that is not armed at runtime.

---

## 3. Companion — failsafe ladder and watchdogs

The ladder is computed in `control/failsafe.py` and actioned in exactly one place
(`app.py:1236-1265`). Nothing else consumes `_failsafe_decision`, so any path
that skips those lines skips the entire ladder.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-01` ✱ | RTL latch is stamped before `set_mode`, so a failed RTL never retries | Failsafe decides `rtl` and `vehicle.set_mode("RTL")` returns `False` or raises | `_last_applied_failsafe` is set *before* the call, so every later 20 Hz tick skips the RTL block and sends hold frames only. The vehicle hovers in GUIDED while telemetry reports `failsafeState=rtl`. | critical | possible | ✔ The RTL latch is stamped only once the FC is OBSERVED in RTL. `_control_tick` calls `_assert_rtl`, which re-commands (throttled to 1 Hz) until `_observed_mode() == 'RTL'`; a refused `set_mode` sets `_last_applied_failsafe = 'rtl-pending:<reason>'` and raises an `rtl_refused` healthEvent. `app.py:_assert_rtl`, `_control_tick` rtl branch; companion/tests/test_failsafe_latches.py::test_a_refused_rtl_is_retried_instead_of_being_latched_as_done | Closed. The retry is throttled to 1 Hz so a refusing FC is not spammed; the hold frame is still emitted on every tick meanwhile. |
| `FM-03` ✱ | Ground-link deadman is one-shot and silences its own trigger | No ground heartbeat within `limits.ground_link_timeout_ms` while tracking/manual/planner is active | `_on_deadman` sets `_control_source = "auto"`, so `link_matters` and `_actual_datalink_lost()` are false from the next tick. Exactly one unverified RTL attempt is ever made; `failsafeState` reads `none` throughout a real outage; the `critical` status goes to zero clients. | high | likely | ✔ Link loss LATCHES in orchestrator state (`_link_lost_latched`), which survives the release to `auto`; `_actual_datalink_lost` reads the latch and no longer duplicates `evaluate_link`'s source gate; `_on_deadman` runs every tick, routes through `_release_all`, re-asserts RTL via `_assert_rtl`, emits an AUDITED `healthEvent` (replayed on reconnect), and the 1 Hz MAV_TYPE_GCS heartbeat STOPS so FS_GCS can fire. Only an authorized inbound frame clears the latch (`_note_ground_heartbeat`). companion/tests/test_failsafe_latches.py (6 deadman tests, all multi-tick) | Closed. `SafetyManager.evaluate_link` keeps its `link_matters` gate for ARMING the trip, so a companion nobody has connected to never latches. |
| `FM-04` ✱ | Takeoff window suspends the entire ladder for up to 60 s | Any takeoff; the control tick returns early while armed and `relAlt < 0.9 × target` | The early return precedes the failsafe block, so battery/sortie/wind RTL, GPS-denied hold, hostile-drone hold, LiDAR climb and site-invalid refuse are all inert during climb. The window is longest exactly when a climb is failing. | high | likely | ✔ The ladder is evaluated BEFORE the takeoff window, and the window now gates only the idle setpoint EMISSION. Any rtl/hold/refuse decision clears `_takeoff_deadline_ms`, and so do `_release_all` (rtl/land/disarm), the e-stop and the deadman. `app.py:_control_tick`, `_release_all`; companion/tests/test_failsafe_latches.py::test_the_failsafe_ladder_still_acts_during_the_takeoff_window | Closed. The 60 s bound is still a literal; it is now an upper bound on the SUPPRESSION of an idle frame, not on the ladder. |
| `FM-17` | LiDAR clear-altitude climb ignores `max_altitude` and only releases on the injected fault | LiDAR health fails while airborne | The climb targets `site.clear_altitude_m` with no intersection against `limits.max_altitude`, climbing through the altitude fence. A *real* LiDAR failure never sets `_lidar_clear_reached`, so the hold-and-climb latches permanently. | high | possible | ✔ `_lidar_clear_altitude_m()` intersects the site's `clear_altitude_m` with `limits.max_altitude` and returns None when there is no site, so the climb can never target above the altitude fence; the clear-altitude latch is now SYMMETRIC with the trip (a real `_lidar_observation_valid is False` satisfies it, not only the injected fault). companion/tests/test_failsafe_latches.py (3 LiDAR-climb tests) | Closed. The branch is still selected by matching the reason string -- a structural weakness of the ladder's string-typed reasons, not of this fix. |
| `FM-18` ✱ | Any failsafe state — including a notification-only `escalate` — locks out `abortPlan`, `disengageTracking` and `setMode` | `_failsafe_decision.state != "none"`, including camera/thermal/degraded-battery escalates that take no control action | The plan keeps commanding motion (plain `escalate` is deliberately not in the hold set) while the operator's least-disruptive stop commands are refused. A `soc_degraded` escalate can hold the lockout for the rest of the sortie. | high | likely | ✔ `_FAILSAFE_SAFE_COMMANDS` adds `abortPlan` and `disengageTracking` (both release-to-safe), and `setMode` is admitted for RECOVERY modes only (RTL/LAND/BRAKE/LOITER/ALT_HOLD). New autonomous work stays refused. companion/tests/test_failsafe_latches.py (4 lockout tests incl. a negative control) | Closed. In-flight `disarm` remains permitted with no airborne guard: it is the deliberate kill switch, and gating it would be the more dangerous change. |
| `FM-20` ✱ | Wind failsafe can never fire without a `WIND` message | FC does not stream `WIND`, or it goes stale | `wind_mps` degrades to `0.0`, indistinguishable from calm, so `wind_above_limit` and `wind_persistent` are permanently `False`. The hold-then-RTL wind ladder is dead outside the `--wind` injection. | medium | likely | ✔ `health_inputs` distinguishes absent/stale WIND (`wind_mps: None`, `wind_present: False`) from calm air; the orchestrator keeps a TRI-STATE `_wind_known`, publishes `wind_estimate_available` in capabilities, and REFUSES an unattended dispatch when the estimate is known to be absent -- nobody can take over up there and the unattended wind limit is half the attended one. companion/tests/test_link_and_sensor_staleness.py (3 wind tests) | Decision recorded: an absent estimate does not block ATTENDED flight (an operator is watching and the FC keeps its own limits); it blocks unattended dispatch and is reported. |
| `FM-21` ✱ | GPS fallback never actually flies — `decide()` holds on raw GPS health | GPS denied, extnav/optflow healthy, source switch voted and ACKed | The raw `gps_healthy` flag feeds `FailsafeSignals`, so `decide()` holds indefinitely on a healthy fallback, with no operator path out (`abortPlan` is locked out by `FM-18`). | medium | likely | Matches the specified behaviour: the contract names `hold` as the intended response for every GPS-loss row. `control/failsafe.py:82-83`; `app.py:837`; `control/nav_health.py:146-158` | `NavSnapshot.source`/`extnav_healthy`/`optflow_healthy` are never consulted, so fallback selection buys no operational capability — the source switch is observable but not actionable. |
| `FM-22` | `FailsafeSignals.manual_engaged` is hardcoded `False` — the manual-takeover hold is dead code | Always | `decide()`'s manual-takeover hold branch is unreachable. The documented hold never appears as a failsafe state in telemetry, audit or e2e assertions. | low | likely | `_engage_manual` force-releases tracking and the plan and re-stamps `controlSource`, so the *behaviour* is correct. `app.py:862`, `1655-1686`; `control/failsafe.py:94-95` | The state, reason and authority never surface. The contract's manual-engage row asserts a `hold` that no wire frame carries. |
| `FM-24` | `emergencyStop` is not latched — it self-clears in the same call | Any `emergencyStop` command | `_estop_latched` is set and cleared inside one coroutine, so commanding resumes on the next tick. If `land()`'s `set_mode` failed, the vehicle is back in GUIDED taking hold frames and any engage re-engages immediately. | high | possible | ✔ The latch is a STATE: `_dispatch` refuses everything but disarm/land/rtl/emergencyStop while it is set, and it clears only when the vehicle is OBSERVED disarmed and on the ground (`_maybe_clear_estop`). The vehicle action's result is checked, so a refused LAND acks failure, and the latch survives a raising release. companion/tests/test_failsafe_latches.py (5 e-stop tests) | Open contract gap (reported, not changed): the contract has no `clearEmergencyStop` command, so the acknowledgement is PHYSICAL (disarmed on the ground) rather than an explicit operator command. |
| `FM-32` | Failsafe gates are skipped entirely before the first health tick | Any command issued while `_failsafe_decision is None`; every gate reads `if decision is not None and …` | `_dispatch`, `_execute_plan` and `_handle_plan_command` all fall through their failsafe checks during startup. The `follow`/`orbit` branch additionally engages the planner with no readiness evidence at all. | medium | possible | `_do_arm` and `_execute_plan` call `_readiness_message()`, which refuses while `_battery_snapshot is None`; `_guidance_preconditions_ok` still gates motion. `app.py:990-993`, `1534-1540`, `1739-1742`, `995-1015`; `tests/test_app_wiring.py:233` | The None-means-pass idiom is repeated in three places and the `follow`/`orbit` branch has neither check. |
| `FM-141` ✱ | A second WebSocket client satisfies the ground-link deadman | A leftover browser tab, capture script, or `headless_client` connected alongside the Electron shell | Any inbound frame from any client feeds the heartbeat, so the organic watchdog never trips even after the operator window closes. Two clients can also both approve and dispatch. | medium | likely | The scripted `--link-loss` injection is an internal boolean OR'd with the real watchdog, so the scripted beat still fires. `app.py:147`, `835`, `956-966`; `api/server.py:108`, `173-186` | The server never distinguishes an authoritative operator from an observer. Documentation describes `--link-loss` as "drops the control WebSocket", which is not what the code does. |

---

## 4. Companion — MAVLink and flight-controller link

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-02` ✱ | `Vehicle.set_mode` never verifies the mode actually changed | Any `set_mode` ArduCopter refuses (RTL without home, GUIDED with unhealthy EKF, LAND while disarmed) or that is lost on the wire | `set_mode_send` is fire-and-forget and returns `True` unconditionally. Every rung of the failsafe ladder plus `engageManual` and `executePlan` believes it succeeded. | critical | possible | ✔ `Vehicle.set_mode` VERIFIES: it returns early if the FC is already in the mode, sends the legacy SET_MODE, escalates to COMMAND_LONG DO_SET_MODE, and confirms via COMMAND_ACK or a HEARTBEAT `custom_mode` readback, with retries. It returns False when nothing confirms. `vehicle.py:set_mode`, `_await_mode`, `_observed_custom_mode`; companion/tests/test_failsafe_latches.py::test_vehicle_set_mode_refuses_a_mode_the_fc_never_confirms | Closed. `verify=False` still exists for a caller that genuinely cannot block; nothing on the failsafe path uses it. |
| `FM-08` | Companion never detects FC/MAVLink link loss — stale telemetry served as live | Telemetry radio/UART/UDP path drops while the companion process stays alive | `_connected` stays `True` and cached `HEARTBEAT`/`GLOBAL_POSITION_INT` are re-stamped with a fresh `ts` forever. Control keeps commanding a dead socket; goto arrival and orbit accumulation freeze; the UI sees plausible, moving-timestamp, frozen telemetry. | high | possible | ✔ `Vehicle.fc_link_lost()` thresholds the heartbeat age (3 s) and a raising socket; `poll` records the exception instead of swallowing it; a heartbeat whose source id cannot be read is treated as FOREIGN. The state reaches telemetry (`fcLink`), `health_inputs`, and the ladder as `FailsafeSignals.fc_link_lost` -> hold, plus a healthEvent per transition. `_telemetry_from_state` no longer reports latencyMs 0.0. companion/tests/test_link_and_sensor_staleness.py (8 FC-link tests) | Closed. |
| `FM-09` ✱ | Blocking `recv_match` calls stall the 20 Hz loop and discard telemetry | `set_ekf_source` (1.5 s), `set_param` (≥3 s), `_wait_command_ack` (2 s) invoked from async paths | The single event loop blocks for the whole wait. `recv_match` also consumes and discards non-matching frames, so `VehicleState`, battery and GPS go stale exactly while a failsafe is being handled. | high | likely | ✔ Every blocking FC call runs off the event loop via `Companion._vehicle_call` (`asyncio.to_thread`), and `Vehicle._recv` no longer passes a type filter down to `recv_match`: it takes every frame, CACHES it, and matches itself, so an ack wait no longer discards the telemetry that arrived during it. companion/tests/test_link_and_sensor_staleness.py::test_a_command_ack_wait_caches_the_frames_it_consumes | Closed. |
| `FM-10` ✱ | EKF-source request storm when the FC does not ACK `SET_EKF_SOURCE_SET` | GPS vote fails, `NavHealth` requests extnav/optflow, FC never ACKs command 42007 | `confirm_source(accepted=False)` clears `_last_requested`, so the same blocking 1.5 s request re-issues every 10 Hz tick. Telemetry degrades to ~0.6 Hz and starves the control loop. | high | possible | ✔ `NavHealth` backs a refused switch off exponentially (2 s doubling, 30 s cap) and gives up after 5 unacknowledged attempts, reporting the source in `source_unavailable`. An accepted switch clears the history. companion/tests/test_link_and_sensor_staleness.py::test_a_refused_source_switch_backs_off_instead_of_re_issuing_every_tick | Closed. |
| `FM-16` ✱ | `failsafe_param_map` is defined and exported but never called — limits never reach the FC | Always | No code path invokes the map that derives `FENCE_ALT_MAX`/`FENCE_RADIUS`/`RTL_ALT`/`BATT_FS`/`FS_GCS` from `Limits`. The configured envelope is enforced only in software; the FC keeps its `.parm` or default values. | high | likely | ✔ `Vehicle.apply_failsafe_params` pushes the whole derived map at connect and VERIFIES each write, returning (applied, attempted, failures); the orchestrator calls it before the fence upload and reports a partial application as a warning + healthEvent. The circle radius is now derived from the SITE, and BATT_* from the configured cell count. companion/tests/test_containment.py::test_apply_failsafe_params_pushes_the_whole_derived_map | Closed. The firmware half of "clamped twice" is now armed by code. |
| `FM-145` ✱ | Companion continues in "degraded mode" with no flight controller instead of failing fast | Any FC connect failure at startup, including a post-bootstrap SITL restart race | The exception is swallowed and the full task graph starts with an unusable vehicle. Every command returns "no FC link", the fence upload is skipped, and the failure surfaces two minutes later as a readiness timeout. | medium | possible | Readiness reports "flight controller not ready"; arm and `planCommand` refuse; `_vehicle_action` returns a clean refusal; `connect`'s 30 s wait plus autoreconnect absorbs a restart race. `app.py:493-508`, `891-892`, `1615-1617`; `vehicle.py:163`, `193-199` | "Degraded" is indistinguishable from "starting up". No fatal-on-SITL mode and no `statusText` naming the failed connection string. |

---

## 5. Companion — control math, setpoints and manual authority

Four independent copies of a NaN-unsafe `_clamp` exist (`app.py:82-85`,
`guidance.py:161-163` and `:183-185`, `manual.py:226`). Under CPython
`min(hi, NaN)` returns `hi`, so every one of them converts a non-finite axis into
the **positive maximum** rather than zero. `planner_exec._clamp_setpoint`
(`planner_exec.py:658-666`) is the only correct implementation in the tree, and
it is on a path the manual and tracking sources never take.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-05` | `manualInput` NaN/Infinity becomes a full-scale stick command | A WS client sends `manualInput` with `NaN` or `Infinity` (`json.loads` accepts those literals by default) | `_clamp01(nan) == _clamp01(inf) == 1.0`, so the axis goes to full deflection: full climb, full forward, max yaw rate. `Vehicle._finite` never fires because the wire value it sees is already finite. | critical | rare | ✔ Three gates: the wire parser rejects the bare NaN/Infinity literals (`json.loads(parse_constant=_reject_constant)`), `_handle_manual_input` drops a frame with any non-finite axis WITHOUT refreshing the watchdog (so it ages into zero-and-hold), and `_clamp01` reads a non-finite axis as CENTRED. companion/tests/test_nonfinite_safety.py (property tests over the whole non-finite domain) | Closed. |
| `FM-06` | Orchestrator `_clamp_setpoint` converts a NaN axis into the maximum allowed command | Any component emits a setpoint with a non-finite axis and `valid=True` | `min(hi, nan)` returns `hi`, so each axis clamps to `+max_speed`/`+max_climb_rate`/`+max_yaw_rate` — all four at once. The final safety clamp is a NaN-to-full-throttle amplifier. | critical | rare | ✔ `app._clamp_setpoint` collapses the WHOLE setpoint to hold when any axis is non-finite (the `planner_exec` guard, ported), and all four `_clamp` copies (app, guidance, manual, pid) now return 0.0 for a non-finite input instead of the positive maximum. companion/tests/test_nonfinite_safety.py::test_orchestrator_clamp_collapses_any_non_finite_axis_to_hold | Closed. |
| `FM-07` | NaN estimated distance bypasses the hard standoff entirely | A detection with a NaN bbox height reaches the tracker, so `estimate_distance` returns NaN | All three standoff re-assertions are comparisons that NaN fails, so `vx` is never forced ≤ 0, and the NaN PID output clamps to `+max_speed`: a commanded full-speed approach *through* the standoff floor. The UI still renders a normal bbox. | critical | rare | ✔ Rejected at the SOURCE and at every guard: `estimate_distance` returns None for a non-finite bbox, the tracker drops non-finite detections at ingest and refuses a Kalman correction that would make the state non-finite, the three guidance standoff predicates use `_usable_distance` (isfinite, not a comparison), the PID refuses a non-finite error, and `_clip01` clips to 0.0. companion/tests/test_nonfinite_safety.py::test_guidance_never_commands_approach_on_a_non_finite_distance | Closed. Controls in the same file pin that a real distance still approaches and that an inside-standoff distance still backs off. |
| `FM-23` | `ManualPilot`'s own input watchdog can never trip on the orchestrator path | Always, while manual is engaged | `update()` feeds the retained `_pending` frame and `feed()` refreshes `_last_input_t` for any non-`None` input, so the age test is permanently zero. The layered watchdog exists but is unreachable. | medium | likely | `app.py`'s `_manual_setpoint` independently ages `_last_manual_input_ms` and calls `manual.reset()` before holding. `control/manual.py:123-132`, `160-175`; `app.py:1389-1409` | False redundancy: the tested watchdog does not cover the production call shape. |
| `FM-33` | `setMaxSpeed` can raise the live speed envelope to the 8 m/s hard cap | Any ground client sends `setMaxSpeed {mps: 8}` | The YAML's 2.0 m/s is not a runtime ceiling. Because `Limits` is shared by reference, the new cap immediately applies to guidance, manual, every planner leg and `goto_global`. | medium | possible | `MAX_SPEED_CAP` is enforced at the wire boundary and NaN falls back to the cap; documented as intended. `app.py:1585-1597`; `control/guidance.py:83-86`; `config.py:688` | No operator confirmation, no audit entry, no revert on disengage; `guidance.set_max_speed` itself has no ceiling. |
| `FM-34` | Takeoff altitude is not validated for finiteness or positivity | `takeoff` with NaN or negative altitude | `min(nan, max_altitude)` is NaN, so `NAV_TAKEOFF` goes on the wire with a NaN `param7` and `_takeoff_target_alt` becomes NaN. | low | rare | Capped at `limits.max_altitude`; the FC rejects an implausible takeoff and `_wait_command_ack` surfaces it; the NaN comparison closes the takeoff window immediately, which is the safe direction. `app.py:1549-1556`; `vehicle.py:896-909` | No `isfinite`/positivity check, unlike the velocity path and `goto_global` (`vehicle.py:1050-1055`). |

---

## 6. Companion — containment: geofence, NFZ and altitude

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-13` | No companion or firmware NFZ containment — NFZs are enforced only by the ground verifier | Any plan reaching the companion without ground verification (raw `executePlan`, teammate dispatch, replay, ground bug), or any `goto_relative` | `planner_exec` validates lat/lon range, altitude band and speed only, and has zero polygon awareness. Only the site perimeter is uploaded as an ArduPilot INCLUSION fence, so an NFZ breach *inside* the perimeter triggers nothing. | critical | possible | ✔ Two independent layers: `_plan_nfz_violation` refuses any dispatch whose target lies inside a buffered NFZ at or below its ceiling (site data in as plain data, polygon maths from the pure `control.envelope` helpers), and `upload_geofence(exclusions=...)` uploads every NFZ to the FC as a NAV_FENCE_POLYGON_VERTEX_EXCLUSION polygon alongside the inclusion perimeter. companion/tests/test_containment.py (NFZ upload + dispatch-refusal tests) | Closed. Overflight above a declared ceiling stays permitted; an UNKNOWN altitude is treated as inside the ceiling. |
| `FM-14` | Geofence upload reports success when `FENCE_ENABLE` was never acknowledged | `MISSION_ACCEPTED` but no `PARAM_VALUE` echo for `FENCE_ENABLE`/`FENCE_TYPE` | `upload_geofence` returns `True` and an info `statusText` claims the fence is live while it may be stored and disabled — the only lateral containment the vehicle has. | high | possible | ✔ `set_param` verifies the echoed `param_value`, not just `param_id`, so a refused or clamped write is detected; `upload_geofence` captures both results and records `fence_status()` = {stored, enabled, enforced, detail}. The orchestrator surfaces a stored-but-disabled fence as a CRITICAL status plus a healthEvent. companion/tests/test_containment.py::test_an_unacknowledged_enable_is_reported_as_stored_but_NOT_enforced | Closed. `upload_geofence` still returns True for a stored polygon (the transfer did succeed); `fence_status().enforced` is the authoritative answer. |
| `FM-15` ✱ | Fence upload failure gates nothing downstream | MISSION NACK, timeout, fewer than 3 vertices, out-of-range vertex, or no FC link at startup | One warning `statusText`, then arm/takeoff/plan dispatch proceed with no polygon fence and no companion containment, while the demo narrative claims native fence authority. | high | possible | ✔ Fence state GATES: `_fence_enforced is False` adds a readiness reason (so arming is refused) and refuses `executePlan` outright. `failsafe_param_map` is applied at connect, so FENCE_ACTION / FENCE_ALT_MAX / FENCE_RADIUS are armed by code rather than by a documentation table. companion/tests/test_containment.py::test_dispatch_is_refused_when_the_fence_is_not_enforced | Closed. `None` (upload never attempted, e.g. no FC layer that reports fence state) does not gate -- only a positive failure does. |
| `FM-31` | Degenerate altitude band collapses the mission onto the fence ceiling | `site.alt_band.min_m` meets or exceeds `limits.max_altitude` | `_alt_band` returns `lo == hi == limits.max_altitude`, so every goto and orbit altitude target sits exactly at the `FENCE_ALT_MAX` value with only a 2 m margin, tripping the fence action. | medium | possible | The collapse is safe by construction (`max_altitude` always wins) and `site.py` validates the band against itself. `control/planner_exec.py:528-533`; `site.py:271-281`; `mavlink/safety.py:392` | No validation that the site band intersects the limits envelope, and no warning when it does not. |
| `FM-38` | A missing site model leaves the companion flying on a 60 m fence cylinder | Site file absent or unreadable at companion startup | No polygon fence is uploaded and containment falls back to the `FENCE_RADIUS` cylinder. `executePlan` is refused, but manual, tracking and `planCommand` continue with no site geometry. | medium | rare | `executePlan` is refused when `_site_valid` is false and a warning `statusText` reports the skipped fence upload. `app.py:330-338`, `1476-1478`, `1716-1717`; `mavlink/safety.py:365-370` | The companion-side degraded-containment consequence is undocumented — the operator is told a mission was refused, not that lateral containment shrank to a cylinder. |
| `FM-53` | Above-ceiling NFZ overflight is approved, then the companion clamps altitude down into the NFZ | A verified overflight above an NFZ ceiling on a companion whose `limits.max_altitude` is lower | Ground passes `nfz_transit` because `minAltM > ceilingM`; `planner_exec` re-clamps every altitude with no warning, no clamped ack and no telemetry, so the flown leg crosses the NFZ *below* its ceiling. | high | possible | ✔ `capabilities.max_altitude_m` is min'd into `policyFor`, so the verifier rejects when capabilities arrive first; the demo runs `sitl.yaml` (80 m). `verifier.ts:111`, `392-399`; `planner_exec.py:528-537` | Depends on capabilities having arrived *and* being well-formed — the same message `FM-39` corrupts. |
| `FM-54` | `clear_altitude_m` is silently clamped below the safe value the site contract forbids clamping | LiDAR degraded plus a clutter-crossing route: the correction inserts a climb at `site.clearAltitudeM` | `planner_exec` clamps the inserted goto to `limits.max_altitude`, so under a 30 m baseline the LiDAR-degraded climb flies *below* the declared obstacle-clear altitude, with no clamped ack. | high | possible | ✔ `SITE_CONTRACT` names this; `capabilities.max_altitude_m` makes the verifier reject rather than correct; the demo runs `sitl.yaml`. `verifier.ts:535-551`; `planner_exec.py:535-537`; [`SITE_CONTRACT.md`](SITE_CONTRACT.md) §clamping | `CommandAck` has no `clamped` status, so no companion clamp of any kind can reach the operator. |

---

## 7. Companion — command dispatch, plan execution and control API

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-11` | `planCommand` follow/orbit writes `limits.standoff` with no upper bound | `planCommand` `orbit`/`follow` with a huge or `Infinity` radius | The shared `Limits` object gets an arbitrary standoff. Guidance retreats at `-max_speed` indefinitely and never stops; the value is never restored; `Infinity` leaks into `capabilities.max_standoff_m`, so a reconnecting ground station is told the wider bound is legitimate. | high | possible | ✔ Standoff is a closed BAND: `Limits.max_standoff` (config ceiling `MAX_STANDOFF_CEIL_M` = 50 m, floored like every other limit), `clamp_standoff` rejects non-finite, `planCommand` refuses a non-finite radius outright and acks `clamped` when the ceiling bites, and `_restore_operator_standoff` gives the shared envelope back on any release. companion/tests/test_command_surface.py (5 standoff tests) | Closed. `1e400` -> inf is refused rather than clamped: an unbounded request is a malformed one, not a large one. |
| `FM-12` | `planCommand` path skips the manual mutual-exclusion guard | `planCommand` follow/orbit arrives while manual control is engaged | `_planner_engaged` is set and `controlSource` is stamped `planner` while `_manual_engaged` is still `True`. On manual release the aircraft autonomously flies a follow/orbit leg the operator never authorised — and the deadman is silenced during that motion. | high | possible | ✔ The `follow`/`orbit` branch now carries the same preflight as `executePlan`: manual engaged, e-stop latched, site validity and readiness are all checked before anything is engaged, and `_release_all` clears `_planner_tracking_tool` so a release cannot leave a leg armed. companion/tests/test_command_surface.py::test_releasing_manual_after_a_refused_follow_does_not_fly_a_plan | Closed. |
| `FM-29` | Zero-speed or refused goto leg stalls the plan silently and starves setpoint frames | A goto leg whose clamped speed is ≤ 0, or any `goto_global` refusal | `_last_goto` is stamped *before* the send, so every tick early-returns from `_stream_goto` for ~1 s and the control tick emits no setpoint. The leg never advances, with no `statusText`, ack or failsafe. | medium | possible | Refusing the leg is correct (a non-positive `DO_CHANGE_SPEED` is DENIED) and velocity frames are deliberately suppressed on the goto path. `vehicle.py:1063-1074`; `app.py:1274-1276`, `1366-1387` | The throttle key is stamped before success; there is no stall detector or operator-visible signal. |
| `FM-30` | Orbit revolution accounting freezes near the centre; a zero speed cap makes the leg immortal | Orbit with the vehicle inside `max(1.0, 0.1 × radius)` of the centre, or an orbit resolved with a plan speed cap of 0 | Revolutions never accrue, and with a zero cap `vx`/`vy` are 0 forever, so the leg never reaches `360 × laps` and the plan is permanently stuck at that tool index. | medium | possible | The `min_standoff` radius floor normally keeps the vehicle outside the freeze gate; the bounded advance loop prevents runaway. `control/planner_exec.py:450-473`, `539-542`, `598-600` | No progress watchdog on any leg type; laps are unbounded; a zero cap silently means hover forever. |
| `FM-36` | Non-finite values in outbound JSON break the ground-side parser | An `inf`/`NaN` reaches a broadcast field (e.g. standoff set to `Infinity` via `FM-11`) | `json.dumps` emits bare `Infinity`/`NaN`, `JSON.parse` in the renderer rejects the frame, and the UI silently loses telemetry or capabilities with no companion-side error. | low | rare | ✔ `api/server.py:_dumps` serialises with `allow_nan=False` and falls back to a recursive `_finite_json` pass that renders non-finite numbers as `null`, so a frame is never emitted in a form `JSON.parse` rejects. companion/tests/test_api_hardening.py (3 serialisation tests) | Closed. |
| `FM-37` | `continueMission` consumes the hostile-drone latch even when it reports failure | `continueMission` after the hostile-drone hold has already released the plan | `resolve_hostile` clears the latch *before* the precondition check fails, so the ack says failure while the operator decision point is spent. A retry answers "no hold is latched". | low | possible | A hold frame is emitted and the next health tick recomputes cleanly; only an approved, still-loaded mission can resume. `app.py:1802-1814`; `control/failsafe.py:109-126` | State mutation precedes the precondition check; ack semantics diverge from state. |
| `FM-40` ✱ | Control WebSocket has no authentication, binds `0.0.0.0`, and arms test-fault hooks | Any host on the venue LAN connects to `ws://<companion>:8765`; the demo exports `EIS_ENABLE_TEST_HOOKS=true` with `sitl: true` | Arbitrary `executePlan`/`arm`/`takeoff`/`rtl`/`emergencyStop` and `testFault` (including `raw_out_of_fence`) execute with no planner, verifier, approval or audit. Any peer's frames also satisfy the ground-link deadman and can mask a real operator link loss. | critical | possible | ✔ Bind policy and authentication are ONE decision: the default bind is 127.0.0.1, widening it (YAML `network.host`, or `EIS_BIND_ALL=true`) FORCES `require_signed_commands` on in `_enforce_bind_policy`, and every inbound frame -- manualInput, planCommand, planHeartbeat, rfEvent, fleet included -- passes `CommandVerifier.verify_frame` BEFORE it counts as ground liveness or reaches a handler. `testFault` now needs a loopback socket as a third gate. companion/tests/test_command_surface.py + companion/tests/test_api_hardening.py | Closed. Key custody remains out of scope (THREAT_MODEL); a missing key means privileged commands are refused, never that verification is skipped. |
| `FM-55` ✱ | Companion night/clutter dispatch gates are unreachable from the UI | Every real `executePlan` from the ground station | `_execute_plan` reads `params["night"]` and `params["routeThroughClutter"]`, which the contract and `App.tsx` never send. Both companion refusals are dead code and the ground verifier is the sole enforcement. | high | likely | ✔ An env-gated SITL-only night injection exists, and `checkReadiness` does enforce both rules. `app.py:1727-1738`; `contract/index.ts:266-276`; `App.tsx:454`; `verifier.ts:280-285` | The contract's thermal-at-night and LiDAR-clutter rows name "ground verifier **and** companion gate" as the authority; the companion half never fires. |
| `FM-56` ✱ | Companion readiness is re-checked only while disarmed | `executePlan` arrives after the vehicle is already armed | Battery SoC, charge state, pack fault, imbalance, temperature and sensor readiness are not re-evaluated at dispatch. A plan verified at 85 % SoC dispatches at 40 %. | medium | likely | The failsafe-state gate refuses when any failsafe is latched, and the battery watchdog RTLs in flight. `app.py:1720-1721`; `App.tsx:447-450` | Neither side re-runs geometry, wind, range, sortie, `navSource` or airspace at dispatch (`FM-57` is the ground half). |
| `FM-105` ✱ | goto leg has no stall timeout: a 2 m arrival gate that is never satisfied hangs the mission | GPS quality `NavHealth` still calls healthy, position-hold drift, or an altitude the vehicle cannot settle within 2 m of | `_tick_goto` advances only when both distance and altitude gates are met, with no timeout, attempt counter or loosening. The vehicle re-commands the same GOTO forever and the mission silently dies mid-air with everything nominal. | high | possible | ✔ The coarse sortie/battery backstops eventually RTL. `planner_exec.py:413-435`; `config.py:219`, `741`; `control/nav_health.py:11-15` | That RTL fires minutes later and is attributed to time/energy. No health event names the stalled waypoint. |
| `FM-106` ✱ | Arrival gating consumes raw unfiltered lat/lon with no jitter debounce | A single noisy fix landing inside the 2 m goto gate or the 15 m staging gate | One sample irreversibly advances the plan leg or fires a staging detection at the wrong place; a single outside fix re-arms the observer. | medium | possible | Non-finite coordinates and the `(0,0)` pre-fix origin are guarded. `planner_exec.py:422`, `425`; `vision/staging.py:258-266`; `vision/multimodal.py:77-89` | No filtering, N-of-M voting or dwell. `NavHealth`'s fix-quality verdict is never consulted by either gate. |
| `FM-112` | Documented outbound backpressure protection does not exist — `max_queue` bounds the wrong direction | A slow or stalled UI client during high-rate telemetry plus large observation/anomaly payloads | `max_queue` bounds the *incoming* frame queue, so one stalled renderer buffers unboundedly in the companion. `max_size` is left at the 1 MiB default, rejecting large inbound frames. | medium | possible | ✔ `broadcast` bounds each send with `asyncio.wait_for` and EVICTS a stalled client, counting `dropped_clients`/`dropped_frames`; `max_size` is pinned to 256 KiB rather than the 1 MiB default. companion/tests/test_api_hardening.py (3 backpressure tests) | Closed. `max_queue` keeps its (inbound) meaning; the outbound protection is the send timeout plus eviction. |

---

## 8. Companion — battery, energy and sortie budget

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-19` | Battery telemetry has no staleness gate — a dead pack sensor reads as healthy | `BATTERY_STATUS`/`SYS_STATUS` stops arriving while the FC link is otherwise up | Cached voltage/current/SoC/temperature read as live. Coulomb integration runs off a frozen current (and a `current_battery == -1` sentinel becomes `0.0 A`), so SoC pins at its last healthy value. Reserve RTL, the pack-fault latch and `charge_state` can never notice. | high | possible | ✔ `health_inputs` emits `battery.age_s` from `_msg_ts` exactly as the nav rails do; `BatterySample.age_s` + `BatteryPolicy.batt_max_age_s` (5 s) make stale telemetry latch a FAULT (which the in-flight ladder consumes as rtl), not merely a readiness reason; and MAVLink's `current_battery == -1` is carried as None (unknown) instead of collapsing to a measured 0 A that froze the coulomb integrator. companion/tests/test_link_and_sensor_staleness.py (4 battery tests) | Decision recorded: `readiness_ok=... or armed` is KEPT. Staleness now reaches the ladder as a fault, which is authoritative in flight; dropping `or armed` would additionally re-block an armed vehicle on pre-arm charge reasons. |
| `FM-27` ✱ | Battery cell-count mismatch produces a permanent arm-blocking `degraded_estimate` escalate | `battery.cell_count` mismatched to the pack — `default.yaml` ships 4S while the SITL pack is 3S | Voltage SoC diverges more than 10 points from integrated SoC every tick, latching `degraded_estimate`. That escalate blocks arm and, through `FM-18`, every other command. | high | possible | ✔ The lower estimate is always used; `cell_count` is floored at 1; demo scripts pin `--config companion/config/sitl.yaml`. `control/battery_health.py:81-98`, `234-235`; `control/failsafe.py:92-93`; `default.yaml:91` vs `sitl.yaml:92` | No cross-check between configured cell count and observed pack voltage. An informational divergence escalates into total command lockout. |
| `FM-28` | Non-finite estimated return time forces `must_rtl_by_s` to 0 and RTLs on the first airborne tick | Non-finite home distance — no site model, non-finite vehicle lat/lon, or out-of-range home | `clamp` maps `inf` to the low bound 0, so `sortie_should_rtl` is true the moment `airborne` becomes true and RTL fires with reason "sortie must-RTL time reached". | medium | rare | The clamp deliberately fails low; the `site_valid` refuse gate usually blocks arming first; the sortie cap is bounded by `MAX_SORTIE_CAP_S`. `control/battery_health.py:75-78`, `106-120`, `238-240`; `app.py:602-609` | "Unknown return time" is silently treated as "zero budget" rather than as a refusal. |

---

## 9. Companion — perception, staging and multimodal observation

The primary observation rail is scripted from site truth. `FM-99` is the framing
mode for this section: everything below describes the behaviour of a synthetic
sensor stack that the wire cannot distinguish from a real one.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-25` ✱ | A missing or non-PNG/JPEG staging fixture permanently and globally fails the thermal/LiDAR/RGB rails | Any staging asset missing, unreadable, or lacking PNG/JPEG magic bytes — `initial_health` uses `all()` across every staging point | One bad asset marks the whole rail unhealthy before flight: permanent `escalate` (thermal, which then locks out `abortPlan` via `FM-18`) or permanent hold-and-climb (LiDAR), refusing night and clutter missions for points whose own assets are fine. A truncated but correctly-headed PNG passes readiness and fails at decode. | high | possible | ✔ Fixture health and live-source health are separate fields, so the 10 Hz perception loop can no longer erase the startup verdict; `asset_health()` / `unhealthy_points()` report PER POINT and `_connect_messages` names the bad fixture; a partially-failed staged observation raises its own healthEvent. companion/tests/test_perception_honesty.py (5 fixture tests, incl. a fixture-existence gate) | Closed for the RGB rail's observability. The night/clutter refusals still key on `params.night` / `params.routeThroughClutter`, which the UI never sends -- a ground-side contract gap, reported. |
| `FM-26` ✱ | A real camera failure self-clears one tick after leaving a staging point | `sensor_suite` reports rgb `failed` at a staging point while the primary source keeps succeeding | `_camera_observation_valid` flaps at 10 Hz between the staged verdict and the unconditional `True` from `source.observe()`. Command acceptance becomes nondeterministic and the documented escalation never persists. | medium | likely | A `healthEvent` "no observation" is broadcast and persisted when all staged frames are invalid. `app.py:1090-1096` vs `1126-1128`, `846-848` | Two writers to one flag with no priority. |
| `FM-35` | Fusion penalises every track when only one modality is healthy | A mission flown with thermal or LiDAR failed — an explicitly allowed degraded state | `fuse_tracks` has no sensor-health notion, so every surviving single-modality track is penalised 0.15 and emitted as a disagreement, feeding the low-confidence escalate that then locks out `abortPlan`. | low | possible | Unmatched tracks are retained, malformed tracks are filtered, and empty input is treated as valid no-detections. `control/fusion.py:38-43`, `112-117`; `vision/multimodal.py:108-147` | Sensor health is out of scope for the function and no caller compensates. |
| `FM-98` ✱ | Live-YOLO staging detector is person-only: vehicle/structure truths detect nothing | Companion installed with the `[detect]` extra so the real `PersonDetector` resolves; arrival at a vehicle- or structure-truth staging point | The detector filters to COCO class 0 and returns `[]`. Staging treats an empty *real* result as a real result, so the arrival emits zero observations and the scripted detection silently disappears — installing the optional extra makes the system detect **less**. | high | possible | ✔ A backend declares `supported_classes`; `StagingObserver.capabilities()` reports backend, classes and `unsupported_truths`, the orchestrator publishes it in `capabilities.detector` and raises a `capability_gap` healthEvent, and an unsupported truth emits NOTHING rather than silently substituting the stub. companion/tests/test_perception_honesty.py (3 capability tests) | Closed. A backend that declares no classes is treated as unrestricted -- we make no claim on its behalf. |
| `FM-99` | The primary observation rail is entirely scripted from site truth with no live inference path | Any arrival at a staging point while a plan is flying | RGB tracks, thermal confidence/delta/range and the LiDAR cloud are all derived from the answer key in live operation as well as in tests. `YoloThermalDetector` is constructed nowhere. | high | possible | ✔ (partial) `StagedObservation.scripted` / `range_frame` record provenance, and `observation_message` prefixes the `scene` string with "[SCRIPTED FIXTURE - not live inference]" so the UI and the report show it. The module docstring states the rule. companion/tests/test_perception_honesty.py::test_the_observation_declares_its_frame_and_its_provenance | REPORTED contract gap: the observation message has no `provenance` FIELD. Adding one is a wire change and would require all three contract mirrors plus the Mock in one commit; not done unilaterally from the companion. |
| `FM-100` | A missing or failed camera frame is reported as a **healthy** observation with zero tracks | Real camera path: `Capture.read()` returns `(False, None)` | `_CameraSource.observe` swallows it and returns `[]`, so `_camera_observation_valid` is set `True`. A dead camera presents as a valid empty frame, which supports a `false_alarm` verdict instead of "no observation"/escalate — closing an incident on evidence that was never captured. | critical | possible | ✔ `_CameraSource.observe` RAISES `FrameUnavailable` on a failed read or a failed inference (`PersonDetector.last_inference_failed`), and the perception loop turns that into `_camera_source_valid = False` plus a `no observation` healthEvent. A healthy empty frame is still a valid observation. companion/tests/test_perception_honesty.py (3 observation-state tests) | Closed -- the observation-state invariant now holds on the real-camera path. |
| `FM-101` | Real camera + detector construction always raises `TypeError` and silently disables all perception | Any run with `camera.source` set to a real capture backend | `PersonDetector` is called with `conf=` but its parameter is `conf_threshold`. The bare `except` logs "perception disabled" and returns `None`, so no observations are produced for the rest of the flight. | high | possible | ✔ The call site passes `conf_threshold=` (the real parameter name), the constructor accepts `conf` as an alias so the same mistake cannot be fatal again, and `_build_vision_source` catches ImportError and TypeError SEPARATELY so a construction bug is never reported as "no hardware". companion/tests/test_perception_honesty.py::test_the_real_detector_is_constructed_with_the_right_keyword | Closed. |
| `FM-102` | Staging still-image detection runs synchronously on the asyncio event loop | Arrival at a staging point with a real detector resolved | `cv2.imread` plus a YOLO forward pass block the loop that drives the 20 Hz control loop, the telemetry pump and the WebSocket server — long enough to trip the ground-link deadman or leave a stale setpoint. | high | possible | ✔ The sibling multi-sensor path is offloaded with `asyncio.to_thread`, and the default SITL backend is the microsecond-cheap stub. `app.py:1101-1108` vs `1112-1123`; `vision/staging.py:297-331` | Nothing bounds staging inference time. `last_inference_ms` is recorded but never read, and no watchdog notices an overrun tick. |
| `FM-103` ✱ | Observation arrival gate chatters at the orbit radius, re-firing observations every lap | An orbit leg whose radius exceeds `staging_arrival_radius_m`, so the observation radius is raised to exactly the servo distance | Noise flips the point in and out of the present set. Each re-entry re-runs the whole scripted suite and broadcasts a new observation, spamming the operator log and audit trail with duplicate evidence. | medium | possible | The UI's `reportingRef` de-dupes the incident-report call per `requestId`. `app.py:1119-1122`; `planner_exec.py:445`, `460-462`; `vision/multimodal.py:79-93` | No hysteresis, dwell timer or per-arrival latch; 0.06–0.20 s of thread work per re-fire at up to 10 Hz. |
| `FM-104` ✱ | `StagingObserver` burst restarts on every radius re-entry | The vehicle crosses the 15 m staging radius outward and back | `inside` and `cached` are cleared on exit, so the next tick is a fresh arrival: detection re-runs and another `emit_repeat` burst of identical observations is spliced in, flapping the tracking frame between locked and idle. | medium | possible | The once-per-arrival debounce and burst prevent per-tick spam while inside the radius. `vision/staging.py:71`, `76`, `266-291` | `test_staging.py` codifies leave-and-return re-arm as intended; there is no minimum time between arrivals. |
| `FM-113` ✱ | Synthetic LiDAR "structure" box fragments below the cluster gate — zero LiDAR evidence | A staging point with truth `structure` | The box grid spacing (1.667 m) exceeds the 1.5 m cluster radius, so it splits into zero-extent slabs and every classifier branch rejects them: zero LiDAR tracks and zero `new_structures`, while LiDAR health still reports `ok`. | medium | likely | ✔ Object grids are sampled at `OBJECT_SAMPLE_SPACING_M` (0.5 m) derived from the box dimensions instead of a fixed 5x4x3 sample count, so a 5 x 4 m structure is denser than the 1.5 m cluster radius and forms a cluster. companion/tests/test_perception_honesty.py::test_a_structure_fixture_produces_lidar_evidence | Closed. Measured: structure went from 0 LiDAR tracks / 0 new_structures to 1 and 1. |
| `FM-114` ✱ | `structure` truth always produces a modality disagreement by construction | A staging point with truth `structure` | RGB is hardcoded at 12.0 m and thermal at 20.0 m against a 5.0 m fusion range gate, so they can never associate: two unmatched penalised tracks and two disagreement strings, predetermined by the fixture tables rather than by evidence. | medium | likely | The resulting behaviour matches the contract's modality-disagreement row (escalate, 0.15 penalty). `vision/multimodal.py:115`; `vision/thermal.py:30`; `control/fusion.py:35`, `70-76` | No fixture produces a genuine evidence-driven disagreement, so the escalate path is only ever exercised by a table mismatch. |
| `FM-115` | LiDAR class vocabulary is the single hardcoded string `vehicle` | Any staging observation whose RGB/thermal class is `person` or `structure` | `fuse_tracks` requires exact class equality, so a LiDAR return can never corroborate a person or a structure — it yields a phantom penalised `vehicle` track or nothing, structurally excluding LiDAR from multimodal confirmation. | medium | possible | ✔ `classify_cluster` returns person|vehicle|structure from footprint and height (and "" for the flat fence run), and `control.fusion.class_compatible` lets an `unclassified` geometric return corroborate any named class without overwriting it. companion/tests/test_perception_honesty.py::test_lidar_can_corroborate_a_person | Closed. |
| `FM-116` | Fence-gap 3-scan debounce is structurally unreachable | Any real or rate-limited LiDAR source feeding fence-gap detection | A fresh `LidarGeometryDetector` is constructed per observation, so `_gap_votes` always resets and real evidence never accumulates. Gaps are reported only because the breach fixture calls `detect()` three times on one frame. | medium | possible | ✔ `StagedSensorSuite` keeps ONE `LidarGeometryDetector` per staging origin across observations, and takes `FENCE_GAP_VOTES` DISTINCT scans (deterministic sub-voxel jitter) rather than replaying one frame, so the debounce is real evidence of persistence. companion/tests/test_perception_honesty.py::test_the_fence_gap_debounce_accumulates_across_distinct_scans | Closed. A single scan provably reports no gap. |
| `FM-117` | Fence-gap `width_m` is a point count mislabelled as metres | Any fence gap reported to the operator or the incident report | `width` is the count of missing sample points, correct only because spacing happens to be exactly 1.0. Changing sampling density silently rescales the reported breach width. | low | possible | ✔ `FENCE_SAMPLE_SPACING_M` is one shared constant used by the generator and the detector, and `width_m = len(missing) * FENCE_SAMPLE_SPACING_M`. companion/tests/test_perception_honesty.py::test_the_gap_width_is_metres_derived_from_the_sample_spacing | Closed. |
| `FM-118` ✱ | Fabricated proximity data is sent to the FC: staging-frame ranges, one value in all 72 sectors | Any staged observation with a healthy LiDAR rail while a plan is flying | `min(lidar_ranges_m)` is published as `DISTANCE_SENSOR` and as a 72-bin `OBSTACLE_DISTANCE` fan with the same value everywhere — an obstacle ring around the vehicle — computed in a frame centred on the staging point, not the vehicle. With avoidance enabled ArduPilot sees a wall in all directions and refuses to move. | high | possible | ✔ NOTHING is published. The staged ranges are in the STAGING POINT's frame, not the vehicle's, so no DISTANCE_SENSOR and no OBSTACLE_DISTANCE fan is emitted from a staged observation; `StagedObservation.range_frame` records why. Sending nothing is correct for a rail that is not vehicle-relative. companion/tests/test_perception_honesty.py::test_no_proximity_frames_are_published_from_a_staged_observation | Closed. A real vehicle-frame LiDAR would publish at this seam; the send helpers are retained for it. |
| `FM-119` | Synthetic LiDAR has unlimited range and no occlusion | Every staged LiDAR frame | The entire perimeter ring is emitted at 1 m spacing regardless of distance, with no range gate, divergence, occlusion or dropout, so every threshold tuned against it is tuned against an impossible sensor. | low | likely | ✔ `SyntheticLidarSource(max_range_m=250)` gates every point, the detector shares the range so an unreachable sample is never counted as missing fence, and the breach gap is now cut from the CLOSEST fence stretch. The remaining simplifications (no occlusion, divergence or dropout) are named explicitly in the module. companion/tests/test_perception_honesty.py (2 range-gate tests) | Bounded and documented. It is a FIXTURE, not a sensor model, and now says so. |
| `FM-120` | Staging `image_root` is derived from `__file__` and breaks outside an editable install | Companion installed as a wheel or into `site-packages` | `parents[3]` points at an unrelated directory, so every staging asset fails, both rails are marked failed, and every arrival emits "no observation". | high | possible | ✔ The observation-state distinction is preserved — it escalates rather than fabricating a `false_alarm`. `app.py:398`, `427`, `1140-1143`; `vision/multimodal.py:168-177` | No configuration key for the asset root, and no startup assertion that it contains the site file's images. |
| `FM-121` | Stub confidences straddle the real detector's threshold | Switching between stub and real staging backends with shipped defaults | The `structure` stub emits 0.32 while the detector threshold is 0.45 (configured 0.4), so a cue the offline demo shows is mathematically unreachable for the real backend and tuning does not transfer. | low | possible | ✔ `STUB_MIN_CONFIDENCE` (0.32) and `DEFAULT_CONF_THRESHOLD` (0.30) are shared constants with a test asserting the ordering, so every cue the offline demo shows is reachable by the real backend. companion/tests/test_perception_honesty.py::test_every_stub_confidence_clears_the_detector_threshold | Closed. |
| `FM-122` ✱ | Staging stub emits vehicle/structure boxes as classless `person` tracks on the tracking wire | Arrival at a vehicle- or structure-truth staging point on the stub path | `TargetObservation` and the tracking message carry no class, so those boxes flow through the tracker and the UI renders a parked vehicle as a locked person target. | medium | likely | ✔ `TargetObservation.cls` carries the class (internal type, not a wire change), the stub table declares it per truth, and the orchestrator routes ONLY person-class observations onto the person-tracking wire; vehicle and structure stay on the observation rail, which does carry a class. companion/tests/test_perception_honesty.py::test_non_person_observations_stay_off_the_person_tracking_wire | Closed. |
| `FM-127` | Overlapping staging radii: only one observation is emitted, the others are silently swallowed | Two staging points closer together than the effective observation radius (which grows to the orbit radius) | `observe()` selects at most one point but marks every in-radius id as inside, consuming the others' arrival edge with no observation until the vehicle leaves and returns. | low | rare | ✔ `StagedSensorSuite.observe` marks only the point it actually OBSERVED as consumed; other in-radius points stay pending and emit on a later tick, with a log line naming them. companion/tests/test_perception_honesty.py::test_an_unobserved_in_radius_point_stays_pending | Closed. |
| `FM-129` ✱ | Detector defaults to `cuda:0` with no CPU fallback, degrading silently forever | `companion[detect]` installed on a machine without CUDA, then any arrival or camera frame | `predict` raises; the live path returns `[]` and staging falls back to the stub per arrival indefinitely, so the system looks like real inference while every result is empty or scripted. | medium | possible | Contained rather than fatal: `[]` instead of raising, and the deterministic stub keeps the demo flow producing its scripted result. `vision/detector.py:96`, `113`, `176-188`; `vision/staging.py:324-331` | No health event, telemetry field or readiness reason reports "detector degraded to stub"; `_camera_observation_valid` stays `True`. |

---

## 10. Ground — MissionVerifier and the trust layer

The verifier is the layer the system's safety claim rests on: 18 deterministic
checks between a model-authored plan and an armed aircraft. Modes here are
therefore weighted toward `critical` even when the mechanism is small.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-39` ✱ | Unvalidated `capabilities.profiles` neutralises the hard standoff floor | A `capabilities` message whose `profiles[].min_standoff_m` is `NaN` or non-numeric reaches `policyFor` | `Math.max(base, NaN)` is `NaN`, so `radius < NaN` is false: an orbit at 0.5 m returns standoff **ok** and verdict **pass** — a green PASS for a plan violating the 3 m floor the whole trust layer is advertised on. | critical | possible | ✔ `planner_exec` floors the flown radius at `limits.min_standoff` and the real companion emits sane values. `verifier.ts:106-115`, `371`; `LiveDataProvider.ts:224-226`; `planner_exec.py:442` (reproduced) | No runtime validation of `CapabilitiesMessage` anywhere on the ground path. `Math.min` fails closed for speed and altitude while `Math.max` fails **open** for standoff. |
| `FM-41` | Nothing binds an executed plan to its verification | Any `executePlan` reaching the companion | The companion cannot distinguish a verifier-approved plan from an unverified one: no `Verification` object, hash, signature or nonce crosses the seam, and `requestId` never reaches the ack path. | critical | likely | ✔ `planner_exec.load_plan` is an independent validator that rejects malformed/unknown tools and clamps scalars; `executePlan` refuses when `_site_valid` is false. `contract/index.ts:212`, `279-281`; `app.py:1743-1752`; `App.tsx:454` | The independent validator re-checks scalars, never geometry, so it cannot catch an unverified-but-well-formed plan. `FM-40` is the reachable path for one. |
| `FM-44` ✱ | Indefinite hold passes the loiter bound, is budgeted at 30 s, and executes forever | A plan containing `{tool: "hold"}` with no `durationS` (the LLM schema allows `durationS: null`, normalised to omitted) | `loiter` reports ok with a false reason, range/sortie budget it at 30 s, correction leaves it unbounded, and `planner_exec` holds forever — the mission never reaches `rtl` and never completes. | high | possible | ✔ Companion sortie/battery reserve watchdogs RTL independently (contract rows "battery below reserve" and "sortie timer"). `verifier.ts:36-37`, `204`, `419-426`; `llm.ts:18`, `83-85`; `planner_exec.py:405-411` (reproduced, verdict=pass) | The verifier asserts a bound it did not enforce, and nothing in the planner flags the stall. |
| `FM-45` ✱ | Sortie check ignores time already flown | The vehicle has been armed/airborne before the mission is approved | `checkSortie` compares only the new plan's duration against a static 480 s cap, so a 400 s plan passes after 300 s of flight and the ground reports that it fits. | high | likely | ✔ `battery_health` computes elapsed vs `must_rtl_by` and forces RTL. `verifier.ts:54-73`, `447-455`; `contract/index.ts:142`; `App.tsx:562-577` | `SortieState.elapsed_s` exists in telemetry and is never read. The operator approves a plan the ground said fits. |
| `FM-46` ✱ | Battery reserve is subtracted twice in the range check | Any range check where the companion supplies `remaining_s` | The companion already reserve-subtracts and the verifier multiplies by `(soc-25)/soc` again: ~28 % over-conservative at 90 % SoC, and outright rejection of physically fine missions below ~40 %, while the reason string still claims "with 25 % reserve". | medium | likely | Companion half documented: `control/battery_health.py` states in its `remaining_s` computation that the value is ALREADY reserve-adjusted (`nominal_endurance_s * (soc - reserve_pct) / 100`), so a consumer that reserve-subtracts again is double-counting. companion/tests/test_link_and_sensor_staleness.py pins the battery rail. | REPORTED, not fixed: the second subtraction is in `ground/planner/src/verifier.ts:436-446`, outside this agent's scope. The reason string that claims "with 25 % reserve" is also ground-side. |
| `FM-47` | Range model is speed- and load-independent — faster is predicted cheaper | Any plan, most visibly when a fast profile is chosen to make a mission fit | `totalFlightS = length / speed` with no power term, so raising speed always makes a mission fit. The check can approve a mission the aircraft cannot complete. | medium | likely | The 25 % reserve plus the `FM-46` double subtraction give a large blanket margin, and the companion RTLs on reserve. `verifier.ts:24-27`, `164-175`, `198-206` | `DRAIN_PCT_PER_S` and `DEFAULT_BATTERY_PCT` are exported dead code; the "battery model" is a fixed-endurance divisor. |
| `FM-48` | Endurance constant is never sourced from the vehicle | Any mission on a vehicle whose real endurance differs from the hardcoded 1500 s | `modelAvailable` budgets a 12-minute airframe as a 25-minute one. `CapabilitiesMessage` has no endurance or capacity field to correct it. | medium | possible | `liveAvailable` from the companion's `remaining_s` is min'd in. `verifier.ts:24`; `policy.ts:10`; `contract.ts:224-233` | In SITL `remaining_s` derives from the same 1500 s constant, so the `min()` is not independent during the demo. |
| `FM-49` ✱ | `sdrState` absent rejects every mission — one sidecar spawn failure kills the demo | The SDR sidecar fails to spawn or emits no spectrum frame | `checkRfEnvironment` hard-fails "RF events and SDR health state are required" on every plan. No mission can be approved, and the message names the SDR rather than the missing sidecar. | high | possible | ✔ The UI auto-starts the sidecar on mount and surfaces stderr as an `sdr` `healthEvent`. `verifier.ts:297-300`; `App.tsx:178`, `375`, `571`; `phase3Host.ts:59-87` | Fail-closed is right, but the reason is indistinguishable from real interference and there is no override. |
| `FM-50` | Saturated or degraded SDR is treated as a healthy RF environment | The sidecar reports a saturated or degraded front end | `checkRfEnvironment` special-cases only `no_device` and otherwise returns ok with "no blocking RF interference" — an assertion a saturated receiver cannot support. | medium | possible | ✔ **MITIGATED (this wave).** `checkRfEnvironment` has three outcomes, and "clear" is the narrowest: `nominal` asserts no interference, `no_device` reports unknown, and `warming`/`degraded`/`saturated` now also report UNKNOWN naming the state — a blind receiver cannot rule out what it cannot see. Unknown is not a refusal, so an impaired SDR still does not ground the aircraft. `policy.ts RF_POLICY.impairedSdrStates`; `verifier.ts checkRfEnvironment`; `planner/test/verifier-fmea.test.ts` §FM-50 | None remaining. The state vocabulary and the check's logic now cover the same set. |
| `FM-51` ✱ | RF events never expire — one stale hostile-drone event blocks every mission | A `hostile_drone` or `gnss_interference` event lands early in the session | The verifier filters by kind only and the UI keeps a count-based window of 20 with no age bound, so a 20-minute-old report keeps failing `airspace` forever. | medium | possible | ✔ **MITIGATED (this wave).** RF reports are windowed by AGE: `currentRfEvents` keeps only events inside `RF_POLICY.eventWindowS` (60 s, the same window `rf_adapter` uses), and `airspace`, `rf_environment` and the unattended envelope all read through it. The UI's event list is bounded by age as well as count. A caller with no `now` gets every event as current — not knowing the time may tighten airspace, never loosen it — and `PlannerService` defaults `now` so the real path always has it. `verifier.ts currentRfEvents`; `service.ts runtimeContext`; `App.tsx RF_EVENT_WINDOW_MS`; `planner/test/verifier-fmea.test.ts` §FM-51 (6 cases) | None remaining. The reviewable V19/V21/V22 fixtures carry no `now` and so still see their events as current, which is the documented pessimistic reading. |
| `FM-52` | `rfEvents` from the socket bypass the RF adapter's validation | A malformed or schema-drifted `rfEvent` arrives on the control socket or a teammate bridge | `LiveDataProvider` forwards the raw parsed object. An event with a misspelled or missing `kind` is silently dropped by both filters, so a real interference report becomes a no-op and the check reports clear. | medium | possible | `adaptRfEvent` does full validation and `checkAirspace` guards `finite(event.lat)`. `LiveDataProvider.ts:225-227`; `rf_adapter.ts:57`, `145`; `verifier.ts:324-327` | The validating adapter is wired only to the fixture path, not the live path. |
| `FM-60` ✱ | Correction edits are displayed on rejected verdicts | A plan failing both a correctable and an uncorrectable check | `checksWithEdits` is returned in the rejected branch and the panel renders "✎ clamped tool 0 altitude…" beside a **failed** check, so the operator reads applied repairs on a plan that was never corrected. | medium | likely | ✔ **MITIGATED (this wave).** The `rejected` branch returns the ORIGINAL checks, unannotated. An `edit` describes a repair that was applied; on a rejected verdict the repair attempt was abandoned and the plan it describes does not exist. `verifier.ts verifyMission` (rejected branch); `planner/test/verifier-fmea.test.ts` §FM-60 (2 cases, incl. one asserting a CORRECTED verdict still carries its edits) | None remaining. |
| `FM-61` | The corrected plan is never re-run through the schema validator | Any `corrected` verdict | `runChecks` takes the default `schema=ok`, so the released corrected plan carries an unevaluated `schema: ok` row even though corrections rewrite coordinates and splice in new tools. | medium | rare | Every geometric check *is* re-walked on the corrected plan, and the companion's `_parse_tool` re-validates lat/lon and finiteness at load. `verifier.ts:457-458`, `531`, `546`, `578`, `635` | The module's "fully re-walked and rechecked" claim is true for geometry and false for schema. |
| `FM-62` | `buildCorrectedPlan` can throw and take the whole proposal down | A degenerate polygon reaching `movePointInsidePolygon` during an orbit-containment correction | The throw escapes `verifyMission`, `scriptedResult`, `propose` and the IPC promise. The UI shows a bare "Planning failed" with no verdict, checks or audit row. | medium | rare | `checkSite` is try/catch-wrapped and rejects most degenerate geometry first; the live branch falls back to scripted. `verifier.ts:616-630`; `site.ts:587`; `service.ts:97-102` | The scripted fallback path — the demo default — has no exception guard around verification. |
| `FM-63` | Boundary epsilons are inconsistent between the target check and the leg check | A target on or within ~2 cm of the geofence boundary | `pointInOrOnPolygon` accepts it while `segmentStaysInsidePolygon` rejects the leg, so one plan gets contradictory reasons and a legal boundary-hugging plan is rejected. | low | possible | `CORRECTION_MARGIN_M = 5 m` and the via-point clearance swamp the rounding, so corrections do not oscillate. `site.ts:344-346`, `412-416`, `443-446`; `verifier.ts:96`, `379-407` | Three different epsilons (2 cm, 5 cm, `round6` ≈ 0.11 m). Documentation claims exact-boundary points never matter. |
| `FM-64` ✱ | `checkStandoff` covers only explicit orbit radii — goto targets have no standoff | A `goto_gps` target metres from the anomaly, a structure or a person | A goto to within 1 m passes every check (`anomaly_proximity` only requires ≤ 200 m) while the panel shows standoff ✓. | medium | possible | `guidance` enforces the floor for the tracking source and `planner_exec` floors orbit radii. `verifier.ts:337-343`, `367-375`; `planner_exec.py:413-435` | A `goto_gps` leg is streamed as an absolute position target with no standoff concept; only the word "explicit" hints at the gap. |
| `FM-65` | The plan is never checked against the anomaly it claims to answer | Two anomalies within 200 m, or a plan whose `anomalyId` does not match the selected anomaly | `plan.anomalyId` is never compared to `context.anomaly.id`, so a plan for anomaly A verifies clean against B and the incident report is filed against the wrong anomaly. | medium | rare | `llm.ts` and `scripted.ts` both force `anomalyId` from the real anomaly. `verifier.ts:337-343` (no `anomalyId` reference anywhere in the file); `contract.ts:102-108` | Closed only for plans produced by this package; a Hub- or operator-supplied plan is unbound. |
| `FM-66` ✱ | An empty `nfz` array reports enforcement that did not happen | A site model with `"nfz": []` | The verifier reports "all legs clear buffered NFZs by 25 m", so a truncated or partially migrated site model reads as a clean bill of health. | medium | possible | A missing `nfz` key throws, and the shipped stub has two NFZs. `site.ts:188-191`; `verifier.ts:392-410`; `site/site.stub.json` | No check counts the zones actually evaluated. |
| `FM-71` | Via-point routing gives up silently after 12 attempts | NFZ geometry where no buffered candidate is both inside the geofence and clear of every other zone | The loop breaks with no note, the violating leg survives, and the final re-check turns it into a plain rejection with no indication that re-routing was attempted and impossible. | low | possible | The bounded loop and mandatory re-check guarantee an unsafe plan is never released. `verifier.ts:553`, `561`, `573`, `635-639` | The contract's double-rejection row promises the escalation contains both check lists; the reason correction failed is in neither. |
| `FM-72` ✱ | Live-path proposals are always rejected: the UI never supplies `windMps` | Operator starts an inspection with the live `DataSource` (what `demo.ps1` selects) | `App.tsx` builds the `VerificationContext` without `windMps`; `checkWind` fails on non-finite wind and wind has no correction rule, so **every** plan — LLM or scripted fallback — is rejected and Approve stays disabled. | critical | likely | ✔ **MITIGATED (this wave).** The live context always carries a wind: measured from the vehicle's `wind` health event when one has arrived, otherwise the documented `ASSUMED_WIND_MPS` (6 m/s, the unattended limit) tagged `windSource: 'assumed'`. The verifier says which it used, and an assumed wind is REFUSED for unattended dispatch — a stand-in number is not a measurement. `ui/src/lib/wind.ts`; `App.tsx startLiveInspection`; `verifier.ts checkWind/unattendedFailures`; `ui/test/wind-context.test.ts` builds the real App.tsx context shape (16 cases) | None remaining for dispatch. The contract has no wind field on `Telemetry`, so the number is parsed out of a health-event detail line; a vehicle that words it differently falls back to the assumption rather than mis-reading it. |
| `FM-74` ✱ | The verifier silently relocates hallucinated coordinates and green-lights a plan whose displayed checks are red | An LLM target outside the geofence or inside an NFZ; `buildCorrectedPlan` moves it and the relocated point happens to satisfy `anomaly_proximity` | Verdict becomes `corrected` and approvable while the rendered check rows are the **original** failing ones — including an unexplained red `anomaly_proximity`. Approving flies coordinates the LLM never proposed. | high | possible | ✔ Correction is disclosed: the corrected plan is re-walked and re-checked and the panel renders an original→corrected per-tool diff. `verifier.ts:507-532`, `631-638`; `VerifierPanel.tsx:84-86`, `148-182` (reproduced at 210 m) | The verdict comes from `correctedChecks` while the displayed rows are the originals. |

---

## 11. Ground — LLM planner and incident reporting

The model emits tool calls and report prose only. Every mode below is bounded by
that architectural choice; the section records where the bound is real and where
it is asserted.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-73` ✱ | Hallucinated coordinates within 200 m pass every check silently | The LLM emits a plausible-but-wrong coordinate up to `ANOMALY_PROXIMITY_M = 200 m` from the true anomaly | The drone flies to and orbits (radius 25 m) the wrong place, sees nothing relevant, and the report describes the result as the anomaly location — all 18 checks green. | high | possible | ✔ **MITIGATED (this wave).** `anomaly_proximity` now carries two bounds. `ANOMALY_PROXIMITY_M` (200 m) stays the outer bound — some target must be near the cue at all — and every OBSERVATION-class target (an orbit centre) must additionally sit within `OBSERVATION_POLICY.orbitCentreToleranceM` (60 m: the fixture site's NFZ buffer plus the correction margin, doubled). The measured distances are in the reason, so the operator reads metres. A 150 m displacement is now `rejected`. `policy.ts OBSERVATION_POLICY`; `verifier.ts checkAnomalyProximity`; `planner/test/verifier-fmea.test.ts` §FM-73 (reproduced at 150 m → rejected) | A `goto_gps` transit waypoint is still bounded only by the outer 200 m: it is a route point, not an observation, and tightening it would reject legal NFZ detours. |
| `FM-75` ✱ | LLM report markdown is passed through verbatim — hallucinated observations reach the operator | Live mode where the LLM's report verdict happens to match the deterministic one | Sensor claims, modality lists, frame citations and geometry claims are never validated against the `ObservationSummary`, so a fabricated narrative can carry a correct verdict and drive operator disposition. | high | likely | ✔ Verdict-only guard: on disagreement the whole LLM report is replaced by the deterministic writer, and any throw falls back the same way. `llm.ts:90-99`; `service.ts:84-92`; `report.ts:36-41` | No check that claimed modalities ⊆ `observation.modalities`, that cited frames exist, or any length bound. |
| `FM-76` | Report verdict miscalibration (LLM under-calls a missing or low-confidence observation) | The live report writer returns `false_alarm`/`log` when the observation was missing, stale, or below the 0.6 confidence bar | A real intrusion is closed as a false alarm with no human review. | critical | possible | ✔ Hard deterministic override: `reportVerdict` is recomputed from the validated observation and any disagreement discards the entire LLM report; escalates whenever the observation is unavailable, confidence < 0.6, or inconclusive. `service.ts:81-92`; `report.ts:33-41` | The override is verdict-only, and the substitution is not surfaced to the operator. |
| `FM-77` | Prompt injection via anomaly metadata; `propose` never validates the anomaly | `anomaly.type`/`id` are unbounded free text `JSON.stringify`'d into both prompts, and `PlannerService.propose` never calls `validateAnomaly` (unlike `report`) | Injected instructions reach the model on every plan and report call, and reach the operator verbatim in the deterministic report's "What was flagged" paragraph. | high | possible | ✔ Downstream containment: strict output schema, tool whitelist, 18 verifier checks and the companion re-clamp — injection cannot widen the safety envelope. `llm.ts:76`, `94`; `validate.ts:173-181`; `service.ts:51-79` vs `82` | No `validateAnomaly` at the `propose` boundary, no length cap, no delimiting of untrusted spans, no injection logging. |
| `FM-78` | Prompt injection via site-model free text (NFZ/clutter zone names) | `constraints(site)` serialises the whole `SiteModel`, including arbitrary `name` strings, into the most-trusted region of the prompt | A crafted or careless operator-supplied site file steers the planner. Same bounded blast radius as `FM-77`, but the site file is the artefact a third party is most likely to edit. | medium | rare | The verifier evaluates real geometry from the same `SiteModel`, so a name cannot move a polygon. `llm.ts:43-49`, `76-77`; `site.ts:194-202` | No allowlist or length bound on name fields; the prompt sends the whole site object. |
| `FM-79` ✱ | Base64 thumbnail dumped into the model prompt | The baked anomaly's 10,010-char data URL is stringified into the plan prompt, the retry, and the report prompt | ~2.9k inert tokens per call, tripled across a retry-plus-report cycle: latency on the demo's critical path, cost, and context-limit exposure with larger thumbnails or multiple frames. | medium | likely | ✔ **MITIGATED (this wave).** `reportPayload` reduces every image payload to a description of itself before the report prompt is built — the counterpart `report.ts` has had since it was written. The baked anomaly's 10,010-char data URL becomes "embedded image/png attachment"; a genuine path passes through. `llm.ts reportPayload/describeEvidenceRef`; `planner/test/llm-payload.test.ts` asserts the serialised payload contains no base64 and is <1/10 the size of the raw one | None remaining. `triagePayload` never carried imagery, and the LLM plan path no longer exists (ADR D20), so these were the last two prompts that could. |
| `FM-80` ✱ | Up to ~40 s of blocking LLM latency with no aggregate deadline or cancel | Live mode with a slow API, or a first plan the verifier rejects: two sequential 20 s calls, each with its own `AbortController` | The UI sits in `livePlanning` for up to ~40 s before the scripted fallback appears, with no progress indication and no cancel — a frozen ground station on stage. | high | possible | ✔ Each call is hard-aborted at 20 s and any throw falls back to `ScriptedPlanner`; scripted is the demo default and `demo.ps1` refuses `--live-llm` without a key. `llm.ts:12`, `101-109`; `service.ts:57-75`; `App.tsx:553-601` | No total budget across attempts, no UI-side timeout or cancel, no connectivity pre-check. The contract specifies a single 20 s deadline that the retry path doubles. |
| `FM-84` | First attempt's verification is discarded — escalation carries only one check list | The verifier rejects the LLM's first plan and the service retries once | Only the second attempt's plan and verification are returned, so operator escalation cannot show what the model tried first — contradicting the contract's double-rejection row. | medium | possible | Exactly one retry with no third attempt, and `escalationReason` set on a second rejection. `service.ts:61-72`; `test/service.test.ts:22-31` | `PlannerProposeResult` has no field for the first attempt. |
| `FM-86` ✱ | Unbounded tool-array length: ground accepts, companion rejects after operator approval | A degenerate model emitting hundreds of near-identical legs; the schema has `.min(1)` and no `.max` | A 61-tool plan verifies **pass**, the operator approves, and the companion rejects at load with "plan too large" — approval-then-refusal in front of the audience. Below 100 tools it executes and the panel renders an unusable list. | medium | rare | Companion `MAX_PLAN_TOOLS = 100` rejects at load and leaves any loaded plan untouched. `llm.ts:23`; `validate.ts:139-141`; `planner_exec.py:58-61`, `303-304` (reproduced) | No ground-side cap and no virtualisation in `VerifierPanel`. |
| `FM-87` | Model-authored rationale is unverified, unbounded, and operator-facing | A `Task.rationale` and a `PlanTraceEntry.effect` are free strings the operator reads | A persuasive rationale can accompany a task that asks for something else. It is text the operator reads while deciding to hold-to-approve, and a runaway generation renders a huge block. | medium | possible | ✔ The model can no longer author a PLAN at all (ADR D20): `planner.ts` composes `triage.ts` → `deterministic.ts` → `verifier.ts`, and the plan's own `rationale` is written by the deterministic planner from the rule table. A model-authored `Task.question` is capped at `TASK_QUESTION_MAX_CHARS` and `taskFromModel` drops any task naming an unknown anomaly. `planner.ts`; `triage.ts taskFromModel`; `llm.ts TASK_LIST_SCHEMA` (no lat/lon/alt/radius/speed/profile/tool) | `Task.rationale` itself has no length cap, and nothing checks a rationale against the task it accompanies. |
| `FM-88` | Untrusted text reaches operator-facing incident markdown on the deterministic path | `writeIncidentReport` interpolates `anomaly.type` and `plan.rationale` into the markdown body with no sanitisation | Injected or misleading text lands in the archived incident report the operator dispositions and that is retained for audit. | medium | possible | XSS is structurally impossible — a hand-written React-node renderer with no `dangerouslySetInnerHTML`; image payloads are elided. `report.ts:63-84`, `142`, `152`; `lib/markdown.tsx:8-136` | No length/charset guard before interpolation; `report.test.ts` covers only the thumbnail case. |
| `FM-89` ✱ | Live LLM is enabled by ambient environment, not an explicit operator action | `llmEnabled` reads `process.env` at construction and needs only `EIS_PLANNER_MODE=live` plus `OPENAI_API_KEY`; the shells load a repo-root `.env` before registering IPC, and `npm run dev` pins neither variable | A stray `.env` or exported shell variable silently turns the offline demo into a network-calling one, so the offline-first guarantee depends on ambient state nobody inspected. | medium | possible | Fail-closed default (scripted), both variables required, and the model's remaining surface is only triage and the incident report — never a plan, a coordinate or a setpoint (ADR D20). `llm.ts llmEnabled/createLlmClient`; `main.ts` dotenv load; `service.ts` constructor | No startup log line stating the resolved mode, no `.env.example` entry, no pin in the dev script. |
| `FM-90` | "Scripted mode never touches the network" is structural but untested and bypassable | The guarantee rests on the `llmEnabled` env gate, and `PlannerService`'s second constructor parameter accepts an injected `LlmClient` that bypasses it | A refactor, or a caller passing a client explicitly, silently breaks the offline guarantee with no failing test — an unexpected outbound request from a demo billed as offline. | medium | rare | The blast radius is now bounded by design: even a live client can only return a task list or an incident report, both schema-bound, and `service.report` discards a model verdict that disagrees with `reportVerdict` and falls back to the deterministic writer. `llm.ts llmEnabled/createLlmClient`; `service.ts propose` (deterministic only) / `report`; `cli.ts` | No test pins the truth table, no egress guard, no offline-mode assertion. |
| `FM-91` | "Mock data source" does not imply "no LLM" — the mock scenario calls the live planner through the bridge | `MockDataProvider` prefers `window.eis.plannerPropose` whenever the Electron bridge exists, and the bridge's mode is decided purely by env | The synthetic/offline UI issues real API calls from the path documented as "synthetic, no network". | medium | rare | Falls back to the in-process `ScriptedPlanner` without the bridge, and the demo scripts pin the mode. `MockDataProvider.ts:657-675`, `1149-1151`; `llm.ts:35` | The planner mode is not scoped to the `DataSource` kind. |
| `FM-92` ✱ | Default model id via the Responses API — every live call may 404 into a silent fallback | `DEFAULT_LLM_MODEL` is hardcoded and used unless `EIS_LLM_MODEL` is set; structured output is parsed via the Responses API | Live mode degrades to scripted on every proposal, with the failure visible only in an audit line, after paying the `FM-80` latency — a `--live-llm` demo runs entirely scripted while being presented as LLM-driven. | medium | possible | Overridable via `EIS_LLM_MODEL`; every failure lands in the scripted fallback; `zodTextFormat` confirmed present in the installed SDK. `llm.ts:3`, `11`, `73-80`, `101-108`; `planner/package.json:14` | Demo preflight validates only that the key is non-empty, and error classes are flattened into one `fallbackReason` string. |
| `FM-93` ✱ | Scripted fallback can itself fail verification when the anomaly is not mid-site | `ScriptedPlanner` assumes the anomaly is inside the geofence and outside every NFZ, but `App.tsx` selects the baked anomaly on **perimeter** containment and RF-derived anomalies carry arbitrary coordinates | The LLM-failure fallback produces a plan the verifier rejects or heavily corrects, so an API outage yields no executable plan at all. | medium | possible | Handled honestly: `escalationReason` is set, `effectivePlan` is withheld, and the approve gate blocks a rejected verdict; the mock path uses a stricter anomaly test. `scripted.ts:44-73`; `App.tsx:102-111`; `MockDataProvider.ts:702-719` | `App.tsx`'s containment test is looser than the scripted planner needs, and nothing re-anchors an out-of-geofence anomaly. |
| `FM-94` ✱ | Verification-record claims do not match the implementation | The record claims tool-calls-only with forced `tool_choice`/strict and an `ANTHROPIC_API_KEY` gate; the code uses the OpenAI Responses API with a zod text format and `OPENAI_API_KEY` | Readers of the audit trail are told a control exists that does not, and look for the key under the wrong name. | low | likely | The substantive guarantee holds by other means: a closed discriminated union plus `validateMissionPlan`'s whitelist. `VERIFY_PHASE3.md:75-77` vs `llm.ts:2-3`, `35`, `73-80`; `README.md:87-89` | No doc/code parity test; the "no temperature/top_p" claim is true only incidentally. |
| `FM-95` | Schema evasion via non-planner tool names | A model or spoofed response emits `manualInput`, or the contract-valid but sequence-forbidden `follow`/`orbit`/`goto_relative` | Would let model output reach a control surface it must never touch. | critical | rare | ✔ Three verified layers: a closed discriminated union over `goto_gps｜orbit_point｜hold｜rtl`, `validateMissionPlan`'s `MISSION_SEQUENCE_TOOLS` rejection, and the companion's `_parse_tool`; covered by a stub-client test. `llm.ts:15-25`, `86`; `validate.ts:16-19`, `116-118`, `143-150`; `planner_exec.py:558-621` | None material. `validateTool` silently drops contract-legal extra keys, so an evasion attempt leaves no audit trace. |
| `FM-96` | Model forging identity fields (`requestId` / `anomalyId` / `missionId`) | The model is required to emit these ids and could set them to another mission's | Would cross-wire correlation, approval and report attribution. | high | rare | ✔ All three are overwritten with caller-supplied values immediately after parsing; the model's values never leave the module. `llm.ts:87`, `98`; `service.ts:58` | None found. |
| `FM-97` ✱ | API refusal, malformed, or unparseable model output in live mode | The model refuses, returns no parsed object, or fails the schema on plan or report | Would block mission planning or report generation entirely if unhandled. | high | possible | ✔ Every failure class converges on the deterministic path (explicit throws plus catch-all to `ScriptedPlanner`/`writeIncidentReport`); no airborne behaviour depends on the LLM. `llm.ts:81`, `97`; `service.ts:73-75`, `91`; `test/service.test.ts:33-39` | Refusal, 401, rate limit and timeout all collapse into one raw error string, so neither operator nor audit can distinguish them. |

---

## 12. Ground — operator UI, Electron shell and IPC

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-42` ✱ | `executePlan` acks are discarded; the UI reports success unconditionally | The companion refuses `executePlan` for any of its six reasons | The operator sees "Mission approved", the audit records it as sent, `approvedAt` permanently disables the approve button, and the map draws a mission that never launched. | high | likely | ✔ **MITIGATED (this wave).** `doApprovePlan` now awaits the ack: `noteApprovalSent` records only that the command went out, `noteApproval` (record opened, gate closed) runs on `ack.success`, and `noteExecuteRefused` puts the vehicle's reason on the audit trail, opens no mission record and re-opens the approve gate. A thrown send takes the same refusal path. `App.tsx doApprovePlan`; `store/mission.ts noteApprovalSent/noteApproval/noteExecuteRefused`; `VerifierPanel.tsx` pending + refusal states; `ui/test/mission-store.test.ts` §FM-42 (7 cases) Companion half: Every dispatch refusal goes through `_refuse_plan`, which emits an audited, hash-chained `healthEvent` replayed to the next client -- so a companion refusal survives the UI that discards the ack. companion/tests/test_command_surface.py::test_a_refused_plan_leaves_a_replayable_health_event | The refusal is visible and recoverable. Remaining: `sendCommand` resolves the ack by `command` name, not `requestId`, so two `executePlan`s in flight for different plans could cross — unreachable today because the gate is shut while one is pending. |
| `FM-43` ✱ | Re-planning the same anomaly silently keeps the stale plan and verification | A second `propose` for the same anomaly (request ids are deterministic) | `ingestPlan` drops the new plan and `ingestVerification` ignores the new verdict. The toast announces the fresh result while approving sends the stale `effectivePlan`. | high | possible | ✔ **MITIGATED (this wave).** A second plan for the same (deterministic) requestId now SUPERSEDES the stored one and takes its verification with it, so nothing can be approved against a verdict that was never rendered for it; the supersede is audited with a revision number. A proposal the operator already dispatched is never overwritten — that is the record of what flew — and the collision is recorded instead. `store/mission.ts ingestPlan/ingestVerification`; `PlanProposal.revision`; `ui/test/mission-store.test.ts` §FM-43 (6 cases) | None remaining for the store. Producers that want two live proposals for one cue must still issue distinct requestIds; `moveAnomaly` and the unattended rail both do. |
| `FM-57` ✱ | Verification is a stale snapshot — nothing is re-verified at approve time | The operator inspects the plan for more than a few seconds before hold-to-approve | The verdict reflects propose-time battery, wind, `navSource`, `rfEvents` and readiness. Rising wind, a nav-source drop, a new hostile-drone event or SoC drain are invisible behind a green PASS that can be minutes old. | high | likely | ✔ `App.tsx` re-checks readiness only; the companion re-checks `site_valid`, manual engagement and latched failsafe. `App.tsx:446-456`, `562-585`; `VerifierPanel.tsx:213-235`; `service.ts:51-79` | No timestamp or freshness indicator on the verdict. `FM-56` is the companion half of the same gap. |
| `FM-58` | Name-based ack FIFO swaps results between same-named commands | Two in-flight commands with the same name | `resolvePending` matches by command name, so ack #2 can resolve command #1's promise. Latent until `executePlan` results are surfaced, then the wrong outcome is attributed to the wrong plan. | medium | possible | Documented in the contract; a 4 s timeout resolves a synthetic failure; the UI ignores acks today. `LiveDataProvider.ts:244-250`, `365-371`; `contract/index.ts:279-289` | The ack envelope has no correlation id, so the fix is a contract change across all three mirrors. |
| `FM-59` ✱ | Deny sends nothing to the vehicle and cannot be undone | The operator denies a proposal | Only local state changes — no `abortPlan`, no companion notification, no planner feedback. A mis-click permanently disables approval and recovery hits the stale-requestId mode (`FM-43`). | medium | possible | A separate `abortPlan` control exists on the `PlannerBanner` while `controlSource` is `planner`. `App.tsx:457-460`; `store/mission.ts:159-166` | Denial is never communicated to the planner either. |
| `FM-70` | A schema-rejected plan's verdict never reaches the verifier panel | `validateMissionPlan` throws on a plan with no string `requestId` | `verifyMission` returns `requestId: "invalid"`, `ingestVerification` finds no proposal and silently drops the rejection — real refusal, zero visibility. | low | rare | The rejection is correct and fail-closed; `blockedChecks` marks downstream checks not evaluated. `verifier.ts:468-471`, `620-625`; `validate.ts:116-150`; `store/mission.ts:130-131` | Only observability is missing. |
| `FM-81` ✱ | No retry after a failed or rejected proposal — the UI latches permanently | `startLiveInspection` sets `liveStartedRef` before calling the planner and resets it only in the catch block, so a resolved-but-rejected proposal leaves it latched | The inspection button is permanently disabled ("Inspection planned"); there is no way to re-request a plan without restarting Electron. | high | likely | ✔ **MITIGATED (this wave).** The latch is set only on a verdict the operator can act on. A rejected verdict, a refused task and a thrown planner all leave it open, and denying a plan clears it. The button renders from state (not a ref), so clearing the latch actually re-enables it. `App.tsx setInspectionLatch/startLiveInspection/doDenyPlan` | None remaining. The ref survives as the re-entrancy guard only, so two fast clicks still cannot both start a proposal. |
| `FM-82` ✱ | Cold `require` of the planner bundle blocks the Electron main process ~3 s on the first proposal | `phase3Host` lazily requires `ground/planner/dist` inside `planner:propose`; `index.ts` re-exports `llm.ts`, which imports the OpenAI SDK at module load even in scripted mode | Measured 3,123 ms synchronous require: no IPC, no repaint, telemetry broadcast stalled exactly when the demo narrative points at the planner. | medium | likely | ✔ **MITIGATED (this wave).** The planner bundle is loaded on `setImmediate` at handler registration, so the ~3 s `require` lands while the window is still painting rather than on the first proposal. A warm-up failure is remembered, never thrown — warm-up must not stop the app starting — and `planner:status` reports it. `phase3Host.ts warmPlanner` (identical in both shells) | The cost is moved, not removed: `index.ts` still re-exports `llm.ts`, which imports the OpenAI SDK at module load. Splitting that import is a separate change. |
| `FM-85` ✱ | Scripted fallback is invisible at plan time — the operator cannot tell which planner ran | Any live-mode API failure silently degrades to `ScriptedPlanner`; the button label is hardcoded "Start scripted inspection" in both modes | Two-way attribution failure: a fallen-back live demo still looks LLM-planned, and a genuine LLM plan is labelled scripted. The core demo claim is unprovable on screen. | medium | likely | `source`/`attempts`/`fallbackReason` are written to the audit log line. `App.tsx:588`, `591-595`, `719`; `service.ts:73-75`; `VerifierPanel.tsx:135-143` | `PlanProposal` carries no source, and there is no LIVE/SCRIPTED badge anywhere in the operator surface. |
| `FM-128` | `observation` messages never carry `missionId`, so reports can bind to the wrong request | An observation arriving after a second plan is dispatched, or a reconnect leaving `executedRequestId` stale | `App.tsx` falls back to the store's `executedRequestId`, so evidence from one pass can be attached to a different mission's anomaly and plan in the audit record. | medium | possible | `reportingRef` de-dupes by `requestId` and the contract declares `missionId` optional. `vision/multimodal.py:181-205`; `contract/index.ts:375`; `App.tsx:295-302` | `stagingId` is emitted but never cross-checked against the anomaly the plan targeted. |
| `FM-130` ✱ | Packaged/built UI is mock-only — `VITE_EIS_DATA_SOURCE` is never set at build time | Any path serving `ground/ui/dist`: the NSIS installer, `electron .` without `EIS_DEV`, or `vite preview` | SITL, the companion and the socket all run while the GCS window renders a fully synthetic flight with no socket at all — the built bundle contains zero `ws://` strings. | critical | possible | ✔ `StatusBar` renders a MOCK/SITL/LIVE chip, and `demo.ps1` uses the dev server, which does pick up the env. `dataSource/index.ts:8-9`; `setup-ground.ps1:97`; `ground/app/windows/package.json:11-12`; `StatusBar.tsx:127` | No build-time assertion and no runtime check that a live build opened a socket. `npm run dist` permanently bakes a mock-only installer. |
| `FM-131` ✱ | Vite port 5173 is not strict and Electron loads whatever answers there | 5173 already bound — a teammate's Console dev server (which pins 5173), a sibling worktree's UI, or a leftover Vite | Vite silently binds 5174, `wait-on` succeeds against the foreign server, and Electron renders someone else's page in the ground-station window. In the reverse order the Console's hub detection breaks and every Hub call 404s against Vite. | high | likely | ✔ **MITIGATED (this wave).** `strictPort: true` — Vite binds 5173 or refuses to start, so it can no longer slide to 5174 and leave the shell rendering a foreign app. Both the UI and the shell read `EIS_UI_PORT`, so two stacks on one machine move together. `verifyDevServer()` fetches the dev URL and checks the page is actually this UI before the window loads, reporting a mismatch by name. `ground/ui/vite.config.ts`; `app/{windows,linux}/src/main.ts`; `docs/runbook.md` §Stage 0.5 | The check logs rather than blocks: a false negative (a changed title) must not stop a demo. Ports 8765/5760 are still not probed — they belong to the launcher, not the shell. |
| `FM-140` ✱ | Default basemap is a remote tile server — a network call on the default demo path | `MapPanel` renders with the default `mapTiles: "satellite"`; no `EIS_*` switch disables it | Contradicts the stated offline guarantee: offline, every tile fails to a CSS grid; on captive-portal wifi the requests hang or redirect and leak DNS. | medium | likely | A first-tile-error watcher falls back to an offline view keeping markers, trail, geofence and the lat/lon readout with an OFFLINE badge. `MapPanel.tsx:24-40`, `117-142`; `store/settings.ts:64` | No bundled local basemap and no offline-by-default setting; the satellite tiles are the only fully local imagery. |
| `FM-148` | `satellite:loadTiles` IPC and its `extraResources` copy are dead code | Always — the renderer imports baked tiles through Vite aliases and never calls the handler | ~870 KB is copied into every packaged build for an uninvoked handler, and the documented "renderer can fall back" recovery path does not exist. | low | likely | ✔ **MITIGATED (this wave).** The handler, its preload method, its bridge type and its `extraResources` entry are removed from BOTH shells. The renderer's Vite-alias import was always the only path; a recovery route that does not exist is worse than none, because an operator can be told to rely on it. `app/{windows,linux}/src/ipc.ts`, `preload.ts`, `electron-builder.yml`; `ui/src/vite-env.d.ts`; `PORT_AUDIT.md` | None remaining. |
| `FM-149` ✱ | SDR sidecar spawn has no `error` listener — a failed spawn crashes the Electron main process | `sdr:start` fires automatically on mount; `pythonPath` falls back to bare `python`/`python3`, which may not exist | The unhandled `ChildProcess` `error` event (and `createInterface` on a null stdout) throws in the main process, so the entire ground-control window disappears mid-demo instead of degrading to "SDR unavailable". | high | rare | ✔ **MITIGATED (this wave).** `child.on('error')` is attached, `spawn` is wrapped for a synchronous throw, the sidecar script's existence is checked first, and `createInterface` is guarded on a null `stdout`/`stderr`. Every failure path reports an `sdr` `no_device` health event: the SDR is an optional sensor and now degrades to "unavailable" instead of ending the session. `app/{windows,linux}/src/phase3Host.ts` (identical); `PORT_AUDIT.md` | None remaining for the start path. A sidecar that starts and then crashes still surfaces only through `close`, which is the pre-existing (and correct) behaviour. |
| `FM-150` | Full `process.env` (including `OPENAI_API_KEY`) is forwarded into the spawned SDR sidecar | `sdr:start` spawns `sidecar.py` with `{...process.env}` while the main process holds the key under `--live-llm` | The key and every `EIS_*` setting are inherited by a subprocess with no use for them, whose stderr is broadcast verbatim into the UI audit trail and recordings. | low | rare | The sidecar is receive-only with no transmit or outbound network path, and its stdout is JSON-validated before broadcast. `phase3Host.ts:67-68`, `77-81`; `demo.ps1:81-83`, `179` | No env allowlist for the child and no filtering of its stderr. |
| `FM-151` ✱ | No Content-Security-Policy; a stray navigation opens the OS browser over the demo window | Any non-`file://` renderer navigation, e.g. a Leaflet attribution link | `will-navigate` preventDefaults and then calls `shell.openExternal`, popping a browser window over the full-screen ground station. With no CSP the renderer may also load arbitrary remote subresources. | low | rare | `contextIsolation`/`sandbox`/`webSecurity` on, `nodeIntegration` off, navigation blocked, `window.open` denied. `main.ts:71-79`, `88-104`; `ground/ui/index.html:1-12` | `openExternal` is the action taken for every blocked navigation rather than a silent drop, and no CSP constrains subresource origins. |

---

## 13. Ground — satellite change detection

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-107` ✱ | Baked satellite tiles and anomalies go stale on a site cutover with nothing to detect it | `site/site.json` lands without someone running `ground/satellite npm run generate` | `tiles.json` bounds, before/after PNGs and `anomalies.json` stay pinned to the previous geometry, so the operator approves a mission against an anomaly at the old staging coordinate over an old tile footprint. | high | possible | ✔ `make-tiles` writes provenance (`generated.siteFile`/`seed`) and self-checks the anomaly against `staging[0]` at generation time. `ground/satellite/scripts/make-tiles.mjs:284-319`; `src/node/baked.ts:67-78` | `generated.siteFile` is read by nothing: no load-time comparison against the resolved `EIS_SITE_FILE`, no mtime/hash check, and generation is a manual npm script wired into no launcher or CI. |
| `FM-108` ✱ | Two inconsistent staleness heuristics for the baked anomaly; the live path uses the weaker one | Site geometry moves a staging point while the baked anomaly still falls inside the perimeter | The mock path re-anchors (perimeter **and** staging within 100 m) while the live path accepts on perimeter alone and dispatches against a stale coordinate — mock and live demos disagree about where the anomaly is. | medium | possible | Both paths re-anchor to `staging[0]` once their own predicate fails, so neither produces a coordinate outside the site. `MockDataProvider.ts:700-719`; `App.tsx:102-110` | Neither heuristic is shared code or unit-tested, and neither consults `tiles.json`, so a re-anchored anomaly is still drawn on stale pixels. |
| `FM-109` ✱ | Anomaly markers are projected onto the tile with no bounds check and can render off-image | An anomaly outside `tiles.json` bounds — re-anchored, RF/SAR-sourced, or from a different georef | `latLonToPixel` extrapolates with no clamp and the panel converts straight to percentages, so the marker lands outside the tile with no indication it is off-frame. | low | possible | ✔ **MITIGATED (this wave).** `projectToTile` returns the raw projection, an `onTile` flag and edge-clamped coordinates, and the panel uses all three: an off-tile cue is clamped to the nearest edge, drawn dashed and dimmed, labelled `· off-tile`, and counted in an `n OFF-TILE` badge. `satellite/src/georef.ts inBounds/projectToTile`; `SatellitePanel.tsx`; `satellite/test/bounds.test.ts` (11 cases, incl. non-finite coordinates) | None remaining. `validateGeoRef` still checks only the sidecar's shape; the bounds question is asked per cue, which is where it belongs. |
| `FM-110` | Tile georef carries ~20 % axis anisotropy that nothing validates | Any site whose bounding box is not shaped to equalise m/px on both axes — including the current stub (3.06 vs 2.54 m/px) | Blob `areaPx`, bbox aspect and thumbnail crops are anisotropic in ground units, so `blobConfidence`'s area term and the synthetic vehicle ellipse are mis-scaled against real object sizes. | low | possible | `detectAnomalies` verifies the georef pixel dimensions match the supplied tiles. `georef.ts:38-45`; `detect.ts:174-179`; `make-tiles.mjs:258-259` | No assertion that `mpp.x ≈ mpp.y`, and no conversion of `areaPx` to m² before use as a confidence input. |
| `FM-111` ✱ | Thumbnail data URLs are uncompressed PNG; live-mode detection can put megabytes of base64 on the wire | `EIS_SAT_MODE=live`, or any `detectFromPngBytes`/`computeBakedResult` call with thumbnails on (the default) and up to 16 blobs | zlib "stored" blocks mean a 232 px crop is ~281 KiB and a near-full-tile crop ~1.34 MiB; times 16 that is tens of MiB flowing through the `DataSource`, Electron IPC, the mission store and the report markdown. | high | possible | ✔ `SatellitePanel` opts out with `{thumbnails: false}` and `report.ts` refuses to inline a data URL. `ground/satellite/src/png.ts:8-9`, `49-72`; `detect.ts:23-30`, `202-227`; `node/baked.ts:55-63` | No dimension cap, downscale, byte budget or size test; nothing truncates before the string reaches the store or the audit log. |
| `FM-123` | SAR rail collapses all changed pixels into one anomaly at their arithmetic mean | A SAR chip with two or more spatially separate changes above threshold | Every hit is averaged into one anomaly at the centroid — for two corner changes, an empty point in the middle. There is no connected-component step. | medium | possible | ✔ **MITIGATED (this wave).** The SAR rail runs the same connected-component pass the optical rail uses (`detect.findBlobs`), so two spatially separate changes are two anomalies instead of one at their arithmetic mean. `sarLogRatio` is now SIGNED and the cue kind names the direction (`sar_backscatter_increase` / `_decrease`); `sarMagnitudeDb` is the absolute value for callers that only want "how much". `satellite/src/sar.ts`; `satellite/test/sar.test.ts` §FM-123 (the two-corner case asserts neither anomaly lands in the empty middle) | None remaining. The rail still has no production caller, but the multi-change case is now exercised by tests rather than unexercised. |
| `FM-124` | SAR georef is assembled from `provenance.json` and used without `validateGeoRef` | Malformed or inverted bounds in `data/tiles/komati/provenance.json` | `detectSarChanges` never validates, so inverted or non-finite bounds silently produce a well-formed anomaly at a wrong or NaN coordinate that downstream validation accepts. | medium | possible | The optical path does call `validateGeoRef`, and `baked.ts` validates `tiles.json` on load. `ground/satellite/src/node/sar.ts:5-13`; `src/sar.ts:40-56`; `detect.ts:238` | Anomaly validation checks lat/lon range but cannot detect a well-formed *wrong* coordinate. |
| `FM-125` | SAR anomaly ships a hardcoded JSON path as its image thumbnail | Any SAR detection | `thumbnail` is `data/tiles/komati/sar-after.json` — a fixture path to a data file, rendered into the incident report as a thumbnail evidence reference for a human reviewer. | low | possible | ✔ **MITIGATED (this wave).** `detectSarChanges` REFUSES a non-image thumbnail reference rather than passing it to a human reviewer, and the reference is a parameter rather than a hardcoded path. `loadBakedSar` resolves it against `dataDir` from the provenance sidecar or a conventional `sar-after.{png,jpg,webp}`, and emits NO thumbnail when the dataset ships no image — which the report writer already describes honestly. `satellite/src/sar.ts`; `satellite/src/node/sar.ts`; `satellite/test/sar.test.ts` §FM-125 | None remaining for the SAR rail. `Anomaly.thumbnail` still has no content-type check at the contract layer, so another producer could reintroduce the same shape. |
| `FM-126` | Optical blob confidence floors at 0.30 and saturates at ~460 m²; nothing downstream filters on it | Any change blob clearing `minBlobAreaPx = 10`, including noise clusters or an illumination edge | Every reported blob scores ≥ 0.30 and a car and a warehouse score identically. No verifier, policy or planner check gates on `Anomaly.confidence`, so a marginal cue is dispatched with full authority. | medium | possible | `make-tiles` keeps synthetic acquisition noise well under the detection threshold for the baked tiles. `detect.ts:23-30`, `174-179`; `make-tiles.mjs:238-249` | No minimum-confidence gate between detection and approval; `ESCALATE_CONFIDENCE` applies to the observation, not the cue. |

---

## 14. Site model and cross-component contract

The site file is the only shared source of geometry. Every mode here is a
divergence between two consumers that both believe they are authoritative.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-67` ✱ | Ground and companion site validators disagree on required fields | A teammate-authored `site.json` lacking `clutter`, `staging`, `nfz` or an explicit `geofence` | The companion loads it (defaulting to `[]`/perimeter), uploads a fence and arms, while the ground planner throws and refuses every mission — two authorities disagree about whether the site is valid. | high | possible | ✔ `SITE_CONTRACT` declares all nine fields required and both validators enforce what they check, loudly. `site.ts:182`, `188-191`, `223-241`; `companion site.py:164`, `171-179`, `252-253` | The contract's `site_valid` row assumes one shared verdict; here `refuse` fires on one side and `accept` on the other. |
| `FM-68` ✱ | Site-file resolution diverges across four consumers with no shared identity | `EIS_SITE_FILE` unset, mistyped, or relative while the planner host, the UI, the Electron `site:load` handler and the companion each resolve it differently | `phase3Host` and the UI's site loader silently fall back to `site/site.stub.json` while `ipc.ts` throws and the companion marks `_site_valid` false. Three anchors (`process.cwd`, app root, file-relative) can resolve one relative path to three files, so the verifier, the map and the vehicle can each be checking different geometry with nothing able to detect it. | critical | possible | ✔ `demo.ps1`/`demo.sh` resolve, existence-check and export an **absolute** `EIS_SITE_FILE`, collapsing all resolvers onto one file; the companion refuses `executePlan` when its own site is invalid. `phase3Host.ts:48-51`; `ground/ui/src/site/index.ts:30-49`; `ipc.ts:128-142`; `planner/src/site.ts:87-99`; `companion/site.py:127-144`; `demo.ps1:98-111` | Two handlers in the same Electron process implement opposite fallback policies. No site hash or version is exchanged on any wire message, so a divergence is undetectable, and the companion refusal does not cover two sides loading different **valid** sites. |
| `FM-69` ✱ | The planner's site model is memoized for the app lifetime | The site file is edited or cut over from stub to production while the ground station is running | `getPlanner` caches the `PlannerService` forever while `site:load` re-reads per invoke: the map updates and the verifier does not, with nothing indicating staleness. | medium | possible | **None.** `phase3Host.ts:40-54`; `ipc.ts:132-142`; `service.ts:42-44` | No reload IPC and no site identity in any message. |
| `FM-158` | The only mechanical guard against three-mirror contract drift never runs in CI | `contract-parity.test.ts` executes only under `ground/planner npm test`; no CI job covers planner or satellite | Any TypeScript mirror can drift while every CI job passes — the UI job typechecks `ground/ui` alone and never compiles the planner mirror or `shared.ts`. | high | likely | ✔ The parity test itself is thorough (20 type-level assertions plus 16 for `shared.ts`) and `npm test` chains build + typecheck + vitest. `ground/planner/test/contract-parity.test.ts:1-79`; `planner/package.json:10`; `.github/workflows/ci.yml` (no planner/satellite job) | [`REPOSITORY_REVIEW.md`](REPOSITORY_REVIEW.md) requires parity tests for planner mirrors; they exist but are never invoked automatically. |
| `FM-159` ✱ | `shared/shared.py` — the third contract mirror — has no parity guard at all | The parity test imports the `.ts` mirror; the Python mirror the companion API implements is checked by nothing | Python wire types can silently diverge from the authoritative contract and no test, typecheck or CI job notices. | high | possible | ✔ `config.py` loads `PROFILE_SPEED_MPS` from `shared.py` by path and a test pins those values. `contract-parity.test.ts:13`, `64-79`; `config.py:77-103`; `tests/test_app_wiring.py:644` | That covers one dict, not the message envelopes, and `config.py` swallows every exception from the load, so a deleted or broken `shared.py` produces no failure. |
| `FM-165` ✱ | Three incompatible site models and three geographic anchors | A WGS84 lat/lon site, an ENU-anchored station model, a third facility file, and a simulator default home on another continent | No shared field names, frame or limit vocabulary: any plan, fence or detection crossing the boundary is geographically meaningless, and an un-overridden default home spawns the aircraft ~10,000 km away. | critical | possible | ✔ `demo.sh` derives `SITL_HOME` from the selected site file and passes it to both backends; [`BASIC_DEMO_PENDING.md`](BASIC_DEMO_PENDING.md) rules "run one named scenario, do not combine sites". `site/site.stub.json:2`; `SITE_CONTRACT.md:23-57`; `sim/site/site.json`; `contracts/site.py`; `sim/run_sitl.sh:53` | The choice is recorded as an unfilled decision. Nothing refuses a plan whose coordinates fall thousands of km outside the selected site. |
| `FM-166` ✱ | Fleet-of-three site files have no counterpart in the site contract | A teammate `site.json` carrying a `fleet[]` array consumed by `home_for(drone_id)`; this contract has no fleet concept and the companion is single-vehicle | A fleet-shaped file fails this validator and every mission is refused; a single-site file raises an unguarded `StopIteration` in the other launcher before any SITL starts. | high | possible | ✔ This side fails closed and loudly, per `SITE_CONTRACT` and the contract's `site_valid` row. `sim/ardupilot/params.py:39-42`; `sim/site/gen_site.py:6-22`; `SITE_CONTRACT.md:59-61`, `106-113` | Deferred as "keep the demo single-vehicle" while `params.py` still raises `StopIteration` rather than a diagnosable error. |
| `FM-167` ✱ | `console/public/site.json` is a generated copy that goes stale silently | `gen_site.py` writes three artefacts and `make site`/`make sim` each take `FLEET` independently | The Console can render pads, fences and no-fly circles that no longer match what ArduPilot and the validator enforce — the projector disagrees with the aircraft. | medium | possible | `launch_sim.py` regenerates before starting any SITL, so the `make sim` path self-heals. `sim/site/gen_site.py:2-22`; `scripts/launch_sim.py:42`, `68` | `make console` and `make hub` do not regenerate, the copy is tracked and can be committed stale, and nothing hashes it against its source. |
| `FM-168` ✱ | `EIS_SITE_FILE` is defined as repo-root-relative, but "repo root" is ambiguous across layouts | Documented as repo-root-relative while each consumer resolves against its own root (file-relative, app root, or `process.cwd`) | Documented shell commands run from the merged root resolve against a tree where `site/` does not exist, or against a different tree than the companion used. | high | possible | ✔ File-relative resolvers are preserved by moving the runtime as one unit, and `demo.sh` absolutises then re-exports `EIS_SITE_FILE` so children agree. `SITE_CONTRACT.md:9-11`; `companion/site.py:49-50`, `124`; `ui/vite.config.ts:8`, `20-21`; `demo.sh:78-83` | The planner's `process.cwd()` default is unprotected — any planner CLI run from the merged root resolves against the wrong tree. |
| `FM-169` ✱ | The site-contract schema example shows an aliased thermal image while the stub has moved on | The doc shows `thermal_image` aliased to the RGB placeholder; `site.stub.json` names distinct thermal PNGs | A reader implements the aliased fixture; and if those PNGs are absent from a clone, the stub references files that do not exist, failing the paired-modality observation at demo time. | medium | possible | ✔ The schema example names the distinct thermal variant (`stage-a-thermal.png`), the aliasing paragraph is replaced by the current rule, and a test asserts every fixture the stub references exists in the checkout and carries PNG/JPEG magic bytes. All four PNGs are tracked. companion/tests/test_perception_honesty.py::test_every_site_fixture_referenced_by_the_stub_actually_exists | Closed. |
| `FM-178` | Manual-control and command envelopes are wholly different protocols at the Hub seam | Discriminated `vehicleId`/epoch-ms envelopes with `engageManual`/`manualInput` vs the Hub's `cmd_id`/ack `drone_id`/ISO protocol with `ManualCommand` `velocity_ned` and its own `ClampEvent` resume/abort modes | Neither side can consume the other's frames. A naive bridge would drop vehicle identity, lose the one-active-`controlSource` invariant, and import automatic-resume semantics where this arbiter must hold. | high | rare | ✔ Documented as a required boundary: convert identity and time at ingestion, drop automatic resume at the adapter, keep the demo single-vehicle with manual release holding. `contract/index.ts:241-251`, `302-304`; `contracts/protocol.py`; `hub/manual.py:14`; `REPOSITORY_REVIEW.md:44`, `48` | No adapter code exists, so the mapping is untested. `hub/manual.py` is a second independent clamp implementation with no parity test against this one. |

---

## 15. Build, launch and simulation environment

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-83` ✱ | The shell executes `ground/planner/dist`, which no setup or shell build script produces or refreshes | Fresh clone, or any edit to `llm`/`report`/`service`/`verifier.ts`; `dist/` and `node_modules/` are gitignored and `setup-ground.ps1` installs/builds only `ground/ui` and `ground/app` | `planner:propose` throws "Cannot find module" on a fresh clone — the LLM→verifier→approve beat dead-ends with a "Planning failed" toast. On an updated tree the app silently runs a stale planner, including any fix to any mode in this catalogue. | critical | likely | ✔ **MITIGATED (this wave).** Both setup scripts and both `make build-ground*` targets now install and build `ground/planner` in load order and then ASSERT the four runtime artefacts exist (`planner/dist/index.js`, `ui/dist/index.html`, `dist-electron/main.js`, `dist-electron/preload.js`), failing loudly when one is missing. The main process checks the bundle path before `require` and names the artefact and the command that builds it. `scripts/setup-ground.ps1`; `scripts/setup-ground-linux.sh`; `Makefile build-ground/check-ground-artefacts`; `phase3Host.ts getPlanner`; `docs/runbook.md` §Stage 0.5 | Freshness is not checked, only existence: editing `ground/planner` and not rebuilding still runs the previous build. Both scripts and the runbook say so explicitly. |
| `FM-132` ✱ | `dist-electron` is never compiled by setup or by the demo launcher | Fresh clone → `setup-ground.ps1` → `demo.ps1`; `package.json` `main` is `dist-electron/main.js`, `dev:electron` runs `electron .` directly, and `dist-electron/` is gitignored | Electron exits immediately with "Cannot find module dist-electron/main.js" and the launcher aborts with "Ground station exited unexpectedly". | high | possible | ✔ **MITIGATED (this wave).** Both setup scripts and both `make build-ground*` targets run the shell's `build:electron` and then assert `dist-electron/main.js` and `dist-electron/preload.js` exist. `scripts/setup-ground.ps1`; `scripts/setup-ground-linux.sh`; `Makefile check-ground-artefacts` | Same residue as `FM-83`: existence is asserted, freshness is not. |
| `FM-133` ✱ | UDP 14550 bind collision with any running GCS | The non-Docker backend sets `EIS_FC_CONNECTION=udp:127.0.0.1:14550`, which pymavlink **binds** — the same port a desktop GCS or a sibling worktree's companion listens on | `Vehicle.connect` fails, the companion continues in degraded mode with no FC (`FM-145`), and the demo stalls at wait-ready before aborting with no message naming the port. | high | possible | ✔ Readiness reports "flight controller not ready", so nothing arms blind, and `connect` waits 30 s for a heartbeat. `demo.ps1:149`, `167`, `331`; `demo.sh:120`, `135`; `vehicle.py:163`, `186-199` | Degrading turns a 2-second diagnosable failure into a 75–150 s silent stall. No preflight probe of 14550. |
| `FM-134` ✱ | `run_sitl.sh`'s `sim_vehicle.py` branch passes `--no-mavproxy`, so its `--out=udp:14550` endpoint never exists | Docker unavailable and `sim_vehicle.py` on PATH — exactly the condition under which `demo.ps1` selects the 14550 transport | `--out` is a MAVProxy option and MAVProxy never starts, so only `tcp:5760` is served. The companion listens on 14550 forever, times out, degrades, and the demo aborts. | high | possible | **None.** `sim/run_sitl.sh:80-93`, `121-152`; `demo.ps1:159-168`, `334` | The backend probe selects a transport without verifying an endpoint is reachable; the TCP→UDP bridge exists only on the fallback path the `exec` makes unreachable. |
| `FM-135` ✱ | WSL/native SITL backend skips the required-parameter bootstrap entirely | Docker Desktop not running, so the launcher takes the WSL/native branch (the bootstrap call sits only inside the Docker branch) | `BATT_MONITOR`, `SIM_BATT_*`, `VISO_TYPE`/`SERIAL5_PROTOCOL`, `EK3_SRC2/3_*`, `SIM_VICON_*` and `SIM_GPS1_ENABLE` are never set, so the GPS-failover, battery and extnav demos have no simulated backing — while the launcher prints "preflight OK". | high | possible | ✖ Launcher-side; the launcher this row cites is not present in this tree. **DOCUMENTED (this wave)** in `docs/runbook.md` §Stage 0.5: read the parameters back from the running SITL (`param show BATT_MONITOR`, `param show SIM_GPS1_ENABLE`) before demonstrating any battery, GPS-failover or extnav beat, rather than trusting a "preflight OK" message. | Unchanged: the bootstrap still needs moving outside the Docker branch, with a post-start verification of the parameters this catalogue depends on. |
| `FM-136` ✱ | Stale persisted SITL EEPROM: a previous run's polygon fence and params survive into the next | The launcher reuses a named volume and never passes `--wipe` | Today's home can sit outside a stored fence, so ArduPilot refuses to arm or breaches right after takeoff — an opaque arm rejection unrelated to anything the operator changed. | high | possible | ✔ The companion re-uploads the current perimeter on every connect and `--home` is passed each start. `demo.ps1:139`, `317-322`; `demo.sh:117`, `273-277`; `app.py:506-508`; `vehicle.py:1155-1181` | The re-upload is best-effort and happens after connect, so a failed upload (`FM-15`) leaves the stale fence armed. Nothing clears or validates fence state at launch. |
| `FM-137` ✱ | Port 8765 collision after an aborted run kills the companion with an unrelated message | An orphan companion (or a sibling worktree's) still holds 8765 and `websockets.serve` raises `OSError` | `await api.start()` is unwrapped, so the companion exits at startup and the launcher reports a readiness failure that never names the port. | high | likely | ✔ `demo.ps1`'s `finally` block taskkills its own tree and force-removes its own container on a clean Ctrl-C. `api/server.py:140-151`; `app.py:503-504`; `demo.ps1:304-316`, `342-343` | Cleanup covers only the container name and only when `finally` runs. Nothing frees or probes 8765/5173/5760, and orphan processes are never reaped. |
| `FM-138` ✱ | SITL param bootstrap is all-or-nothing across 30 params | Any FC build whose parameter set differs from the pinned ArduCopter image | One missing cosmetic param raises `SystemExit` and the launcher aborts before anything starts. | medium | possible | Three retries with a 3 s confirmation window and tolerance comparison; the Docker path pins and verifies an exact binary. `demo.ps1:141`, `155-157`, `193-233`; `demo.sh:156-190` | No required/optional split, no legacy-name aliases, no reduced-capability path. |
| `FM-139` ✱ | `gps_loss` injection uses only `SIM_GPS1_ENABLE` — no legacy `SIM_GPS_DISABLE` fallback | `--gps-loss` against firmware older than Copter 4.5 | The fault ack is `success=false`, the injector raises `SystemExit`, and the whole scheduled fault sequence stops — the GPS-loss failover beat never happens. | medium | possible | ✔ `_test_fault` tries `SIM_GPS1_ENABLE` then falls back to the legacy `SIM_GPS_DISABLE`, and the internal `_faults` flag is set ONLY when the simulator accepted the write -- so the companion never believes in a fault the simulator does not have. companion/tests/test_command_surface.py (2 injection tests) | Closed. |
| `FM-142` | `scripts/run-sim-e2e.ps1` does not parse under Windows PowerShell 5.1 | Running the documented acceptance gate with the default `powershell.exe` (6 parse errors from `??` operators) | Immediate parse failure, so the acceptance gate cannot run at all without PowerShell 7, and the pre-demo confidence check is silently unavailable. | medium | likely | ✔ **MITIGATED (this wave).** The script is now Windows PowerShell 5.1 compatible: the two `??` operators are gone and the environment defaults are resolved in 5.1 syntax, so the acceptance gate runs on whatever PowerShell the operator has. `#Requires -Version 7` would NOT have helped — a parse error precedes it. `setup-ground.ps1` had the same class of defect from a UTF-8 em-dash in a BOM-less file read as ANSI; both scripts are now ASCII-only and both verified with `Parser::ParseFile` under 5.1.19041. `scripts/run-sim-e2e.ps1` header; `docs/runbook.md` §Stage 0.5 | Nothing enforces it: a future `??` would reintroduce the mode. The header states the rule and gives the one-line parser check. |
| `FM-143` ✱ | wait-ready can burn ~150 s before the launcher gives up | Any startup problem leaving the companion running but not ready — the connect budget and the readiness budget stack at 75 s each | Silence for up to two and a half minutes before "companion did not become ready", indistinguishable from a hang, with no intermediate signal about the blocking reason. | medium | possible | The final exit prints the last readiness reasons, and the companion pushes readiness every 5 s while disarmed. `demo.ps1:240-269`, `294`; `demo.sh:197-218`; `app.py:868-878` | The helper discards those periodic reasons instead of echoing them, and the two budgets stack instead of sharing a deadline. |
| `FM-144` ✱ | Docker probe can stall, and its absence silently reroutes to an untested backend | `docker.exe` on PATH but the daemon stopped, so `docker info` blocks; per the ADR this host's WSL has no native SITL | A perceptible preflight stall, then a silent backend switch that either throws or proceeds down the path that skips the param bootstrap (`FM-135`) and mis-selects the 14550 transport (`FM-134`). | medium | possible | The WSL branch probes for `run_sitl.sh` plus `sim_vehicle.py` or a built binary and throws a clear message when neither exists. `demo.ps1:143-171`; `demo.sh:121-140`; `ADR-hackathon.md:61-68` | No timeout on `docker info`, and the switch is not announced as a capability downgrade — the operator sees "preflight OK (wsl)". |
| `FM-146` ✱ | Demo logs use fixed filenames with no rotation | Two overlapping runs, or an orphan still holding `logs/demo/companion.out.log` | `Start-Process` fails with a file-in-use error mid-launch, leaving a half-started demo and a stranded container. On a hard kill the Docker SITL log is lost entirely. | low | possible | `logs/` is gitignored, the directory is created idempotently, each subprocess gets its own pair, and the path is printed on start. `demo.ps1:95-96`, `187-188`, `338-377`; `demo.sh:76-77` | No per-run timestamped directory, no size cap, and generated helper scripts are rewritten in place each run. |
| `FM-147` ✱ | `demo.ps1` site validation raises an opaque .NET error under StrictMode 2.0 | A site JSON where `home` exists but `lat`/`lon`/`alt_m` is missing | The launcher aborts with "The property 'alt_m' cannot be found on this object" instead of the authored message naming the site file. | low | possible | Still a hard abort at preflight before anything starts, and the invalid-JSON path does produce the intended message. `demo.ps1:6`, `112-117`; `demo.sh:96-110` | `demo.sh`'s Python equivalent validates correctly, so the two launchers disagree on error quality for the same input. |
| `FM-154` ✱ | The Docker demo path loads no `FENCE_*` params at all — the documented firmware backstop does not exist | `demo.sh` prefers Docker whenever reachable; the bootstrap sets 30 named params, none of them `FENCE_*`, and `eis-sitl.parm` is never loaded on this path | The only FC-side fence is the runtime upload. Since `upload_geofence` never raises and returns `False` on NACK, a failed upload leaves ArduCopter with default `FENCE_ENABLE` and no containment while the UI claims native fence authority. | critical | possible | ✔ `upload_geofence` is attempted at connect and, on success, sets `FENCE_TYPE=5`/`FENCE_ENABLE=1`; failure surfaces as a warning `statusText`. `demo.sh:152-190`, `274-279`; `app.py:1481`, `1493-1495`; `vehicle.py:1276-1277` | The warning is advisory — nothing refuses arming or dispatch, and no assertion checks `FENCE_ENABLE == 1` before takeoff. The contract's firmware-backstop row is unmet on this path. |
| `FM-155` ✱ | The native SITL fallback fences the aircraft into a 60 m circle at a 30 m ceiling on an ~800 m site | Docker unavailable, so `demo.sh` runs `sim/run_sitl.sh`, which loads `eis-sitl.parm` (`FENCE_TYPE 3`, `RADIUS 60`, `ALT_MAX 30`, `ACTION RTL`) against a ~720×880 m geofence with a 20–80 m band | The first outbound leg, or any leg above 30 m, breaches and ArduPilot RTLs mid-demo with firmware authority — looking exactly like a real safety event. The LiDAR-fail climb to 45 m guarantees it. | critical | likely | ✔ `sim/params/eis-sitl.parm` now carries FENCE_RADIUS 1000 m and FENCE_ALT_MAX 80 m -- numbers that CONTAIN the Komati site rather than the launch pad -- and the companion re-derives FENCE_* from its Limits and the loaded perimeter at connect (`_site_containment_radius_m` -> `apply_failsafe_params`), then switches FENCE_TYPE to 5 with the site polygon. companion/tests/test_containment.py::test_the_sitl_param_file_contains_the_site_it_flies | Closed. A test now reconciles the .parm numbers against site.stub.json, so the two cannot drift apart again silently. |
| `FM-156` ✱ | Companion `sitl.yaml` `max_altitude` is raised to 80 m with a stale comment claiming 30 m | `sitl.yaml` sets `limits.max_altitude 80.0` while its own planner comment block and `default.yaml` still say 30.0 | Readers get the wrong ceiling; combined with `eis-sitl.parm`'s `FENCE_ALT_MAX 30` there is a 50 m gap between what the companion commands and what the firmware tolerates. | medium | possible | ✔ The stale planner note in `sitl.yaml` is corrected (80 m, not "30"), and the 3S `cell_count` carries the reason it differs from default.yaml's 4S: SITL simulates a 12.6 V pack. Every voltage-derived FC threshold is DERIVED from that number and pushed at connect, so the firmware and the companion cannot describe different packs. `config/sitl.yaml`, `config/vehicle2.yaml`, `sim/params/eis-sitl.parm` | Closed for the disagreement; the two configs still legitimately differ, and now each says why. |
| `FM-157` ✱ | The acceptance gate exercises a different SITL transport and parameter set than the demo | `run-sim-e2e.sh` uses native `run_sitl.sh` over `udp:14550` with `eis-sitl.parm`; `demo.sh` prefers Docker over `tcp:5760` with bootstrap params and a vicon serial | A green e2e gate is not evidence the demo path works, and vice versa — fence behaviour, EKF source config, transport and param provenance all differ. | high | likely | ✖ Sim/CI-side. **DOCUMENTED (this wave)** in `docs/runbook.md` §Stage 0.5: a green acceptance gate is necessary, not sufficient — run the path you are about to show, on the machine you are about to show it on. | Unchanged: `run-sim-e2e` still does not share the demo's backend selection, and nothing runs the demo path in CI. |
| `FM-164` | Root pytest `testpaths` silently excludes every companion test | Root `pyproject` sets `testpaths = ["tests"]` while the companion suite lives under `platform/companion/tests` | `pytest` or `make test` from the merged root collects none of the companion's ~216 tests and reports success. | medium | likely | The CI companion job invokes the path explicitly. Root `pyproject.toml`; `Makefile`; `.github/workflows/ci.yml`; `BASIC_DEMO_PENDING.md:22` | Root `make test` is a documented quickstart command and gives a false all-clear. |
| `FM-175` | Two CI workflows collide and the wrong resolution silently disables all jobs | `.github/workflows/ci.yml` exists at the same path in both layouts, differing only in the `platform/` prefix | Resolving in favour of the root-relative copy leaves jobs `cd`-ing into directories that no longer exist — they error out or pass vacuously. | medium | possible | The platform-prefixed copy is correct end to end, and [`REPOSITORY_REVIEW.md`](REPOSITORY_REVIEW.md) lists each required edit. `.github/workflows/ci.yml:19`, `30`, `64-78`; `REPOSITORY_REVIEW.md:76-83` | Neither copy adds a planner or satellite job, so the merge cannot fix the `FM-158` coverage hole either way. |

---

## 16. Repository integration — two stacks, one tree

Modes that exist because the drone platform and the Hub/Console/argus-core stack
occupy one repository with independently authored contracts, layouts and
launchers.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-152` ✱ | Two directory layouts coexist — every root-relative path is wrong on one side | One branch keeps the stack at the repository root; the other places it under `platform/` | Mass path breakage at merge (root `sim/`, `scripts/`, `docs/`, `Makefile`, `README`, `.gitignore`, `.github` all conflict), and afterwards every doc or script still naming `scripts/demo.sh`, `make e2e`, `ground/ui` or `companion/config` points at nothing. | critical | likely | ✔ [`REPOSITORY_REVIEW.md`](REPOSITORY_REVIEW.md) prescribes the move and lists the CI edits; the platform-prefixed CI is already re-pointed, and sibling-relative code is preserved by moving the runtime as one unit. `REPOSITORY_REVIEW.md:54-85` | Nothing enforces the layout: no test, lint or CI check fails on a stale root-relative path, and keeping a root-relative `ci.yml` silently breaks all three jobs. |
| `FM-153` ✱ | SITL TCP port 5760 is claimed by both stacks | One launcher publishes Docker SITL on `127.0.0.1:5760` while the other starts ArduCopter instance 0 on `tcp:127.0.0.1:5760` | Whichever starts second fails (Docker port allocation error, or the fleet tears down). Worse, this companion can connect to the *other* stack's SITL and fly a different aircraft with this site's waypoints. | critical | likely | **None.** `demo.sh:171`, `276`, `281`; `scripts/launch_sim.py:36`, `83`, `88-89` | `launch_sim.py` has `--instance-base` but the Docker port is hardcoded, and nothing checks the port is free first. |
| `FM-160` ✱ | `Detection` (Hub) and `Anomaly` (this stack) are structurally incompatible with no adapter | The Hub's `Detection` is a polygon model with `extra="forbid"` and required `before_ref`/`after_ref`; `Anomaly` is a point with `lat`/`lon`/`thumbnail`/`source` | Nothing can move a cue either way without hand-written conversion; a hand-rolled POST is rejected 422 on three counts at once. | high | likely | ✔ **MITIGATED on this side (this wave).** `ground/planner/src/hub_adapter.ts` is the mapping as CODE: `parseHubDetection` validates an untrusted record against the Hub's own constraints (rejecting extra fields the way `extra="forbid"` does), `anomalyFromHubDetection` collapses the polygon to its centroid and returns the footprint, refs and `change_type` the point form cannot hold, and `hubDetectionFromAnomaly` REFUSES rather than inventing the `before_ref`/`after_ref` the Hub requires. `planner/test/hub_adapter.test.ts` covers both directions, the round trip and every refusal (24 cases) | The adapter is not yet wired into a transport — nothing POSTs a Detection or ingests one — and the Hub side has no counterpart. `intruder_vehicle` → `vehicle` is a documented lossy direction; `DetectionExtras.changeType` round-trips it. |
| `FM-161` ✱ | Two rival `Detection` schemas inside the teammate tree | `contracts/models.py` (Hub) and `argus-core/contracts/models.py` both define `Detection` under the same import name with different polygon shapes and extra fields | An argus-core-native `Detection` posted to the Hub is rejected 422 (`contract_version` forbidden, `[lat, lon]` pairs fail `LatLon`). | high | possible | ✔ `scripts/argus_detect.py` is a correct adapter: it never imports argus-core's contracts, rebuilds polygons as objects, clamps confidence and maps labels to `ChangeType`. `scripts/argus_detect.py:39-61`, `155-170` | The adapter is one script; any other producer reaching for argus-core's validator gets the wrong shape with no import-time signal. |
| `FM-162` ✱ | Python package-name collisions across three roots | argus-core ships `agent`/`planner`/`safety`/`contracts`; the mock agent ships `agent/`; the root ships `contracts/`; `hub/autonomy.py` mutates `sys.path` at import time and the root `pyproject` declares wheel packages that do not exist | Whichever tree lands first wins the name: argus-core on the path shadows the mock agent and the Hub fails at import. The phantom packages also break root `uv sync`, step one of the documented quickstart. | high | possible | ✔ `argus_detect.py` refuses to put argus-core on `sys.path` and loads modules by file path, citing this hazard; the rule is stated in `REPOSITORY_REVIEW`. `hub/autonomy.py`; `argus-core/{agent,planner,safety,contracts}/__init__.py`; `pyproject.toml` | `hub/autonomy.py` still mutates `sys.path` unconditionally with nothing guarding ordering, and no test imports both trees in one interpreter. |
| `FM-163` ✱ | Dependency pins conflict between the root project and the companion | Root requires `pymavlink>=2.4.49`, `websockets>=13`, `numpy>=1.26`, Python ≥3.12; the companion pins `pymavlink==2.4.41`, `websockets==12.0`, `numpy==1.26.4`, Python ≥3.10 | A shared environment either fails resolution or upgrades the companion off its pins. `websockets` 13 moved the server API, so the control WebSocket the whole demo runs on breaks. | high | possible | ✔ They are separate installs and `demo.sh` hard-requires `companion/.venv/bin/python`. Root `pyproject.toml`; `companion/pyproject.toml:28`, `36-40`; `demo.sh:85-89`, `196` | Nothing prevents a root `uv sync` inside the companion venv; `demo.sh` checks the venv exists but never verifies versions. |
| `FM-170` ✱ | The argus-core vision seam requires a live hosted-model call, contradicting the offline-demo claim | `argus-core/vision/detect.py` drives a hosted model with a key from the environment, and `argus_detect.py` calls it in every mode including `--dry-run` | The wide-area detection leg cannot run without network and a key. On venue wifi it hangs on a 60 s timeout while the README promises a fully offline demo. | high | likely | ✔ The dependency is disclosed in the script docstring and the seam commit, and the Hub also has a numpy-only detector needing no network. `argus-core/vision/detect.py`; `scripts/argus_detect.py:16-17`, `73`, `144` | `--dry-run` still makes the network call, there is no fixture-replay mode, and nothing routes to the offline detector when the key is absent. |
| `FM-171` ✱ | Console 3D assets are fetched from the internet at runtime and are gitignored | `console/public/assets/` is ignored and populated by `scripts/fetch_assets.py` from a remote CDN | A fresh clone at an offline venue renders the Three.js console with no ground texture, sky or models — bare or throwing. | high | possible | ✖ Not ours to fix: `console/` is the teammates' stack and nothing in `platform/ground/` depends on its assets. **DOCUMENTED (this wave)** in `docs/runbook.md` §Stage 0.5 with the three mitigations in preference order (fetch before losing connectivity and verify on the PRESENTING machine; hand-copy a populated `console/public/assets/`; present the 2D mission map, which is fully offline — baked tiles in the renderer bundle, cue rails on bundled fixtures). The ARGUS World view is an `<iframe>` and is the only view affected. | Unchanged upstream: still no offline bundle, no fallback material and no preflight asset check in `console/`. The operator now knows the symptom and the fallback before the venue. |
| `FM-172` ✱ | Two demo entry points give conflicting instructions and only one can be right | One README instructs root `scripts/demo.*`; the other instructs `uv sync`/`make hub`/`make sim` plus a separate platform block, and the root `Makefile` has no `e2e` or `demo` target | Following the wrong README from the merged root runs nothing, and `make e2e` is "No rule to make target" — a dead minute, or the wrong stack launched under stage pressure. | high | likely | ✔ The integrated README warns that two runtimes coexist and points at [`BASIC_DEMO_PENDING.md`](BASIC_DEMO_PENDING.md), telling readers to run platform commands from the platform root. `README.md:14-37`; root `Makefile` | The "pick one" decision is unfilled, so both READMEs remain live and contradictory. |
| `FM-173` ✱ | Core companion and demo modules exist in a working tree but in no published branch | Seven companion modules, five test files, both demo launchers, staging PNGs and `docs/BOM.md` untracked, with eleven tracked files modified | Any clean clone, CI run or teammate merge produces a system with no battery health, failsafe, fusion, nav health, thermal, LiDAR or multimodal — and no demo launcher. | critical | likely | ✔ (companion + sim half) Every companion module, its tests, the site fixtures and `sim/params/eis-sitl.parm` are tracked on the integration branch and covered by the suite; `git status --short` is clean for `platform/companion/**`. Modules this catalogue prescribes behaviour for are now runnable from a clean clone. | Ground/demo-launcher half is outside this agent's scope and is REPORTED rather than claimed. |
| `FM-174` | Two workflows commit into one working tree concurrently | Two agents or sessions operating on the same checkout while a sibling worktree holds a divergent branch | Any analysis, edit or merge races the other: edits are lost against files being rewritten, and parallel merges of the same branches yield divergent conflict resolutions. | high | likely | ✔ The two branches occupy separate worktrees, and merge state (`MERGE_HEAD`/`MERGE_MSG`/`rebase-merge`/`CHERRY_PICK_HEAD`) is checked before writing. `git branch -a -vv` | Nothing serialises the two workflows, and a large branch divergence means the resolving merge is attempted under time pressure. |
| `FM-176` ✱ | Zero-area and low-confidence `Detection`s pass every gate at the argus seam | `argus_detect.py` defaults `--min-confidence` and `--min-area-m2` to `0.0`; a degenerate bbox still yields four corners and area `0.0` | Every model finding is posted and stored. A `MissionSpec` built on a zero-area survey polygon expands into a degenerate `FlightPlan`, and the operator's list fills with noise. | medium | possible | Confidence is clamped into 0..1 before posting, and unmapped labels degrade to `unknown` with the original in metadata. `scripts/argus_detect.py:91-98`, `119-120`, `151`, `158`; `contracts/models.py` | `Detection` declares `area_m2` as feeding a minimum-area gate whose default admits everything; no validator rejects a zero-area polygon. |
| `FM-177` | Model-authored prose crosses into operator-facing surfaces as `Detection.metadata` | The vision model's free-text description is packed into metadata, stored by the Hub, published on `/ws/live` and rendered by the Console; autonomy mirrors LLM plan reasoning the same way | Model-authored text reaches an operator display and sits next to an approve control, so content inside a rendered image could steer what the operator reads at the decision point. | medium | rare | Layered and deliberate: the prompt forbids obeying in-image text, the Hub docstring states metadata is data and nothing acts on it, posting authorises nothing, and dispatch still passes the Safety Validator. `scripts/argus_detect.py:19-21`, `164-169`; `hub/detections.py`; `hub/server.py:23`, `45` | Enforced by policy and prose, not code: nothing strips or marks the text at the render boundary, and the Console does not distinguish model-authored fields. |

---

## 16b. Ground ↔ companion seam after the deterministic-planner wave

Modes raised by the cross-agent review of the merged tree. They share one shape:
the two halves of a seam were written against different versions of it, and the
ground half is what these rows are measured against.

Three of them (`FM-182`, `FM-183`, `FM-184`) need a change to the WIRE contract —
`ground/ui/src/contract/index.ts` plus both mirrors, `shared/shared.ts` and
`shared/shared.py`, in one commit. That change is not made here: the mirrors are
the companion workstream's, and a half-applied contract change is a worse failure
than the one it fixes. Each row states exactly what the change is, so it can be
made once, atomically, by whoever owns all three files.

| id | Mode | Trigger | Effect | Sev | Lik | Mitigation | Gap |
|---|---|---|---|---|---|---|---|
| `FM-179` | Hub fleet mapper fabricates vehicle position `(0, 0)` and a null sortie | `HubDataProvider.onFleet` mapped Hub `FleetEntry` rows into contract `FleetVehicle`s | The rows carried altitude but no lat/lon, so the mapper filled the hole with `{lat: 0, lon: 0}` — a real point in the Gulf of Guinea that the verifier's `deconfliction` check and the mission map both consume as the peer's true location. | high | likely | ✔ **MITIGATED (this wave).** `HubDroneState` already carries lat/lon on every frame; `publishFleet` now carries them onto the row, and `onFleet` OMITS any vehicle whose position is not finite rather than inventing one. A vehicle missing from the fleet is visibly missing; a vehicle at a fabricated coordinate is a peer the separation check believes in. The Hub-native `onFleetRows` stream still carries every row, so the vehicle selector is unaffected. `dataSource/HubDataProvider.ts`; `ui/test/hub-fleet.test.ts` (6 cases) | `sortie` is still `null`: the Hub reports which mission a drone flies but not its `must_rtl_by`, and the time-overlap test needs a deadline. An unknown deadline is reported as no sortie clock, never as a guess. |
| `FM-180` | `CueBus` was not wired into any UI data source | `ground/cues` exports a `CueBus` multiplexing seven rails onto `anomaly` + `healthEvent`; nothing in `ground/ui/src/dataSource` consumed it | The sar / sdr / rf_drone / cctv / fence_sensor / drone_survey rails never reached the map or the task panel: the only cue in the UI was the scripted optical one. | high | likely | ✔ **MITIGATED (this wave).** `ui/src/cues/index.ts` is the renderer-side counterpart of the package's Node factory (which reads its site, normalcy and fixtures off disk): it builds a `CueBus` from bundled fixtures with no filesystem and no Electron. Wired into MockDataProvider (six rails; the scripted scenario IS the optical rail, so wiring its fixture too would raise the same cue twice) and into LiveDataProvider (all seven — the rails are ground-side producers and are deliberately independent of the vehicle link). Rail health rides the existing `healthEvent` channel; `railHealth()` adds the per-rail view the coarser `HealthComponent` cannot express. One `RAIL_PIN_COLOUR` table drives the map pin, the cue marker and the badge. No wire message added. `ui/src/cues/index.ts`; `MockDataProvider.startCueRails`; `LiveDataProvider.startCueRails`; `MissionMap.tsx`; `SatellitePanel.tsx`; `ui/test/mock-mission.test.ts` §FM-180 | The `cctv` rail runs in `event` mode only; `pixel` mode needs calibrations the renderer does not load. The bus's `onRejection` reasons reach the log, not a badge. |
| `FM-181` | UI mission flow still used the legacy `ScriptedPlanner` path | The UI was built against the pre-wave planner API: the deterministic planner now emits `corridor`/`planTrace` and the verifier emits `attended`/`deconfliction`, and `PlannerProposeResult` changed shape | Corridor band, plan trace, attendance and separation were never shown from their real producers — they were re-derived by `scriptedRails.ts` and, for the two checks, the scripted stand-ins REPLACED the verifier's own. The renderer's `PlannerProposeResult` declaration still promised a non-optional `plan`/`verification` and an LLM `source`, so it typechecked against a shape the host cannot return. | high | likely | ✔ **MITIGATED (this wave).** `MockDataProvider.proposePlan` routes triage → `planMission` → `verifyMission` (or the host's `planner:propose`, which runs the same pipeline), and returns null on an infeasible task rather than inventing a plan to display. `verifierContext` now carries the fleet, the attendance mode and `now`, so the REAL verifier produces `attended` and `deconfliction`; `dressVerification` overlays only the operator-PRESENCE rule, which is a ground-station fact the verifier has no input for. `withCorridorAndTrace` is a no-op for deterministic plans. The peer's corridor comes from the same planner. `ScriptedPlanner` survives for exactly one thing: the deliberately-invalid plan the verifier must refuse (DEMO_RUNBOOK R1). `vite-env.d.ts` now mirrors `service.PlannerProposeResult`. `ui/test/mock-mission.test.ts` (11 cases) | `scriptedRails.ts` still exists for the corridor/trace of the scripted bad plan and for the operator-presence check. Its header's "delete this file" condition is now half met. |
| `FM-182` | Ground never stamps `recordHash` on the mission record sent with `executePlan` | The companion verifies `security.record_hash` over the record (`sig`/`hash`/`prev`/`recordHash` excluded) before letting the envelope monitor run | The companion refuses dispatch as "monitor input stale" or, if lenient, the monitor runs against a record nobody verified — the contract row "Monitor input stale — mission record hash mismatch" cannot be satisfied from this side. | critical | likely | ◐ **HALF-DONE (this wave).** `ground/planner/src/security.ts` is the byte-for-byte TypeScript mirror of `security.py`'s `record_hash`/`chain_hash`, including the excluded fields, recursive key sorting, `ensure_ascii=False` and the `undefined`-drops-like-Python rule; it refuses a non-finite number rather than emitting `null` where Python emits `Infinity`, and it carries its own SHA-256 so the renderer (which has no Node builtins) can use it too. `planner/test/security.test.ts` checks it against hashes GENERATED by the companion's own helper (7 cases) plus the published SHA-256 vectors. | **The wire half is not done and is deliberately left out of this commit.** `Command.params` has no `missionRecord` field in any of the three mirrors, so there is nowhere to stamp the hash. The change is: add `missionRecord?: MissionRecord & { recordHash: string }` to `Command.params` in `contract/index.ts`, `shared/shared.ts` and `shared/shared.py` in ONE commit, then call `stampRecordHash` at the `executePlan` seam in Live + Mock. |
| `FM-183` | Command envelope has no `sig`/`nonce`/`ts` fields in the contract mirrors | The companion's `CommandVerifier` accepts only signed commands (HMAC-SHA256 over the canonical payload plus nonce and timestamp, session key per vehicle from the environment) | Every live command is rejected as unsigned, except the `SAFE_DIRECTION_COMMANDS` exemption. | critical | likely | ◐ **PARTIAL (this wave).** The canonicalisation and the hash primitive the signer needs are in `ground/planner/src/security.ts` and are parity-tested against the Python side, so the remaining work is HMAC plus three field additions rather than a from-scratch implementation. | **The wire half is not done and is deliberately left out of this commit.** Add `sig`, `nonce` and `ts` to `Command` in all three mirrors + the Mock's ack path in ONE commit; `LiveDataProvider` then signs with the per-vehicle session key from the environment and `HubDataProvider` passes through. Note `security.py:SAFE_DIRECTION_COMMANDS` — `emergencyStop`, `disarm`, `land`, `rtl`, `disengageManual`, `disengageTracking`, `abortPlan` — must stay accepted unsigned: refusing an emergency stop for want of a signature is a worse failure than the DoS it prevents. |
| `FM-184` | `FleetMessage` is inbound-only; the companion expects the hub to relay it to each vehicle | ADR D26's star topology: the envelope's separation constraint needs peer positions at 5 Hz from the ground | The envelope monitor's separation constraint never engages, because peer state never reaches the vehicle. | high | likely | ◐ The ground side already HAS the data: `MockDataProvider` publishes a full two-vehicle `FleetMessage` at the envelope cadence, and `HubDataProvider` derives one from Hub state (with real positions since `FM-179`). | **The wire half is not done and is deliberately left out of this commit.** Declare `fleet` as a ground→companion OUTBOUND message in all three mirrors in ONE commit; `LiveDataProvider` then forwards it to each connected vehicle at 5 Hz. Until then the verifier's ground-side `deconfliction` check is the only separation gate, and it runs before dispatch rather than continuously. |
| `FM-185` | `FM-87`/`FM-89`/`FM-90` cited planner symbols the deterministic wave deleted | `LlmPlanner`, `MISSION_PLAN_INPUT_SCHEMA` and `createLlmPlanner` were removed when plan generation moved to `deterministic.ts` (ADR D20) | Stale evidence in this catalogue: a reader following the citations finds nothing, and cannot tell whether the mode still exists. | low | likely | ✔ **MITIGATED (this wave).** All three rows rewritten against `planner.ts` (the orchestrator), `triage.ts` and the surviving `llm.ts` surface — `TASK_LIST_SCHEMA` and the incident report, both schema-bound, neither able to emit a coordinate. Each row now records that the model cannot author a plan at all. `docs/FAILURE_MODES.md` §11 | Nothing mechanically checks a citation against the code, so the same drift can recur on the next wave. |

---

## 17. Verified mitigations

Every mitigation claim on a `high` or `critical` mode was **adversarially
code-checked**: the claim was treated as a hypothesis to be broken, not a fact to
be confirmed. The check consisted of reading the cited code, following each
control to the specific call site that reaches it on the production path,
confirming the guard's predicate actually excludes the failing input, and — where
the claim was mechanical — executing the real module against the failing input.
Claims below `high` were recorded as written and **not** checked; their table
cells carry no marker and should be read as assertions.

Forty-three claims survived that audit and are marked `✔` in their table row.
Fourteen did not; those rows carry `✗` and the mode is counted as unmitigated in
[§18](#18-open-gaps).

Mitigations added by the **ground FMEA wave** are marked `✔ **MITIGATED (this
wave).**` in their rows and cite the code and the regression test that pins them.
They were written to close the mode, not to argue it away, and each names what
remains in its `gap` cell. A row marked `◐` is HALF done: the part that needs no
wire change is implemented and tested, and the `gap` cell states exactly which
contract change the rest requires. Three modes this wave could not fix
(`FM-135`, `FM-157`, `FM-171`) belong to launcher, CI and `console/` — they
carry `✖` and are documented in [`runbook.md`](runbook.md) §Stage 0.5 with the
operator's move instead.

### Claims upheld (`✔`)

| Severity | Modes |
|---|---|
| `critical` | `FM-39`, `FM-41`, `FM-68`, `FM-76`, `FM-95`, `FM-130`, `FM-152`, `FM-154`, `FM-165` |
| `high` | `FM-27`, `FM-44`, `FM-45`, `FM-49`, `FM-53`, `FM-54`, `FM-55`, `FM-57`, `FM-67`, `FM-74`, `FM-75`, `FM-77`, `FM-80`, `FM-96`, `FM-97`, `FM-102`, `FM-105`, `FM-107`, `FM-111`, `FM-120`, `FM-133`, `FM-136`, `FM-137`, `FM-158`, `FM-159`, `FM-161`, `FM-162`, `FM-163`, `FM-166`, `FM-168`, `FM-170`, `FM-172`, `FM-174`, `FM-178` |

`✔` means the control exists, is reachable, and does what the claim says. It does
**not** mean the mode is closed — every `✔` row still carries a `gap`, and
several (`FM-53`, `FM-54`, `FM-130`, `FM-165`) are upheld only because a demo
script or config choice happens to select the safe branch.

### Claims refuted (`✗`)

Each of these was recorded as a mitigation and does not survive inspection. The
mode is unmitigated.

| id | What the claim said | Why it fails |
|---|---|---|
| `FM-03` | Hold frame, critical `statusText`, planner release, and FC `FS_GCS_ENABLE` as the backstop | `_on_deadman` sets `_control_source = "auto"`, and both `SafetyManager.evaluate_link` and `_actual_datalink_lost()` gate on that same source set — so the trigger silences itself one tick after firing. `_deadman_latched` has four writes and zero reads. The `critical` status is a `statusText`, which `broadcast` drops when there are no clients (there are none — the link is down) and which is never replayed on reconnect. `FS_GCS` times the *companion→FC* heartbeat, which this process keeps pumping at 1 Hz throughout, so the firmware never sees a loss; the 20 Hz zero-velocity stream also suppresses ArduPilot's stale-setpoint fallback. `failsafe_param_map`, which would set `FS_GCS_ENABLE`, has no runtime caller. |
| `FM-04` | The early return is deliberate, so the idle zero-velocity frame cannot overwrite the `NAV_TAKEOFF` target | The rationale covers one of the things the return suppresses and the code suppresses all of them. The `rtl` branch does `_release_all` + critical status + `set_mode("RTL")` *before* any hold frame — a mode change cannot overwrite a takeoff target — and the LiDAR branch emits an active climb setpoint, not an idle one. Correct shape is to evaluate the ladder and gate only the emission. The window is also longest exactly when a climb is failing, and no path (`rtl`, `land`, `disarm`, e-stop, `_release_all`) clears it. |
| `FM-06` | `planner_exec._clamp_setpoint` collapses non-finite to hold and `Vehicle._finite` coerces to `0.0` on the wire | Two different functions on two different paths. `planner_exec`'s guard is reached only from the planner branch; manual and tracking hit `app.py`'s clamp, which gates only on `sp.valid`. `Vehicle._finite` runs *after* the clamp has already converted `NaN` into a finite maximum, so it is unreachable dead code here. Executed: a four-axis `NaN` setpoint clamps to `(2.0, 2.0, 1.5, 45.0)` — full forward, full right, full descent, full yaw. |
| `FM-07` | Standoff re-asserted three times in guidance; `estimate_distance` rejects `None`/tiny/`≤0` bboxes | All three re-assertions are the same predicate `est_distance <= limits.standoff`, which is `False` for `NaN`; three copies of one NaN-blind comparison fail identically. `estimate_distance` rejects `None`, negatives and tiny values — every one of those tests is also `False` for `NaN` — so it returns `NaN`, breaking its own `Optional[float]` contract. Executed against the real `Guidance.update()`: `vx` climbs `+0.80 → +1.28 → +1.57 → +1.74 → +1.84 → +1.91`, asymptoting to `+max_speed`. Controls confirm `None` yields `0.0` and an inside-standoff distance backs off, so `NaN` is the single worst input. |
| `FM-08` | `fc_heartbeat_age_s` is surfaced as `link.latencyMs`; FC `FS_GCS_ENABLE` RTL fires | `fc_heartbeat_age_s` has exactly two references: its definition and its own cosmetic use in the telemetry dict. No failsafe field, no threshold, no consumer. The `datalink_lost` signal is the *ground* WebSocket, not the FC link. On the degraded path `_telemetry_from_state` hardcodes `latencyMs: 0.0` — a perfect link readout when the link is dead. `FS_GCS` fires when the FC stops *receiving*, so it cannot cover the RX-only failure this mode describes, and nothing pushes it anyway. |
| `FM-11` | Only the floor is enforced; `_sync_safety_limits` propagates the change | `_sync_safety_limits` is vacuous: `SafetyManager` and `Vehicle` are constructed with the same live `Limits` object and `update_limits` is a bare rebind of that same reference, so the re-bind is a no-op and cannot be a safety control. "Only the floor" is not a safe posture: the branch returns before `_execute_plan`, so the verifier and `envelope.py` never see it; `float(args.get("radius"))` has no `isfinite` guard (plain JSON `1e400` parses to `inf`); a large standoff makes the three "forbid positive `vx`" guards permanently true while a `-max_speed` retreat passes through untouched; the mutation is process-wide and never restored by any of the nine `_planner_engaged = False` sites; and the ack reports `accepted`, not `clamped`. |
| `FM-12` | `executePlan`/`engageTracking`/`engageManual` enforce exclusion; the manual branch wins the tick | `planCommand` is a distinct wire type routed to `_handle_plan_command`, whose follow/orbit branch engages and returns before `_execute_plan`'s manual guard is reachable. "Manual wins the tick" describes only the ticks while manual is still engaged; the failure lands on release, because `_disengage_manual` never clears `_planner_engaged`. One tick after the operator lets go, guidance flies an unauthorised follow/orbit leg — with the ground-link deadman silenced, because `_control_source` is now `auto`. This is verbatim the hazard `_abort_plan`'s own docstring says must never happen. |
| `FM-14` | Deliberate so a SITL param quirk cannot block startup; pinned by a test | "Deliberate and tested" documents the hazard rather than mitigating it. Both `set_param` returns are discarded and the function returns `True` regardless; `set_param` matches the echo on `param_id` only and never reads `param_value`, so even an acknowledged write proves nothing. The operator-visible signal is byte-identical whether the fence is armed or inert. The `FENCE_TYPE` rewrite to 5 drops the circle bit, so a partially applied write can leave *less* containment than before. The named backstop (`failsafe_param_map`) has no runtime caller. |
| `FM-15` | Loud warning `statusText`; upload never raises; scalar `FENCE_RADIUS`/`FENCE_ALT_MAX` remain | The warning covers only transfer-level failure — the failure that matters least. The param-enable failure, which decides whether the fence is enforced at all, produces only a log line. `FENCE_TYPE 5` clears the circle bit, so `FENCE_RADIUS` is written but inert and only `FENCE_ALT_MAX` survives, which does not bound a lateral excursion. "Gates nothing downstream" is wholly unmitigated: the `ok` flag is a local, `envelope.py`'s breach logic is imported only by tests, and `goto_global` clamps speed and altitude but never lat/lon against the perimeter. `FENCE_ACTION` is never set at all. |
| `FM-17` | Climb rate clamped through `_clamp_setpoint`; stops at `clear_altitude_m`; `rtl`/`land`/`disarm` still accepted | A rate clamp is not an altitude ceiling — `_clamp_setpoint` has no altitude term, and the body-velocity path is the one path with no `max_altitude` clamp (takeoff, goto and planner legs all have one). "Stops at `clear_altitude_m`" *is* the defect, because nothing compares that value to `limits.max_altitude`. The second clause of the mode is untouched by the claim: a real LiDAR failure never sets the clear latch, so the hold-and-climb latches permanently with no path back to the mission. |
| `FM-18` | `emergencyStop` and `continueMission` dispatch before the gate | Neither escape covers the three commands named. `continueMission` returns "no hostile-drone hold is latched" unless the hostile latch is set, and the notification-only escalates this mode is about never set it. `emergencyStop` works but is a LAND/BRAKE plus e-stop latch — not a substitute for a graceful abort. Meanwhile plain `escalate` is deliberately not in the hold set, so the plan keeps commanding motion while the operator's least-disruptive stop commands are refused. |
| `FM-19` | `require_cell_telemetry`/`require_temperature_telemetry` produce readiness reasons; in-flight SoC is monotonically non-increasing | Wrong predicate: both flags test *absence*, and a frozen cache is populated, so no reason is produced. Both are hard-disabled under `sitl`. Readiness is nulled by `readiness_ok = ready or armed` and is only consulted on the ground anyway. Monotonicity is vacuous: a dead pack reporting the `current_battery == -1` sentinel yields `current_a = 0.0`, so the integrator subtracts nothing and `min()` never lowers SoC. The sibling `nav` dict emits three age fields from the same `_msg_ts` map; the `battery` dict emits none. |
| `FM-24` | `_release_all` + hold frame + critical status before the vehicle action; FC-side LAND/disarm is the real stop | The latch is set and cleared in one coroutine and `_dispatch` has no e-stop gate, so `setMode GUIDED`, `takeoff` and every engage are accepted immediately afterwards. The "real stop" is `set_mode("LAND")` with no ACK wait and no readback, whose return value is discarded inside a bare `except`, so a dropped SET_MODE returns `success: true, "emergency stop -> land"` having stopped nothing. With no `try/finally`, a raise before the clear leaves the latch stuck on and no LAND ever commanded. The land-vs-kill branch keys on `airborne`, which is `False` whenever `GLOBAL_POSITION_INT` is absent. |
| `FM-25` | Startup `healthEvent`s report each rail; `executePlan` refuses night/clutter when a rail is not ok; fail-closed | The fixture verdict is clobbered ~100 ms after boot: `_perception_tick` sets `_camera_observation_valid = True` on any successful `observe()`, and the SITL sim source cannot fail. The health messages are not startup-latched — they are built lazily per WebSocket client, so an operator connecting after the first perception tick is told the camera is ready. There is no RGB refusal anywhere, and the two refusals that exist read `params` keys the contract and `App.tsx` never send (`FM-55`). The LiDAR rail's `initial_health` never opens a fixture at all. |

---

## 18. Open gaps

> **Cleared in the ground FMEA wave.** `FM-42` · `FM-43` · `FM-50` · `FM-51` · `FM-60` · `FM-72` · `FM-73` · `FM-79` · `FM-81` · `FM-82` · `FM-83` · `FM-109` · `FM-123` · `FM-125` · `FM-131` · `FM-132` · `FM-142` · `FM-148` · `FM-149` · `FM-160` were closed and are no longer listed below; their table rows carry the
> mitigation and its evidence. `FM-135`, `FM-157` and `FM-171` are not this
> stack's to fix and are now documented in [`runbook.md`](runbook.md) §Stage 0.5
> with the operator's move; they remain listed. `FM-182`, `FM-183` and `FM-184`
> are in [§16b](#16b-ground--companion-seam-after-the-deterministic-planner-wave)
> and each need a change to all three contract mirrors in one commit.
> Counts below are therefore no longer exact; the table rows are authoritative.

Ordered by severity. Every unmitigated `critical` and `high` mode appears in
full; `medium` and `low` are listed compactly. Before the ground wave the counts
were **13 critical**, **31 high**, **18 medium**, **8 low** — 70 of 178 modes
with no mitigation. Twenty of those are now closed (see the note above), so the
totals are lower; the section headings no longer carry a count, because a count
that drifts out of date is worse than none. The table rows are authoritative.

### Critical, unmitigated

| id | Lik | Gap | Smallest correct fix |
|---|---|---|---|
| `FM-153` ✱ | likely | Both stacks hardcode SITL on `tcp:127.0.0.1:5760`; the second to start fails, or this companion flies the other stack's aircraft. | Parameterise the Docker port, probe it before binding, and stamp a vehicle identity the companion verifies on connect. |
| `FM-155` ✱ | likely | The native SITL fallback loads a 60 m radius / 30 m ceiling fence against an ~800 m site with a 20–80 m band. | Derive the `.parm` fence values from the selected site file, or refuse to start when they do not contain it. |
| `FM-173` ✱ | likely | The modules this catalogue describes exist in a working tree and in no published branch. | Commit them; add a CI job that imports every module the contract table names. |
| `FM-01` ✱ | possible | RTL is latched before `set_mode`, with no verification, no retry and no de-latch on failure. | Latch only after an observed mode change; re-assert until `mode == RTL`. |
| `FM-02` ✱ | possible | `set_mode` returns `True` unconditionally — no `COMMAND_ACK`, no `HEARTBEAT.custom_mode` readback. | Mirror the arm/takeoff pattern already in the same file: wait for the ACK, then confirm the mode from the heartbeat. |
| `FM-05` | rare | `manualInput` accepts `NaN`/`Infinity` from the wire and `_clamp01` saturates them to full deflection. | `json.loads(raw, parse_constant=_reject)`; `isfinite` check on the four axes at ingest; make `_clamp01` reject non-finite. |
| `FM-06` | rare | `app.py`'s `_clamp_setpoint` has no finite guard, so a `NaN` axis becomes the positive maximum on all four axes. | Port `planner_exec.py:660`'s `isfinite`-else-hold guard into `app.py`; make `_clamp` NaN-safe in all three copies. |
| `FM-07` | rare | Every standoff guard is a comparison, none is `isfinite`; `tracker.py` has no `isfinite` anywhere. | Reject non-finite in `estimate_distance` and on `lt.height`; add `not isfinite(...)` to the three guidance predicates. |
| `FM-13` | possible | `site.py` parses `nfz` but no runtime code consumes it; only the perimeter is uploaded as a fence. | Wire the existing `control/envelope.py` breach logic into `planner_exec` and the control tick. |
| `FM-40` ✱ | possible | The control WebSocket has no token, origin check or loopback bind, and arms `testFault` including `raw_out_of_fence`. | Bind `127.0.0.1` by default and require a shared token; gate test hooks on an explicit non-default flag. |
| `FM-100` | possible | A failed camera read returns `[]` and marks the observation **valid**, violating the observation-state invariant. | Return an explicit invalid-frame result from `_CameraSource.observe` and let it set `_camera_observation_valid = False`. |

### High, unmitigated

| id | Lik | Gap | Smallest correct fix |
|---|---|---|---|
| `FM-03` ✱ | likely | The ground-link deadman silences its own trigger and never escalates; `_deadman_latched` is never read. | Latch link loss in orchestrator state, drop the `control_source` gate from `_actual_datalink_lost`, re-assert RTL until observed, and emit a `healthEvent` rather than a `statusText`. |
| `FM-04` ✱ | likely | A takeoff suspends the whole failsafe ladder for up to 60 s and no path clears the window. | Evaluate the ladder first; gate only the `_send_hold`/`_send_setpoint` emission. Clear the deadline on `rtl`/`land`/`disarm`/e-stop. |
| `FM-09` ✱ | likely | Blocking `recv_match` waits run on the event loop; there is no `asyncio.to_thread` on the MAVLink path. | Offload every blocking MAVLink call, or give `_maybe_await` a thread path for sync callables. |
| `FM-16` ✱ | likely | `failsafe_param_map` has no runtime caller, so the configured envelope never reaches the FC. | Call it at connect and assert the readback; fail readiness if the push fails. |
| `FM-18` ✱ | likely | Any failsafe state, including notification-only escalates, refuses `abortPlan`, `disengageTracking` and `setMode`. | Add the two release-to-safe commands to the allow-list; separate "escalate" from "control-actioning" states in the gate. |
| `FM-98` ✱ | possible | The live detector is person-only, so vehicle/structure truths silently produce zero observations. | Assert at startup that the resolved backend can produce every truth label the site declares; fall back to the stub when it cannot. |
| `FM-99` | possible | The observation rail is scripted from site truth with no provenance flag on the wire. | Add a `synthetic: true` field to the observation message and render it in the UI and report. |
| `FM-101` | possible | `PersonDetector(conf=…)` raises `TypeError`, and a bare `except` reports it as "no hardware". | Fix the keyword; narrow the `except` so a construction bug is distinguishable from a missing device. |
| `FM-118` ✱ | possible | Staging-frame ranges are published as `DISTANCE_SENSOR` and a 72-bin isotropic `OBSTACLE_DISTANCE` fan. | Do not publish proximity from staging fixtures; if it stays, transform to the vehicle frame and reject isotropic or out-of-range fans. |
| `FM-134` ✱ | possible | The `sim_vehicle.py` branch passes `--no-mavproxy`, so its `--out=udp:14550` endpoint never exists. | Drop `--no-mavproxy` on that branch, or select the TCP transport whenever it is taken. |
| `FM-135` ✱ | possible | The WSL/native backend never runs the param bootstrap the fault demos depend on. | Move the bootstrap outside the Docker branch and verify the params after start. |
| `FM-157` ✱ | likely | The acceptance gate and the demo run different transports, params and fence behaviour. | Make `run-sim-e2e` use the demo's backend selection, and run the demo path in CI. |
| `FM-171` ✱ | possible | Console 3D assets are gitignored and fetched from a CDN at runtime. | Bundle an offline asset set or a fallback material, and add an asset check to preflight. |
| `FM-08` | possible | Nothing consumes `fc_heartbeat_age_s`; there is no `fc_link_lost` signal and `read_telemetry` re-stamps `ts`. | Add `fc_link_lost` to `FailsafeSignals` from `_msg_ts`, and stamp telemetry with the sample time, not `now()`. |
| `FM-10` ✱ | possible | The EKF-source request re-issues every tick with no backoff, counter or give-up latch. | Add exponential backoff and a give-up latch that raises a health event. |
| `FM-11` | possible | `planCommand` writes `limits.standoff` with no ceiling, no finite guard and no restore. | Clamp to a configured maximum, reject non-finite, and restore on disengage. |
| `FM-12` | possible | The `planCommand` follow/orbit branch has no manual, site-valid or readiness check. | Apply the `was_engaged` release idiom used by `_disengage_tracking` and `_abort_plan`. |
| `FM-14` | possible | Fence `set_param` results are discarded and matched on `param_id` only. | Capture both returns, read the value back, and degrade the operator status to a warning when either fails. |
| `FM-15` ✱ | possible | Fence state gates nothing: no arm/plan gate, no retry, no `FENCE_ENABLE == 1` assertion. | Store the fence result and refuse arm/dispatch when the fence is not verified enabled. |
| `FM-17` | possible | `clear_altitude_m` is never intersected with `limits.max_altitude`, and a real LiDAR failure never clears the latch. | Clamp the climb target to `min(clear_altitude_m, max_altitude)` and set the clear latch from the real health rail, not the injected flag. |
| `FM-19` | possible | No age field on the battery rails, unlike GPS/EKF/extnav. | Emit `battery_age_s` from `_msg_ts`, add `age_s`/`batt_max_age_s` to the sample and policy, and latch a fault (not a readiness reason) in flight. |
| `FM-24` | possible | The e-stop latch self-clears in the same call and `_dispatch` has no e-stop gate. | Keep the latch until an explicit operator acknowledgement; verify the LAND mode change. |
| `FM-25` ✱ | possible | Fixture health is clobbered by the perception tick and is reported per-client rather than latched. | Latch the startup verdict separately from the live flag, validate per staging point, and decode-check assets. |

### Medium, unmitigated

`FM-20` ✱ (no `WIND` presence/staleness distinction — the wind ladder has no live
detector) · `FM-46` ✱ (reserve subtracted twice; the reason string hides it) ·
`FM-69` ✱
(planner site model memoized for the app lifetime) · `FM-112` (outbound backpressure protection does not exist) · `FM-113` ✱
(synthetic structure box fragments below the cluster gate) · `FM-115` (LiDAR
class vocabulary is one hardcoded string) · `FM-116` (fence-gap debounce
structurally unreachable) · `FM-122` ✱ (vehicle/structure boxes emitted as
`person` tracks) · `FM-139` ✱ (no legacy `SIM_GPS_DISABLE` fallback) · `FM-156` ✱ (config pairs disagree on
ceiling and cell count) · `FM-169` ✱ (site-contract example diverges from the
stub; referenced images may be absent).

### Low, unmitigated

`FM-36` (no `allow_nan=False` on outbound JSON) · `FM-117` (fence-gap `width_m` is a point
count) · `FM-119` (synthetic LiDAR has unlimited range and no occlusion) ·
`FM-121` (stub confidences unreachable for the real backend) · `FM-127` (overlapping staging radii swallow
observations) · .

---

## 19. Demo-day contingency

One row per `✱` mode: what it looks like from the audience's side, and the move
that recovers it. Rows are ordered by when in a run the mode can first appear.
Where the recovery is "restart", the row says what to change first so the restart
is not wasted.

### A. Preflight, build and launch

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-83` | "Planning failed" toast the moment the inspection button is pressed; no verdict, no checks. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Build the planner (`ground/planner` install + build) before the run. Mid-demo: fall back to the mock data source, which uses the in-process scripted planner. |
| `FM-132` | Launcher aborts with "Ground station exited unexpectedly" seconds after start. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Compile the shell TypeScript, then relaunch. There is no in-run workaround. |
| `FM-130` | Everything looks nominal but the aircraft on screen never matches SITL; no socket is ever opened. | Confirm the MOCK/SITL/LIVE chip in the status bar reads SITL. If it reads MOCK on a packaged build, run the dev-server path instead. |
| `FM-131` | The ground-station window renders another app's page, or Hub calls 404. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Free port 5173 before launching (close other dev servers), then relaunch. |
| `FM-133` | Long silence at wait-ready, then abort with no port named. | Close any other GCS or companion holding UDP 14550, then relaunch. |
| `FM-134` | Same silent stall on the native backend specifically. | Force the Docker backend if available; otherwise use the TCP transport rather than 14550. |
| `FM-135` | Launcher prints "preflight OK", but the GPS-loss, battery and extnav beats do nothing. | Verify the SITL params after start. If unset, restart on the Docker backend, which runs the bootstrap. See [`runbook.md`](runbook.md) §Stage 0.5 for the full pre-venue procedure. |
| `FM-136` | Arm is refused, or the aircraft breaches immediately after takeoff, for no reason the operator changed. | Wipe the SITL EEPROM volume and relaunch; confirm the fence upload status line after connect. |
| `FM-137` | Companion exits at startup; the launcher reports a readiness failure that never names a port. | Kill orphan companion processes holding 8765, then relaunch. |
| `FM-138` | Launcher aborts during param bootstrap over a single unknown parameter. | Remove the offending param from the bootstrap set and relaunch; it is cosmetic if the fault beats still work. |
| `FM-143` | Up to ~2.5 minutes of silence before "companion did not become ready". | Do not wait it out. Read the companion log for the blocking readiness reason and fix that. |
| `FM-144` | A visible pause at preflight, then "preflight OK (wsl)" and a capability-reduced run. | Start Docker Desktop before the run. If the pause appears, expect `FM-134`/`FM-135` and plan for the native path. |
| `FM-145` | Every command answers "no FC link"; failure surfaces two minutes later as a readiness timeout. | Treat "degraded mode" as fatal: stop, fix the FC connection string, relaunch. |
| `FM-146` | Launch fails mid-way with a file-in-use error on the demo log. | Close the process holding the log, or clear `logs/demo/`, then relaunch. |
| `FM-147` | Launcher aborts with a raw .NET property error instead of a site message. | Read it as "site file is malformed"; validate the site JSON and relaunch. |
| `FM-152` | Documented commands do nothing, or resolve into the wrong tree. | Run platform commands from the platform root, per the integrated README. |
| `FM-153` | One stack refuses to start, or this companion connects to the other stack's aircraft. | Start only one SITL. If both are needed, move one off 5760 before either starts. |
| `FM-154` | Fence "uploaded" is reported but the firmware has no fence — visible only if a breach is attempted. | Confirm the fence status line; if it warns, do not run the out-of-fence beat. |
| `FM-155` | The aircraft RTLs by itself on the first outbound leg or above 30 m, looking like a real safety event. | Do not run the native SITL fallback for a site this size. If it is the only option, keep every leg inside 60 m and below 30 m and skip the LiDAR-fail climb. |
| `FM-156` | The companion commands an altitude the firmware will not tolerate. | Confirm the effective ceiling from the running config, not the comment; keep legs below the lower of the two numbers. |
| `FM-157` | A green acceptance gate that proves nothing about the path being demonstrated. | Do a full dry run on the demo path itself; treat the gate as necessary, not sufficient. See [`runbook.md`](runbook.md) §Stage 0.5 for the full pre-venue procedure. |
| `FM-171` | The 3D console renders bare — no ground texture, sky or models. | Fetch the assets before going offline; otherwise present the 2D map view instead. See [`runbook.md`](runbook.md) §Stage 0.5 for the full pre-venue procedure. |
| `FM-172` | A minute lost following instructions that run nothing, or the wrong stack launched. | Fix the entry point on the run sheet in advance; name one launcher and one root. |
| `FM-173` | A clean clone or a teammate's machine produces a system with no failsafe, battery, fusion or perception modules. | Demo only from the working tree that has them. Commit before anyone else clones. |
| `FM-16` | Nothing visible until a limit is tested; the firmware then does not enforce the envelope the UI advertises. | Do not present ArduPilot as the backstop unless the params were flashed. State the software limit as the authority. |

### B. Configuration, site identity and cross-stack seams

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-27` | Arm is refused and every command is locked out with a degraded-battery escalate. | Pin the SITL config so `cell_count` matches the simulated pack; relaunch. |
| `FM-40` | An unexpected command executes with no approval — or a stray peer masks a real link loss. | Run the venue network isolated, or bind the companion to loopback. Do not enable test hooks on a shared LAN. |
| `FM-67` | The vehicle arms while the ground station refuses every mission, or vice versa. | Use the shipped stub site, which satisfies both validators. |
| `FM-68` | The map, the verifier and the aircraft disagree about where the site is, silently. | Always launch through the demo script, which exports an absolute `EIS_SITE_FILE`. Never launch components by hand. |
| `FM-69` | The map updates after a site edit and the verifier keeps using the old geometry. | Restart the ground station after any site change. Do not edit the site file mid-run. |
| `FM-159` | A Python/TypeScript wire mismatch surfaces as a silently missing field. | Do not change contract types on demo day. If a field is missing, check `shared.py` against the TypeScript mirror. |
| `FM-160` | A cue posted between stacks is rejected 422, or never appears. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Do not demonstrate cross-stack cue transfer without the adapter; show each stack's own detection path. |
| `FM-161` | The same rejection with a confusing schema error. | Use the adapter script as the only producer into the Hub. |
| `FM-162` | The Hub fails at import, or root `uv sync` fails outright. | Install and run the two stacks in separate environments; do not put argus-core on the shared path. |
| `FM-163` | The control WebSocket breaks after an environment "fix". | Never run a root dependency sync inside the companion venv. Keep the venvs separate. |
| `FM-165` | The aircraft spawns thousands of kilometres from the site, or plans are geographically meaningless. | Run one named site scenario end to end. Confirm `SITL_HOME` matches the selected site before takeoff. |
| `FM-166` | Every mission is refused, or the other launcher dies before any SITL starts. | Keep the demo single-vehicle and use the single-site file on this stack. |
| `FM-167` | The projected console shows pads, fences and no-fly circles that the aircraft does not honour. | Regenerate the site artefacts before the run; prefer the launcher path that regenerates automatically. |
| `FM-168` | A relative site path resolves to a different file than the companion used. | Use an absolute `EIS_SITE_FILE`; never rely on the documented default from a different working directory. |
| `FM-169` | A staging arrival produces "no observation" because a referenced image is absent. | Verify every image the site file names exists before the run. |
| `FM-170` | The wide-area detection leg hangs for ~60 s on venue wifi. | Use the offline numpy detector, or pre-record the detection result; do not run the hosted-model seam live. |
| `FM-176` | The detection list fills with zero-area noise; a survey polygon degenerates. | Set non-zero `--min-confidence` and `--min-area-m2` before the run. |

### C. Ground station up, before a plan exists

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-49` | Every plan is rejected with "RF events and SDR health state are required". | Check the SDR sidecar started. The message names the SDR but the fault is usually the sidecar; restart the ground station. |
| `FM-51` | An early hostile-drone event keeps failing `airspace` for the rest of the session. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Restart the ground station to clear the event window before the mission beat. |
| `FM-89` | The offline demo makes network calls, or live mode silently does not engage. | Confirm the resolved planner mode before starting; launch through the demo script, which sets it explicitly. |
| `FM-94` | A question about the LLM control that the documented answer does not match. | Describe the control that exists: a closed tool union plus schema whitelist. Do not cite the verification record. |
| `FM-107` | The anomaly sits at a stale coordinate over a tile footprint that no longer matches the site. | Regenerate the satellite tiles after any site change, before the run. |
| `FM-108` | Mock and live runs disagree about where the anomaly is. | Pick one data source for the whole demo and rehearse on that one. |
| `FM-109` | The anomaly marker renders outside the tile image with no explanation. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Re-anchor the anomaly to a staging point, or narrate the map instead of the tile. |
| `FM-111` | The UI stalls while tens of MiB of base64 move through IPC. | Keep the baked (thumbnail-free) satellite path; do not enable live detection with thumbnails. |
| `FM-140` | The map is a bare CSS grid, or tile requests hang on captive-portal wifi. | Expect the OFFLINE badge; markers, trail, geofence and coordinates still render. Narrate from those. |
| `FM-149` | The entire ground-control window vanishes shortly after start. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Ensure a working `python`/`python3` is on PATH before launch. Relaunch; the sidecar starts automatically. |
| `FM-151` | A browser window pops over the full-screen ground station. | Close it and avoid clicking map attribution links. |

### D. Propose → verify → approve

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-72` | Every plan comes back rejected on wind and Approve never enables. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: The blocking check is wind, not the plan. Run the mock data source, which supplies `windMps`. |
| `FM-80` | The ground station appears frozen for up to ~40 s with no progress or cancel. | Wait; it self-recovers to the scripted planner. Prefer scripted mode on stage. |
| `FM-81` | After one rejection the inspection button stays disabled ("Inspection planned") forever. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Restart the ground station. There is no in-app reset. |
| `FM-82` | ~3 s freeze on the first proposal, exactly when the narrative points at the planner. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Press the inspection button once during setup to warm the module, then reset before the demo. |
| `FM-85` | The button says "scripted" whether or not the LLM ran; the core claim is unprovable on screen. | Read the source from the audit log line, and say which planner ran rather than pointing at the UI. |
| `FM-86` | A plan is approved and then refused by the companion as "plan too large". | Do not run a degenerate model output live. If it happens, re-plan; the loaded plan is untouched. |
| `FM-92` | Live mode silently produces scripted plans after the full latency cost. | Set the model id explicitly, or run scripted and say so. |
| `FM-93` | The fallback plan is itself rejected, so an API outage yields no plan at all. | Select an anomaly well inside the geofence and clear of NFZs before starting. |
| `FM-97` | A live call fails and the reason string is a raw error. | It falls back automatically; narrate the fallback. Do not diagnose the error class on stage. |
| `FM-79` | Slower proposals than rehearsed, especially on retry. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Nothing in-run. Prefer scripted mode on the critical path. |
| `FM-73` | All 18 checks green on a plan that flies to the wrong place. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Read the plan's target coordinate against the anomaly on the map before approving. |
| `FM-74` | A green `corrected` verdict beside red check rows, including an unexplained red proximity row. | Read the original→corrected diff in the panel and confirm the corrected coordinate before approving. |
| `FM-60` | "✎ clamped …" annotations appear next to failed checks on a rejected plan. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Ignore the annotations on a rejected verdict; no corrections were applied. |
| `FM-64` | Standoff shows ✓ on a plan with a goto target metres from the subject. | Check goto targets by eye; the standoff check only covers explicit orbit radii. |
| `FM-66` | "All legs clear buffered NFZs" on a site model whose NFZ list is empty. | Confirm the site file actually declares NFZs before citing the check. |
| `FM-44` | The mission flies out, holds, and never returns or completes. | Abort the plan, or wait for the sortie/battery RTL. Avoid plans containing an unbounded hold. |
| `FM-45` | The ground says a plan fits after the aircraft has already been flying. | Land and reset the sortie before the mission beat; do not approve a plan mid-sortie. |
| `FM-46` | A physically fine mission is rejected on range, more often at lower SoC. | Start the mission beat on a full pack. The reason string understates the effective reserve. |
| `FM-57` | A green PASS that is minutes old and no longer reflects wind, battery or airspace. | Re-propose if more than a few seconds pass between verdict and approval. |
| `FM-59` | A mis-clicked deny permanently disables approval with nothing sent to the vehicle. | Do not use deny during the demo. If pressed, restart the ground station. |
| `FM-39` | A green PASS on an orbit below the 3 m standoff floor. | Sanity-check the orbit radius against the floor before approving. |
| `FM-43` | The toast announces a fresh plan while the stale one is what gets sent. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Do not re-plan the same anomaly. If a re-plan is needed, restart the ground station first. |
| `FM-42` | "Mission approved" and a drawn mission that never launched. | **Closed in the ground FMEA wave** — this should no longer fire. Previous move: Confirm launch from telemetry (`controlSource` and motion), not from the UI banner. |
| `FM-55` | The night/clutter refusal is described as double-enforced; only the ground half fires. | Present the ground verifier as the authority for these two rules. |
| `FM-56` | A plan verified at high SoC dispatches at low SoC with no re-check. | Approve and dispatch promptly after the verdict. |

### E. In flight

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-01` | Telemetry says `failsafeState: rtl` while the aircraft hovers in GUIDED. | Command RTL manually. Do not wait for the automatic retry — there is none. |
| `FM-02` | A commanded mode change appears to succeed and nothing happens. | Confirm the mode from telemetry after every mode command; re-issue if it did not change. |
| `FM-03` | The link drops, the aircraft holds, and no RTL or failsafe state ever appears. | Restore the link, or command RTL from a second client. Treat the hold as terminal. |
| `FM-04` | A failsafe injected during climb does nothing until the aircraft levels off. | Run fault injections only after the climb completes and the ladder is live. |
| `FM-09` | Telemetry visibly stutters and setpoints go stale during a fault beat. | Expect it around GPS-source and param operations; do not inject two faults at once. |
| `FM-10` | Telemetry drops to a crawl after a GPS-loss injection. | End the GPS beat (restore GPS) to stop the request storm. |
| `FM-15` | The fence is announced as live while the firmware may hold none. | Read the fence status line after connect. If it warned, skip the containment beat. |
| `FM-18` | Abort and disengage are refused during a camera/thermal/battery escalate while the plan keeps flying. | Use `emergencyStop`, or engage manual and then disengage, to release the plan. |
| `FM-20` | The wind beat produces no hold and no RTL. | Use the scripted `--wind` injection; the organic detector has no data. |
| `FM-21` | The aircraft holds indefinitely after a GPS-loss/fallback beat, and abort is refused. | This is the specified `hold`. Command RTL to exit. |
| `FM-105` | The mission stops advancing mid-air with everything reading nominal. | Abort the plan and command RTL; do not wait for the sortie backstop minutes later. |
| `FM-106` | A leg advances early, or an observation fires at the wrong place, on one noisy fix. | Nothing in-run. Note it and continue; the leg is already consumed. |
| `FM-118` | With avoidance enabled, the aircraft refuses to move in any direction after an observation. | Confirm no `PRX`/`AVOID` params are enabled before the run. If it happens, disengage the plan and fly manually. |
| `FM-129` | Inference looks live but every result is empty or scripted. | Confirm the detector backend before the run; narrate the stub as a stub. |
| `FM-139` | The `--gps-loss` beat aborts the whole scheduled fault sequence. | Confirm the firmware supports `SIM_GPS1_ENABLE` before the run; otherwise drop the GPS beat from the sequence. |
| `FM-141` | A "link loss" beat produces no watchdog trip, because another client is feeding the heartbeat. | Close every other client — browser tabs, capture scripts, headless clients — before the link-loss beat. |

### F. Observation, report and disposition

| id | On-stage symptom | Operator recovery |
|---|---|---|
| `FM-25` | Night or clutter missions are refused, or a rail is escalated, for points whose own assets are fine. | Verify every staging asset exists and is a real PNG/JPEG before the run. |
| `FM-26` | The camera escalation appears and disappears; command acceptance becomes erratic. | Do not interleave the camera-fail beat with staging arrivals. |
| `FM-75` | The report reads convincingly and may describe evidence that was never captured. | Cross-read the report's claimed modalities against the observation panel before dispositioning. |
| `FM-98` | Arrival at a vehicle or structure point produces no detection at all. | Use the stub backend for the demo, or a person-truth staging point. |
| `FM-103` | The operator log and audit fill with duplicate observations, once per lap. | Keep the orbit radius at or below the staging arrival radius. |
| `FM-104` | Tracking flaps between locked and idle, with repeated identical observation bursts. | Avoid crossing the staging radius outward and back; keep one arrival per point. |
| `FM-113` | A structure point yields zero LiDAR evidence while LiDAR health still reads ok. | Use a vehicle- or person-truth point for the multimodal beat. |
| `FM-114` | A structure point always escalates on modality disagreement, regardless of evidence. | Narrate this as the fixture's disagreement, not a sensor finding — or use a different truth label. |
| `FM-122` | A parked vehicle renders as a locked person target. | Read the class from the observation panel, not the tracking overlay. |

## Runtime assurance, unattended mode and fleet (Phases 2–3)

Corridor geometry and tolerances are ADR D21; monitor policy per constraint is
ADR D22; `UNATTENDED_ENVELOPE` is ADR D23; the escalation adapter is ADR D24; cue
rails are ADR D25; the two-vehicle star topology is ADR D26. The columns, the
one-state-per-row rule, and the authority invariant above apply unchanged to every
row in this section.

Two conventions specific to this section. **Warning-and-slow is not a failsafe
state**: drift inside 2× tolerance reduces commanded speed and raises a warning,
and only the rows below change state. **Recovery is hysteretic**: a condition
clears only after the measurement returns inside tolerance by a margin and stays
there, so a vehicle riding the tolerance edge does not flap between states.

| Failure | Phase | Detection | Response | Authority | SITL/scripted injection | Required e2e assertion |
|---|---|---|---|---|---|---|
| Corridor breach | In flight, first trip | Cross-track error exceeds 2× lateral tolerance (10 m `inspect` / 15 m `survey`), or orbit radial error exceeds 2× 5 m, or altitude leaves the plan's band | `hold` | Companion monitor | `--gust SECONDS` pushes the vehicle off the corridor | State becomes `hold` within 5 s of the 2× crossing, the setpoint is zeroed, and the audit entry names the breached constraint, the measured error and the tolerance |
| Corridor breach | Recovery | Error returns inside tolerance by the hysteresis margin and holds for the recovery window | `hold` | Companion monitor | End the injected gust | The vehicle leaves `hold` only after the hysteresis window, resumes the same plan, and the audit shows one entry and one exit — no state flapping |
| NFZ buffer or geofence margin breach | In flight | Position enters an NFZ polygon buffered by `nfz_buffer_m`, or crosses the geofence margin | `rtl` | Companion monitor | `--guidance-override SECONDS` commands toward the switchyard NFZ | RTL begins within 5 s, no position sample lies inside the unbuffered NFZ or outside the geofence, and the plan cannot resume without a new verified dispatch |
| Persistent breach | In flight, breach unresolved | Any monitor trip above persists 5 s | `escalate` | Companion monitor and ground escalation adapter | Hold the injected gust or override past 5 s | One escalation is raised within 1 s of the 5 s mark, carries `vehicleId`, the constraint and the measured excursion, and the vehicle's `hold`/`rtl` state is unchanged by the escalation |
| Monitor input stale — mission record hash mismatch | Dispatch | The mission record hash the monitor holds does not match the verified plan's hash, or the audit chain has a gap | `refuse` | Companion validator | Mutate the plan after verification, or drop an audit link | Dispatch is refused with a hash-mismatch reason, no setpoint is issued, and the refusal names both hashes; the monitor never runs against an unverifiable record |
| Unattended task outside envelope | Unattended dispatch | Task violates any `UNATTENDED_ENVELOPE` constraint — outside perimeter, non-`inspect` profile, altitude outside 30–50 m, more than one lap, hold > 15 s, third sortie in the hour, `navSource != gps`, RF interference, hostile drone, night without healthy thermal, or wind above half the attended limit | `refuse` | Ground verifier and companion validator | `--unattended` with a fixture violating each constraint in turn | Dispatch is refused, the refusal names the violated constraint and its envelope value, and no arming occurs; refusal happens with the operator absent, not merely without approval |
| Unattended task outside envelope | Operator notification | The refusal above is raised while no operator session is connected | `escalate` | Ground escalation adapter | Same fixtures with `--operator-absent` | An escalation is raised for every envelope refusal, reaching the control-room destination first, and the escalation is retained in the outbox when no channel is available |
| Operator absent, approval expired | Attended dispatch | No operator session, or the approval covering this task has passed its expiry | `refuse` | Ground verifier | `--operator-absent` after issuing a short-lived approval | Dispatch is refused with an expired-approval reason, an expired approval cannot be reused or extended by any wire message, and entering unattended mode still requires a signed command |
| Operator absent, escalation unacknowledged | Escalation chain exhausted | No acknowledgement at any destination through the full chain | `escalate` | Ground escalation adapter | `--operator-absent` for the whole chain duration | Every step fires at its configured time and destination, the vehicle RTLs on its own sortie/battery timer regardless of acknowledgement, the same cue is not re-flown, and no party outside the site is contacted |
| Escalation delivery failure | Any escalation | A channel's retries are exhausted without delivery | `escalate` | Ground escalation adapter | Fail the scripted channel, then each stub channel | Retries occur on the configured schedule, a `healthEvent` of `escalation_undelivered` is emitted, the incident remains in the local outbox, and further unattended dispatch is refused while it is undelivered |
| VMS down | Cue ingest | The CCTV rail's VMS connection fails or its events stop inside the freshness window | `escalate` | Ground cue adapter (`cctv`) | Stop the scripted VMS event source | The `cctv` rail reports `failed` (never `healthy`, never `unknown`-as-nominal), a health event names the rail, every other rail continues to produce cues, and no cue is synthesised for the failed rail |
| Camera offline | Cue ingest | One camera's health fails while the rail is otherwise up | `escalate` | Ground cue adapter (`cctv`) | Mark one fixture camera offline | The zones covered by that camera are reported blind and shown as blind, no dispatch originates from a blind zone, and coverage elsewhere is unchanged |
| Miscalibrated camera | Cue ingest | A pixel-derived cue projects outside the operational geofence, or calibration validation fails | `refuse` | Ground cue adapter (`cctv`) | Corrupt the fixture camera's heading/FOV/range calibration | The cue is rejected without dispatch, a calibration warning names the camera, and the invalid projection never reaches triage or the planner |
| Peer data stale | Separation widened | Peer telemetry age exceeds 3 s; the separation requirement becomes 80 m | `hold` | Companion monitor | `--peer-stale SECONDS` with `SECONDS` between 3 and 10 | The separation requirement widens to 80 m within one tick; the vehicle holds rather than closing inside the widened separation, and continues only while the widened separation is satisfied |
| Peer data stale | Beyond 10 s | Peer telemetry age exceeds 10 s | `hold` | Companion monitor | `--peer-stale 12` or longer | The vehicle holds unconditionally within 5 s regardless of geometry, and does not resume until fresh peer data restores the age below 3 s under hysteresis |
| Ground station down, two vehicles airborne | Loss detected | Both vehicles lose the ground and planner heartbeats; peer data ages past its limits with no relay | `hold` | Companion watchdog, each vehicle independently | Terminate the hub with both vehicles on active legs | Each vehicle holds within 5 s on its own watchdog, neither waits on the other, separation is evaluated from last-known peer data at the widened requirement, and no vehicle-to-vehicle path is used |
| Ground station down, two vehicles airborne | Hold timer expires | Each vehicle's own hold/sortie/battery timer reaches its return trigger | `rtl` | Companion watchdog and ArduPilot firmware | Keep the hub down past both timers | Each vehicle RTLs on its own timer, the two returns are independent and need no coordination, and reconnecting the hub resyncs state without replaying commands |
| One vehicle lost | Fleet, other vehicle airborne | One vehicle's heartbeat and telemetry disappear beyond the loss timeout while the other flies | `escalate` | Ground fleet monitor | Drop one vehicle fixture permanently mid-mission | The operator alert names the lost `vehicleId`, the surviving vehicle continues its plan under the widened 80 m separation against the peer's last-known position, and no automatic reallocation of the lost vehicle's task occurs |
| Allocation loop | Task allocation | A task cannot be allocated because each candidate vehicle is excluded by a different constraint, and re-evaluation would repeat the same exclusions | `refuse` | Ground fleet allocator | Construct a two-vehicle fixture where each vehicle is excluded for a different reason | The task is refused once, the refusal names **both** exclusion reasons with the `vehicleId` each applies to, and the allocator does not re-enter the loop or hand the decision back to the LLM |

### Section invariants

- **The monitor constrains; it never guides.** Every row above is the monitor
  observing a plan being flown and changing state; no row produces a new
  trajectory, a new waypoint, or a relaxed limit.
- **Envelope and corridor values are not configurable downward into unsafety.**
  A profile or configuration may tighten a tolerance, a band or a sortie cap; it
  can never widen one past the hard envelope, and a configuration that tries is a
  `refuse`, not a clamp.
- **Every row carries `vehicleId`** in its wire message and its audit entry, so a
  fleet event is always attributable to a specific vehicle rather than to "the
  fleet".
- **Escalation never changes the flight state.** An escalation is a message to
  humans; the vehicle's `hold`/`rtl` is decided by the companion and remains
  decided by the companion whether or not anyone reads it.
- Demo flags (`--gust`, `--guidance-override`, `--operator-absent`, `--bad-plan`,
  `--handoff`, `--peer-stale`) are the injection surface for these rows and are
  listed with their demo beats in [`docs/DEMO_RUNBOOK.md`](DEMO_RUNBOOK.md).

---

## 20. Companion remediation wave

This section records the wave that closed the companion-owned rows above. It is
authoritative where it contradicts [§17](#17-verified-mitigations),
[§18](#18-open-risk-by-severity) or [§19](#19-demo-day-contingency): those
sections describe the tree BEFORE this wave, and their entries for the ids listed
here are superseded by the `✔` cells now carried in the tables.

### What changed, by theme

| Theme | Ids | The shape of the fix |
|---|---|---|
| Ground-link deadman | `FM-03` | The latch moved into orchestrator state so it survives the release to `auto` that used to silence it; the deadman acts every tick, releases through `_release_all`, re-asserts RTL until observed, emits an audited `healthEvent`, and stops the MAV_TYPE_GCS heartbeat so the firmware backstop can fire. |
| Mode changes are verified | `FM-01`, `FM-02` | `Vehicle.set_mode` confirms by COMMAND_ACK or HEARTBEAT `custom_mode` readback with retry, and the failsafe latch is stamped only once RTL is OBSERVED. |
| The ladder always runs | `FM-04`, `FM-18` | The takeoff window gates the idle setpoint EMISSION, not the ladder; release-to-safe commands (`abortPlan`, `disengageTracking`, recovery `setMode`) survive a failsafe. |
| Non-finite inputs | `FM-05`, `FM-06`, `FM-07` | Rejected at the wire, at the source and at every clamp. All four `_clamp` copies return `0.0` for a non-finite input instead of the positive maximum, and the standoff predicates test `isfinite` rather than ordering. Property tests cover the whole domain. |
| Staleness ≠ health | `FM-08`, `FM-09`, `FM-10`, `FM-19`, `FM-20` | Every rail carries its own age: FC heartbeat, battery, wind. Blocking MAVLink calls run off the event loop and cache what they consume. A refused EKF-source switch backs off and gives up. |
| Containment is armed | `FM-13`, `FM-14`, `FM-15`, `FM-16`, `FM-17`, `FM-155` | NFZs are checked at dispatch AND uploaded as firmware exclusion fences; `FENCE_ENABLE` is verified by value readback and GATES arming and dispatch; `failsafe_param_map` is applied at connect with a site-sized circle; the LiDAR climb has an altitude ceiling and a symmetric release. |
| One active source | `FM-11`, `FM-12`, `FM-24` | Standoff is a closed band that a plan gives back; the `planCommand` branch carries the same preflight as `executePlan`; the e-stop is a state that clears only on an observed disarmed-on-ground. |
| The socket is not open | `FM-36`, `FM-40`, `FM-112` | Loopback by default, widening forces signing, every frame type is authorised before it counts as liveness, outbound JSON is always parseable, and a stalled client is evicted rather than buffered. |
| Perception honesty | `FM-25`, `FM-98`, `FM-99`, `FM-100`, `FM-101`, `FM-113`, `FM-115`–`FM-122`, `FM-127` | A failed frame is "no observation", not an empty one; scripted evidence is labelled; fabricated proximity data is not sent at all; LiDAR geometry produces classified evidence a person can be corroborated by. |

### Regression coverage

Every `high`/`critical` id above is pinned by a test that FAILS on the pre-wave
tree and passes after. New files, all under `platform/companion/tests/`:

| File | Ids |
|---|---|
| `test_nonfinite_safety.py` | `FM-05`, `FM-06`, `FM-07` (property-based over NaN/±Infinity) |
| `test_failsafe_latches.py` | `FM-01`, `FM-02`, `FM-03`, `FM-04`, `FM-17`, `FM-18`, `FM-24` |
| `test_link_and_sensor_staleness.py` | `FM-08`, `FM-09`, `FM-10`, `FM-19`, `FM-20` |
| `test_containment.py` | `FM-13`, `FM-14`, `FM-15`, `FM-16`, `FM-155` |
| `test_command_surface.py` | `FM-11`, `FM-12`, `FM-40`, `FM-42`, `FM-139`, `FM-186`, `FM-20` |
| `test_perception_honesty.py` | `FM-25`, `FM-98`–`FM-101`, `FM-113`–`FM-127`, `FM-169` |
| `test_api_hardening.py` | `FM-36`, `FM-40`, `FM-112` |

Each file also carries NEGATIVE controls — a legitimate full-deflection stick, a
real distance that still approaches, a clear plan that is still accepted — so a
guard cannot pass by refusing everything.

### Design decisions recorded (not bugs, choices)

1. **`FM-19` — `readiness_ok=... or armed` is kept.** Stale pack telemetry now
   latches a *fault*, which the in-flight ladder consumes as `rtl` independently
   of readiness. Dropping `or armed` as well would additionally re-block an
   already-armed vehicle on pre-arm charge reasons, which is a different change
   with its own hazard.
2. **`FM-20` — an absent wind estimate blocks UNATTENDED dispatch only.**
   Attended flight continues (an operator is watching, and the FC keeps its own
   wind limits); the absence is reported in capabilities and health. Unattended
   refuses, because nobody can take over and the unattended limit is half.
3. **`FM-24` — the e-stop clears on a PHYSICAL acknowledgement.** The contract
   has no `clearEmergencyStop` command, so requiring an explicit operator
   acknowledgement over the wire would be a contract change. "Disarmed and on
   the ground" is the strongest acknowledgement available without one.
4. **`FM-18` — in-flight `disarm` stays permitted with no airborne guard.** It
   is the deliberate kill switch; gating it would be the more dangerous change.
5. **`FM-118` — sending nothing beats sending something.** No proximity frame is
   published from a staged observation, because the ranges are in the staging
   point's frame rather than the vehicle's.

### Still open, and why

| Id | What remains | Why it was not done here |
|---|---|---|
| `FM-99` | The `observation` message has no `provenance` FIELD; provenance rides the `scene` string. | A wire change requires all three contract mirrors plus the Mock in one commit. Not done unilaterally from the companion. |
| `FM-24` | No `clearEmergencyStop` command exists. | Same: a new command is a contract change. |
| `FM-42` | The ground half still discards the `executePlan` ack and stamps `approvedAt` regardless. | `ground/ui` is outside this agent's scope. |
| `FM-46` | The verifier's second reserve subtraction. | `ground/planner/src/verifier.ts` is outside this agent's scope. |
| `FM-25` | The night/clutter refusals key on `params.night` / `params.routeThroughClutter`, which the UI never sends. | The missing keys are a ground/contract gap. |
| `FM-134` | `run_sitl.sh`'s `sim_vehicle.py` branch passes `--no-mavproxy` alongside `--out=udp:14550`. | `sim/run_sitl.sh` is outside this agent's scope (only `sim/params` was in it). |
| `FM-160` | No `Detection` ↔ `Anomaly` adapter. | Lives in `contracts/` and `ground/satellite/`, outside this agent's scope. |
