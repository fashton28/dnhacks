# ============================================================================
# Drone Safety Platform -- COMPANION pinned runtime requirements.
#
# These mirror the PINNED core + detect dependencies in pyproject.toml. Use this
# file for a plain `pip install -r requirements.txt` (e.g. inside the Docker
# image) when you don't want the editable package install.
#
# IMPORTANT (Jetson / JetPack):
#   torch, torchvision, and TensorRT are PROVIDED BY NVIDIA JetPack (L4T) on the
#   Orin Nano. DO NOT pip-install them on the Jetson -- a pip wheel is the wrong
#   (non-CUDA) build and will break GPU inference. They are NOT listed here for
#   that reason. On a non-Jetson CUDA dev box, install them separately (see the
#   [jetson] extra in pyproject.toml) if you want GPU detection off-device.
# ============================================================================

# --- core (SITL + contract API + pure-logic control core) -------------------
pymavlink==2.4.41
websockets==12.0
numpy==1.26.4
pyyaml==6.0.1
python-dotenv==1.0.1

# --- real person detection + frame I/O (omit for the pure SITL sim path) ----
# ultralytics pulls torch on non-Jetson platforms; on the Jetson rely on the
# JetPack-provided torch/TensorRT and install ultralytics with --no-deps.
opencv-python==4.9.0.80
ultralytics==8.2.0

# --- test (optional; for the headless unit/e2e suite) -----------------------
pytest==8.2.0
